# Player update remainder literal ledger

Complete review of all **168 retained occurrences** in `src/game/player_update.c`,
including its initialized data. The [campaign review](game-player-update-constants.md)
explains the named values and retail evidence. Claims, enum definitions, comments
and string contents are excluded; signs are operators. Repeated rows preserve multiplicity.

| Function | Line | Token | Expression | Reason |
| --- | --- | --- | --- | --- |
| `initializers` | 69 | `666` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `233` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `1333` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `666` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `233` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `1333` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `666` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `233` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 69 | `1333` | `{{666, 233, 1333}, {666, 233, 1333}, {666, 233, 1333}},` | Authored Q12 darkness color coefficient: each RGB output row uses the same 666/233/1333 input-channel weights. Retail bytes at GAME 80055858 prove the tuple; the original color-tuning rationale is unknown. Keep coefficients in the matrix. |
| `initializers` | 70 | `0` | `{0, 0, 0}` | The darkness MATRIX has zero translation in each of its three SDK translation lanes; these are neutral stored components, not fade steps. |
| `initializers` | 70 | `0` | `{0, 0, 0}` | The darkness MATRIX has zero translation in each of its three SDK translation lanes; these are neutral stored components, not fade steps. |
| `initializers` | 70 | `0` | `{0, 0, 0}` | The darkness MATRIX has zero translation in each of its three SDK translation lanes; these are neutral stored components, not fade steps. |
| `initializers` | 75 | `0` | `{0, 0, 0, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 75 | `0` | `{0, 0, 0, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 75 | `0` | `{0, 0, 0, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 75 | `0` | `{0, 0, 0, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 76 | `32` | `{-32, 0, -32, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 76 | `0` | `{-32, 0, -32, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 76 | `32` | `{-32, 0, -32, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 76 | `0` | `{-32, 0, -32, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 77 | `64` | `{-64, 0, -64, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 77 | `0` | `{-64, 0, -64, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 77 | `64` | `{-64, 0, -64, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 77 | `0` | `{-64, 0, -64, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 78 | `48` | `{-48, 0, -32, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 78 | `0` | `{-48, 0, -32, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 78 | `32` | `{-48, 0, -32, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 78 | `0` | `{-48, 0, -32, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 79 | `32` | `{-32, 0, 0, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 79 | `0` | `{-32, 0, 0, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 79 | `0` | `{-32, 0, 0, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 79 | `0` | `{-32, 0, 0, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 80 | `16` | `{-16, 0, 32, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 80 | `0` | `{-16, 0, 32, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 80 | `32` | `{-16, 0, 32, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 80 | `0` | `{-16, 0, 32, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 81 | `0` | `{0, 0, 64, 0},` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 81 | `0` | `{0, 0, 64, 0},` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 81 | `64` | `{0, 0, 64, 0},` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 81 | `0` | `{0, 0, 64, 0},` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 82 | `16` | `{-16, 0, 32, 0}` | Authored damage-camera pitch offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 82 | `0` | `{-16, 0, 32, 0}` | Authored damage-camera yaw offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 82 | `32` | `{-16, 0, 32, 0}` | Authored damage-camera roll offset in angle units, in an eight-row sequence whose row zero is the neutral view. GAME 80055878 proves the signed tuple. These keyframe components stay together; the original animation tuning is unproven. |
| `initializers` | 82 | `0` | `{-16, 0, 32, 0}` | The fourth halfword is the SVECTOR SDK pad; retail stores zero. Preserve the authored lane without inventing a gameplay field. |
| `initializers` | 86 | `0` | `static u32 player_previous_input = 0;` | The saved button mask starts with no buttons set, so the first pressed button is detected as an input edge. |
| `player_update` | 129 | `1` | `collision_adjust_cell_occupancy(player_state.map_cell.x, player_state.map_cell.z, -1);` | Remove one player contribution before movement, then add one at the final cell. The signed unit delta is an occupancy count, not a collision selector. |
| `player_update` | 130 | `1` | `input = PadRead(1);` | The retail caller passes one in a0, but this version of PadRead reads the global PadIdentifier and does not consume the argument itself. Preserve the call ABI; do not invent controller-port semantics for this ignored legacy argument. |
| `player_update` | 140 | `0` | `if (item >= 0) {` | Nonnegative menu results are inventory item IDs; negative control results take the separately named load/exit branches. Zero is the signed domain boundary. |
| `player_update` | 183 | `0` | `} else if (player_state.motion_state.forward_velocity > 0) {` | Test the sign of forward_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 186 | `0` | `if (player_state.motion_state.forward_velocity < 0) {` | Test the sign of forward_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 187 | `0` | `player_state.motion_state.forward_velocity = 0;` | Clamp forward_velocity to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 189 | `0` | `} else if (player_state.motion_state.forward_velocity < 0) {` | Test the sign of forward_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 192 | `0` | `if (player_state.motion_state.forward_velocity > 0) {` | Test the sign of forward_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 193 | `0` | `player_state.motion_state.forward_velocity = 0;` | Clamp forward_velocity to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 212 | `0` | `} else if (player_state.motion_state.strafe_velocity > 0) {` | Test the sign of strafe_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 215 | `0` | `if (player_state.motion_state.strafe_velocity < 0) {` | Test the sign of strafe_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 216 | `0` | `player_state.motion_state.strafe_velocity = 0;` | Clamp strafe_velocity to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 218 | `0` | `} else if (player_state.motion_state.strafe_velocity < 0) {` | Test the sign of strafe_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 221 | `0` | `if (player_state.motion_state.strafe_velocity > 0) {` | Test the sign of strafe_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 222 | `0` | `player_state.motion_state.strafe_velocity = 0;` | Clamp strafe_velocity to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 230 | `0` | `if (magnitude == 0) {` | Zero speed selects the stationary branch and avoids division by the zero movement magnitude. |
| `player_update` | 231 | `0` | `forward = 0;` | The stationary branch supplies a zero movement component instead of dividing by the zero magnitude. |
| `player_update` | 232 | `0` | `strafe = 0;` | The stationary branch supplies a zero movement component instead of dividing by the zero magnitude. |
| `player_update` | 235 | `0` | `if (player_state.motion_state.strafe_velocity < 0) {` | Test the sign of strafe_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 239 | `0` | `if (player_state.motion_state.forward_velocity < 0) {` | Test the sign of forward_velocity to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 244 | `0` | `if (forward > 0) {` | The component sign selects the camera-relative travel direction; zero requests no horizontal move on this axis. |
| `player_update` | 246 | `0` | `} else if (forward < 0) {` | The component sign selects the camera-relative travel direction; zero requests no horizontal move on this axis. |
| `player_update` | 250 | `0` | `if (strafe > 0) {` | The component sign selects the camera-relative travel direction; zero requests no horizontal move on this axis. |
| `player_update` | 253 | `0` | `} else if (strafe < 0) {` | The component sign selects the camera-relative travel direction; zero requests no horizontal move on this axis. |
| `player_update` | 268 | `0` | `} else if (player_state.motion_state.yaw_step > 0) {` | Test the sign of yaw_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 270 | `0` | `if (player_state.motion_state.yaw_step < 0) {` | Test the sign of yaw_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 271 | `0` | `player_state.motion_state.yaw_step = 0;` | Clamp yaw_step to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 273 | `0` | `} else if (player_state.motion_state.yaw_step < 0) {` | Test the sign of yaw_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 275 | `0` | `if (player_state.motion_state.yaw_step > 0) {` | Test the sign of yaw_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 276 | `0` | `player_state.motion_state.yaw_step = 0;` | Clamp yaw_step to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 283 | `1` | `if (player_state.motion_state.pitch_step >= PLAYER_PITCH_STEP_LIMIT + 1) {` | One expresses the first integer above the inclusive positive limit; preserve the original >= boundary and signed halfword narrowing. |
| `player_update` | 291 | `0` | `} else if (player_state.motion_state.pitch_step > 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 293 | `0` | `if (player_state.motion_state.pitch_step < 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 294 | `0` | `player_state.motion_state.pitch_step = 0;` | Clamp pitch_step to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 296 | `0` | `} else if (player_state.motion_state.pitch_step < 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 298 | `0` | `if (player_state.motion_state.pitch_step > 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 299 | `0` | `player_state.motion_state.pitch_step = 0;` | Clamp pitch_step to rest after deceleration crosses zero; zero is the arithmetic origin of this signed motion quantity. |
| `player_update` | 302 | `0` | `if (player_state.motion_state.pitch_step > 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 304 | `1` | `if (player_state.camera_rotation.vx >= PLAYER_CAMERA_PITCH_LIMIT + 1) {` | One expresses the first integer above the inclusive positive limit; preserve the original >= boundary and signed halfword narrowing. |
| `player_update` | 307 | `0` | `} else if (player_state.motion_state.pitch_step < 0) {` | Test the sign of pitch_step to choose motion direction, deceleration or its zero-crossing clamp. This is a continuous signed quantity, not an enum state. |
| `player_update` | 318 | `1` | `if (player_state.weapon_attack_fully_charged == 1) {` | Encoded boolean one records a fully charged committed swing; zero consumes or clears that snapshot. It is separate from the current numeric charge amount. |
| `player_update` | 319 | `0` | `player_state.weapon_attack_fully_charged = 0;` | Encoded boolean one records a fully charged committed swing; zero consumes or clears that snapshot. It is separate from the current numeric charge amount. |
| `player_update` | 327 | `1` | `/ KF_WEAPON_ATTACK_PHASE_STEP + 1;` | Include the current eligible weapon phase in the remaining-shot count; one is the inclusive endpoint correction. |
| `player_update` | 335 | `1` | `player_state.weapon_magic_shots_remaining = 1;` | One is a literal shot count: Triple Fang/Moonlight start one cast; the last shot alone spends MP and bypasses the burst cone/jitter branch. Keep count comparisons as arithmetic. |
| `player_update` | 346 | `1` | `/ KF_WEAPON_ATTACK_PHASE_STEP + 1)` | Include the current eligible weapon phase in the remaining-shot count; one is the inclusive endpoint correction. |
| `player_update` | 355 | `0` | `player_state.weapon_magic_delay = 0;` | An ordinary selected-spell cast cancels the pending weapon-magic countdown by clearing it to zero; the counter decrements only when nonzero. |
| `player_update` | 356 | `0` | `player_state.weapon_magic_shots_remaining = 0;` | Zero clears or tests the remaining shot count; it expresses an empty sequence, independently of the separate countdown. |
| `player_update` | 358 | `1` | `cost = player_state.selected_magic_record->mp_cost >> 1;` | Wind Blade Bracelet halves Wind Cutter MP cost with an integer right shift; one is the binary half ratio, including truncation. |
| `player_update` | 365 | `0` | `player_state.magic_charge = 0;` | A successful ordinary spell empties the current magic-charge counter; zero is its numeric origin. |
| `player_update` | 366 | `0` | `player_state.weapon_attack_fully_charged = 0;` | Encoded boolean one records a fully charged committed swing; zero consumes or clears that snapshot. It is separate from the current numeric charge amount. |
| `player_update` | 390 | `0` | `if (player_state.weapon_magic_shots_remaining != 0) {` | Zero clears or tests the remaining shot count; it expresses an empty sequence, independently of the separate countdown. |
| `player_update` | 429 | `0` | `player_state.weapon_magic_shots_remaining = 0;` | Zero clears or tests the remaining shot count; it expresses an empty sequence, independently of the separate countdown. |
| `player_update` | 430 | `1` | `player_state.weapon_magic_delay = PLAYER_WEAPON_MAGIC_READY + 1;` | Cancellation clears the shots and leaves one countdown update before ready. The added one is a local counter distance from the named ready threshold. |
| `player_update` | 434 | `1` | `if (player_state.weapon_magic_shots_remaining == 1) {` | One is a literal shot count: Triple Fang/Moonlight start one cast; the last shot alone spends MP and bypasses the burst cone/jitter branch. Keep count comparisons as arithmetic. |
| `player_update` | 453 | `1` | `&& player_state.weapon_magic_shots_remaining != 1) {` | One is a literal shot count: Triple Fang/Moonlight start one cast; the last shot alone spends MP and bypasses the burst cone/jitter branch. Keep count comparisons as arithmetic. |
| `player_update` | 461 | `1` | `attachment = player_state.weapon_magic_shots_remaining & 1;` | Keep the low parity bit of the shots remaining in constructor slot seven: it gates the Light Needle launch sound on alternating shots. Fire Ball ignores this slot; do not assign it a universal homing-target meaning. |
| `player_update` | 468 | `0` | `if (target == 0) {` | Null means the cone query found no actor; only that branch supplies the named homing-wander selector. |
| `player_update` | 477 | `10` | `10, KF_EFFECT_USE_PLAYER_MAGIC \| KF_EFFECT_COLLISION_TARGET_ACTORS, effect,` | The constructor id byte is ten for all three weapon-magic calls. Effects propagate this mixed id/damage argument through child effects and damage calls; this audit does not establish one general owner identity for it. Keep it distinct from effect kind ten and from a universally typed multiplier until the per-kind contract is recovered. |
| `player_update` | 478 | `1` | `&position, &direction, &player_state.camera_rotation, attachment, 1);` | The eighth constructor slot is true: the homing kind uses it to enable the central projectile sound. Fire Ball, Light Needle and Moonlight do not consume that slot; keep this variadic boolean without inventing a universal flag. |
| `player_update` | 485 | `10` | `10, KF_EFFECT_USE_PLAYER_MAGIC \| KF_EFFECT_COLLISION_TARGET_ACTORS,` | The constructor id byte is ten for all three weapon-magic calls. Effects propagate this mixed id/damage argument through child effects and damage calls; this audit does not establish one general owner identity for it. Keep it distinct from effect kind ten and from a universally typed multiplier until the per-kind contract is recovered. |
| `player_update` | 487 | `0` | `0);` | The eighth constructor slot disables the additional Triple Fang projectile sound; the central projectile already requests it. Zero is an explicit boolean argument. |
| `player_update` | 488 | `2` | `angles.x -= 2 * PLAYER_TRIPLE_FANG_PITCH_OFFSET;` | Subtract twice the offset to move from the positive side of the center to its negative counterpart. Two is the geometric symmetry factor; preserve the mutation order. |
| `player_update` | 489 | `2` | `position.vy -= 2 * PLAYER_TRIPLE_FANG_Y_OFFSET;` | Subtract twice the offset to move from the positive side of the center to its negative counterpart. Two is the geometric symmetry factor; preserve the mutation order. |
| `player_update` | 491 | `10` | `10, KF_EFFECT_USE_PLAYER_MAGIC \| KF_EFFECT_COLLISION_TARGET_ACTORS,` | The constructor id byte is ten for all three weapon-magic calls. Effects propagate this mixed id/damage argument through child effects and damage calls; this audit does not establish one general owner identity for it. Keep it distinct from effect kind ten and from a universally typed multiplier until the per-kind contract is recovered. |
| `player_update` | 493 | `0` | `0);` | The eighth constructor slot disables the additional Triple Fang projectile sound; the central projectile already requests it. Zero is an explicit boolean argument. |
| `player_update` | 498 | `0` | `} else if (player_state.weapon_magic_delay != 0) {` | Only a nonzero countdown decrements; zero disables the pending sequence and must not wrap the stored byte. |
| `player_update` | 505 | `1` | `collision_adjust_cell_occupancy(player_state.map_cell.x, player_state.map_cell.z, 1);` | Remove one player contribution before movement, then add one at the final cell. The signed unit delta is an occupancy count, not a collision selector. |
| `player_update` | 510 | `1` | `&& player_state.darkness_timer >= DARKNESS_FADE_STEPS + 1) {` | One expresses the first integer beyond the fade-out duration; clearing the darkness flag shortens only longer timers to the named fade length. |
| `player_update` | 519 | `0` | `if (fade < 0) {` | The sign distinguishes the timed fade interval from the fully dark hold; zero is the interpolation origin, with the original counter order preserved. |
| `player_update` | 522 | `0` | `if (fade >= 0) {` | The sign distinguishes the timed fade interval from the fully dark hold; zero is the interpolation origin, with the original counter order preserved. |
| `player_update` | 540 | `0` | `if (player_state.vitals.current_hp == 0) {` | Zero HP starts death after the damage-camera sequence ends. This numeric depletion test retains the existing delayed transition. |
| `player_update` | 550 | `0` | `if (player_state.equipped_weapon_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 551 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_weapon_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 552 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 554 | `0` | `if (player_state.equipped_weapon_record->mp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 555 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_weapon_record->mp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 556 | `1` | `player_adjust_mp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 560 | `0` | `if (player_state.equipped_head_armor_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 561 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_head_armor_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 562 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 564 | `0` | `if (player_state.equipped_head_armor_record->hp_drain_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 565 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_head_armor_record->hp_drain_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 566 | `1` | `player_adjust_hp(-1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 570 | `0` | `if (player_state.equipped_body_armor_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 571 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_body_armor_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 572 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 574 | `0` | `if (player_state.equipped_body_armor_record->hp_drain_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 575 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_body_armor_record->hp_drain_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 576 | `1` | `player_adjust_hp(-1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 580 | `0` | `if (player_state.equipped_shield_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 581 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_shield_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 582 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 584 | `0` | `if (player_state.equipped_shield_record->hp_drain_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 585 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_shield_record->hp_drain_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 586 | `1` | `player_adjust_hp(-1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 590 | `0` | `if (player_state.equipped_arm_armor_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 591 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_arm_armor_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 592 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 594 | `0` | `if (player_state.equipped_arm_armor_record->hp_drain_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 595 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_arm_armor_record->hp_drain_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 596 | `1` | `player_adjust_hp(-1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 600 | `0` | `if (player_state.equipped_leg_armor_record->hp_regen_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 601 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_leg_armor_record->hp_regen_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 602 | `1` | `player_adjust_hp(1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 604 | `0` | `if (player_state.equipped_leg_armor_record->hp_drain_interval != 0` | A zero authored equipment interval disables this regeneration/drain effect and prevents a modulo-by-zero operation. |
| `player_update` | 605 | `0` | `&& player_state.equipment_effect_ticks % player_state.equipped_leg_armor_record->hp_drain_interval == 0) {` | Remainder zero triggers the effect exactly on multiples of this equipped record interval; this is the modulo phase origin. |
| `player_update` | 606 | `1` | `player_adjust_hp(-1);` | Apply one vital point of regeneration or drain at the equipped record interval. The signed unit delta is separate from that authored timing interval. |
| `player_update` | 614 | `5` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall authored damage component 0 is 5; the ordered first three channels are 5/3/5. Channel identities and original balance rationale remain unproven, so keep this tuple rather than inventing elemental/weapon damage names. |
| `player_update` | 614 | `3` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall authored damage component 1 is 3; the ordered first three channels are 5/3/5. Channel identities and original balance rationale remain unproven, so keep this tuple rather than inventing elemental/weapon damage names. |
| `player_update` | 614 | `5` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall authored damage component 2 is 5; the ordered first three channels are 5/3/5. Channel identities and original balance rationale remain unproven, so keep this tuple rather than inventing elemental/weapon damage names. |
| `player_update` | 614 | `0` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall adds no status flags and zeroes the final two damage channels; these are neutral arguments in the existing ordered damage API. |
| `player_update` | 614 | `0` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall adds no status flags and zeroes the final two damage channels; these are neutral arguments in the existing ordered damage API. |
| `player_update` | 614 | `0` | `player_apply_damage(5, 3, 5, 0, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Pitfall adds no status flags and zeroes the final two damage channels; these are neutral arguments in the existing ordered damage API. |
| `player_update` | 618 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Poison-hole contact supplies zero in every direct-damage channel and requests only the named poison status; the later timer applies its HP ticks. |
| `player_update` | 618 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Poison-hole contact supplies zero in every direct-damage channel and requests only the named poison status; the later timer applies its HP ticks. |
| `player_update` | 618 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Poison-hole contact supplies zero in every direct-damage channel and requests only the named poison status; the later timer applies its HP ticks. |
| `player_update` | 618 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Poison-hole contact supplies zero in every direct-damage channel and requests only the named poison status; the later timer applies its HP ticks. |
| `player_update` | 618 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, KF_PLAYER_DAMAGE_MULTIPLIER_ONE);` | Poison-hole contact supplies zero in every direct-damage channel and requests only the named poison status; the later timer applies its HP ticks. |
| `player_update` | 644 | `0` | `if (phase == 0) {` | The zero modulo phase is the poison damage tick itself, within the two-update flash window. |
| `player_update` | 645 | `1` | `player_adjust_hp(-1);` | Poison removes exactly one HP at phase zero of its named tick interval; one is the unit vital-point decrement. |
| `player_update` | 653 | `0` | `player_state.curse_timer = 0;` | Zero is the expiry value before the signed status timer decrements to its named inactive -1 sentinel. Preserve expiry and stat recalculation ordering; zero is not inactive. |
| `player_update` | 656 | `0` | `} else if (player_state.curse_timer == 0) {` | Zero is the expiry value before the signed status timer decrements to its named inactive -1 sentinel. Preserve expiry and stat recalculation ordering; zero is not inactive. |
| `player_update` | 665 | `0` | `if (player_state.fire_defense_timer == 0) {` | Zero is the expiry value before the signed status timer decrements to its named inactive -1 sentinel. Preserve expiry and stat recalculation ordering; zero is not inactive. |
