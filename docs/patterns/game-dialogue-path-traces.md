# GAME dialogue-path address traces

## Function Match Plan

At `030e6ae`, GAME `8002c9d4 talk_show_dialogue_page`, 164 bytes, remains
strict **98.780490%** in `game.save_system`. Fresh native/debug/traced whole ELF
parity holds for source SHA-256
`e14173a0b9d6ea9c59f8a0943043cef086e7fc8c8718c4849b7aa0706506fd1a`,
with compiler fingerprint
`222b6cc36272847ffde09ad4b7f2db81632fe7fd282bfc6df1f155939b63cb12`.
The isolated capture is `build/gcc257/game-dialogue-path-traces/`.

All six GAME views, complete retail/candidate bodies, four caller windows,
the image-display callee and adjacent actor allocator, source history,
shared enum/byte interfaces, nineteen-byte mutable path owner and the
[previous save-UI trials](game-save-ui-matches.md) were read. The four callers
load floor/stage/character/page bytes, with literal pages two or three on
the exchange paths. Preserve the signed word division by ten, byte path
encoding and void return. Game-specific dialogue naming and its game image
callee exclude a vendored body. SDK graphics/input bodies remain external.

Retail has three divide-guard branches, one direct call, seven validated
address pairs, no strings referenced as separate literals, no indirect
transfer and no candidate outgoing references. The 24-byte frame and saved
`ra` begin after the checked division; the final return delay restores the
frame. Preserve all 41 words' operations, store order, offsets and numeric
referents. The shared unit has 22 exact siblings plus the unchanged
97.980770% status-message residue.

The first difference is `mfhi v1` instead of retail `v0` at +0x30. Retail
uses `a0` for the directory address at path+6; the candidate uses `v0`.
Both write the first directory digit through that register and the second
through an independent path+7 pair, then derive the full pathname by -6
in the call delay slot. The source currently expresses both directory writes
through one pointer. Its second write becomes absolute in CSE1, while the
filename-byte addresses remain related until CSE2. At first scheduling the
outgoing pathname and directory pointer overlap before the first directory
store; no historical scheduler attribution follows from this observation.

First test spelling only the second directory byte through its complete
array owner, `talk_image_path_template[7]`. Keep the actual first-directory
pointer and every other expression fixed. This differs from the rejected
all-direct-member trial, which lost the +6 root, and from byte-digit/cursor
rewind controls already known to be ineffective. The independent retail
pair supports the access hypothesis, without proving unique original syntax.
Inspect whether the earlier address expression changes the ownership and
schedule; reject an unchanged object rather than retaining cosmetic churn.

Every trial must preserve all 23 other function bodies and ordered referents,
including the existing status residue. Require strict 100% and all retail
words, native/debug/traced parity, focused/full builds and normal verification
before applying and banking a closure. Keep the initialized path, SDK
interfaces, data inventory and compiler profile unchanged. No carrier local,
forced register, artificial stack extent or extra call is allowed.

The independent second-byte spelling is byte-identical at 98.780490%.
CSE already gives that store its absolute address in the baseline; making
the source access explicit does not resolve the earlier outgoing-argument
overlap. Reject the no-effect source change.

Next test the directory's two actual sequential byte writes with an advancing
character cursor: write the tens digit through `*directory_character++`,
then the ones digit through `*directory_character`. Keep both corresponding
filename stores and the final complete-path call unchanged. This is a real
two-byte traversal, distinct from the earlier post-write cursor rewind.
Inspect whether the first directory address and increment retain different
value ownership before scheduling; reject a new +7 base or altered address
sequence. No unused cursor step or synthetic dependency is introduced.

The advancing cursor produces **79.853660%**, 160 bytes. It retains the
wrong remainder/directory registers, folds the floor digit through directory
base+4, and moves the second directory store into the call delay slot. The
pathname is formed by two additions before the digit stores instead of the
retail call-slot subtraction. This is a different address/dataflow result,
not progress toward closure, and the candidate is rejected.

Both trials preserve all 23 other unit bodies, including the 22 exact
functions and the unchanged status-message residue. Native/debug/traced whole
ELF parity holds for the baseline and both trials. The production source,
seven dialogue referents, path owner and strict score remain unchanged.
No new function is banked here.
