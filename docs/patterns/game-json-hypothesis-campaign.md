# GAME exact campaign

## Function Match Plan: inventory preview

At d746e9c, GAME 800279c4 / 0x1b8, game.menu_item_model_preview,
strict 98.181816%. Fresh retail hash verification and full build establish
330/362 exact. Six semantic views, seven caller windows, predecessor and
successor, current source history, shared glyph types and earlier source
controls were inspected in build/preview-{evidence,context}.txt.

The void word item interface receives byte IDs or a forwarded word. Preserve
255 guard, 152-byte frame, glyph workspace plus three authentic SDK matrices,
ten calls, two branches, five CFG blocks, eight validated address pairs,
eight zero rotation bytes, and the return's frame-restoring delay slot.
There are no strings or indirect transfers. This is game menu policy;
LIBGTE RotMatrix/setters have archived exact/FID evidence, and MulMatrix0
has version-qualified library lineage. No vendor body changes.

Only the item-name stride sequence differs: retail 80027a88/8c materializes
item_name_rows before the last shift at 80027a90; current C shifts first.
All referents and remaining instructions agree. The loop reads ten halfwords
in order, advances source and destination by two, increments a signed word
counter, and branches after the store. Row extent is the existing ten-s16
MenuGlyphRow; do not replace it with a raw byte view.

Use the JSON hypotheses tool for eight real traversal forms crossed with
name selection at its original location or immediately after model rendering.
Indexed and source-cursor forms, and early selection, include previously
negative controls. New controls express destination traversal, both cursors,
bottom-tested destination traversal, an end pointer, and remaining count.
Each preserves the exact ten ordered reads/writes and real live objects.
Comparison must reject any changed CFG, call set, widths, or instruction
ordering inconsistent with retail, regardless of score. No padding, fake
locals, declaration permutations, volatile, forced registers or SDK changes.

Inspect winners from the first raw difference; retain only humane supported
source. An exact result requires a fresh canonical compile/match, all words
and ordered targets, then full build, lint/tests and whitespace checks before
banking. Full-build data ownership/placement failures predate this campaign.

### Preview verdict

Sixteen JSON states compiled, none exact. Indexed originals and early-name
controls remain 98.181816%; explicit source traversal is 97.818184%.
Destination traversal is 94.081820%, both cursors 94.036360%, source-end
94.836365%, destination-end 92.527275%, remaining-count 93.054540%.
The destination trial first moves destination/counter setup ahead of the
item base; its store uses sp+20 through a3 instead of sp+16 through a0+4.
End-pointer trials remove the retail signed-counter structure and shorten
the body. No source is retained. Generated manifest/results and 16 sources
are in build/hypotheses/20260908-155024-game-menu_item_model_preview-menu_item_model_preview.

## Function Match Plan: status message result

GAME 8002c510 / 208 bytes, game.save_system, strict 97.980770%, current
212 bytes. Fresh six views, five proven caller windows, previous buffer
initializer and following image loader, source history and earlier rejected
return trials are recorded in build/save-evidence.txt, status-context.txt
and save-compare.txt. Callers pass word statuses or 12 and ignore the return.
Preserve signed-halfword status and message conversions, 24-byte frame,
all cases/default, one table address pair, twelve internal jumps and one
menu_load_message_image call. Fifteen candidate pointer rows do not promote
the unresolved indirect jump. No strings or vendor body belong to this helper;
it selects game-authored message assets through a separate game loader.

The only extra instruction is move a0,v0 after the loader returns; the later
comparison uses a0 instead of retail v0. The successful non-one result is
forwarded unchanged; one becomes -1. Test two numeric-value ownership models
crossed with four return CFG spellings. The baseline separates halfword
message selection from the full-word result. The alternative reuses one
word numeric value through selection and loading, explicitly converting to
s16 at the call to preserve both decoded narrowing instructions. It removes
a local; it introduces no fake state, extra call or truncated loader result.
Return options are baseline early-forward, conditional assignment, exceptional
return first, and conditional expression. Earlier separate-variable controls
are deliberate baselines; the composed shared-value cases are new.

All 22 exact siblings must remain unchanged. Any winner needs strict full
function comparison, first-divergence/raw and ordered-referent audit, fresh
canonical rebuild, full build and repository checks before banking.

### Status verdict

All eight states compiled and remained partial. Separate/early-forward is
still best at 97.980770%; shared/early-forward drops to 94.711540%.
The shared-value hypothesis changes the selection prefix as well as the
return, so it is rejected. No production C or bank row changes. The unchanged
22 siblings compare identically in the focused listing. Results are in
build/hypotheses/20260908-155345-game-save_system-memory_card_show_status_message.

## Function Match Plan: actor awareness

GAME 8002e6a8 / 684 bytes, game.actor_behavior, strict 97.736840%.
Fresh six-view evidence and full caller/callee/adjacent disassemblies are in
build/awareness-{evidence,context}.txt. The only external call is actor pool
update at 80030864, after binding the current actor and rejecting the free
slot. It passes no arguments and consumes updated lifecycle, not a return.
The routine has 25 CFG blocks, 17 conditional branches, 11 static calls,
six validated internal jumps and nine address pairs; no strings or indirect
transfers. Preserve actor/definition ownership, signed-short local coordinates,
byte tile/slot/lifecycle/chance fields, unsigned-short collision radius,
28000/26000/32000 distance gates, seven-bit chance shift, RNG order, -1
query miss, and all activation and range-exit behavior. This is custom actor
policy; its rand provider is separately archive-signature attributed LIBAPI.
The predecessor action selector and ten exact unit siblings are controls.

The current body is one word short. Retail saves s0..s6 plus ra in its
64-byte frame; current C saves s0..s5 plus ra in the same frame. The missing
save/restore correspond to retail's constant one retained in s5 from the
lifecycle dispatch through the persistent spawn-policy test. Current C
materializes one twice, and keeps query-miss -1 in s5 instead of s6. All
remaining instructions align after displacement/register accounting.

Test a promoted lifecycle snapshot before the switch: retail reads one
unsigned byte then uses a signed word decision tree. This is a real decoded
input value, not a constant carrier. Cross it with the explicit common
actor_select_next_action block reached from both dormant initialization paths
and active distance handling, and with the shared wait-for-range-exit store
reached from near-spawn rejection, overlap rejection and failed chance. All
joins already exist in retail (action at 8002e8e4; wait store at 8002e8a0).
Separate statements are controls. Preserve RNG and query execution order,
loads across calls, constants, types and all exact siblings; no forced saved
register, padding or altered public interface. Twelve JSON combinations test
these independent source facts, followed by raw comparison of the best result.

### Awareness result

Eight of twelve states reach strict 100%. All eight share the near-rejection
wait join; the lifecycle snapshot and action join are non-discriminating.
Keep only `goto wait_for_range_exit` from the near-spawn exclusion and its
label on the existing chance-failure assignment. The overlap rejection stays
unchanged. This preserves one dynamic lifecycle update and exits the switch
without executing RNG or overlap work on the rejected near-spawn path.

The canonical object restores all 171 retail words, including the saved
constant-one register, miss sentinel register, prologue/epilogue offsets,
all delay slots and all ordered referents. The label's retail block starts
at 8002e8a0; its actual lifecycle store is the jump delay slot at 8002e8a4.
This identifies an effective source join, not historical compiler attribution
or a proved internal optimizer mechanism.

Fresh canonical compilation and strict report make game.actor_behavior
11/11 exact and GAME 331/362 (from 330). An independent numeric relocation
resolution checks the complete unit's 2,380 instruction words, 144 calls and
52 address materializations against both target and retail. Shifting the
actor_distance_to_point target by four bytes changes exactly its three calls
in awareness, establishing sensitivity to an incorrect referent. All ten
previously banked siblings retain their full retail bodies. Generated raw
verification is build/awareness-raw-verification.json; twelve matrix sources
and scores remain in build/hypotheses/20260908-155752-game-actor_behavior-actor_update_awareness.

The required full build was run after the canonical compile. It reports
331 GAME, 106 OPEN and one PSX exact, with all thirteen vendor controls exact.
It exits nonzero on the pre-existing data ownership, placement and target
relink gates (GAME target relink 75/77, OPEN 34/38, PSX 1/1; no artifact
failures). No comparison criterion or inventory is weakened.

Ruff and whitespace checks pass. All 713 repository tests pass in 125.046
seconds with nine optional skips. `kf bank --unit game.actor_behavior`
records eleven exact rows: newly exact awareness plus refreshed hashes for
ten unchanged exact siblings. Preview, status, notification, radial and all
other partial functions are unbanked; no trial sources enter the commit.

## Function Match Plan: radial damage traversal

At 481e430, GAME 8002d4a8 / 504 bytes, game.actor, remains 90.087300%
with 508 compiled bytes; all 28 siblings are exact. The preceding campaign's
six-view dossier is unchanged and was reread against current source/history.
Fresh full dispatcher caller, both callee, adjacent attack and exact player
counterpart disassemblies are in build/radial-context.txt. The three proven
callers at 80039658/80039ae8/80039d84 pass all eleven arguments with u16
combat components; preserve the origin pointer, unsigned-word radius and
halfword falloff/interface. The actual non-unity path must remain even though
these three calls pass 4096. This is game actor policy, not a vendor body.

Preserve the 136-byte frame, 128 records at stride 72, signed-halfword index,
three ordered early exclusions, height-aware distance query, unsigned division
and its trap, ratio/weight/scale halfword narrowing, signed multiply, three
validated address pairs, one internal jump, both calls and their stack slots.
There are fourteen CFG blocks, seven conditional branches, no strings or
indirect transfers. Each control transfer retains its delay slot. Initial
falloff normalization and the real cached word deficit are required; earlier
controls removing either changed the frame or introduced repeated masks.

The first difference is entry scheduling at 8002d4d4. Retail normalizes
falloff, materializes 4096 once in s7, computes/stores the deficit, then binds
other arguments. The candidate binds arguments first, uses v0 for the first
4096, then separately initializes s7 for the loop guard. Every loop-body
instruction aligns after accounting for this extra word and shifted branches.

Generate 36 reviewed combinations: three loop continuation forms (for header,
explicit body increment join, bottom-tested join), separate versus combined
ordered eligibility tests, early distance continue versus positive distance
guard, and three scale joins (existing calculated-first, default scale with
conditional overwrite, explicit shared damage-call label). The matrix varies
real control-flow ownership already decoded at the loop tail and damage-call
join. No extra live constant, fake local, padding, declaration permutation,
forced register or arithmetic simplification is permitted. Preserve read and
call order on every path and all 28 exact siblings. Compare first raw
instruction differences, not score alone; bank only a verified canonical100%.

### Radial verdict and integration rule

None of the 36 radial combinations reaches exact. Twenty-four existing/label
scale forms retain 90.087300%; twelve default-scale forms drop to 87.706350%.
The original remains production source. Subsequent main-worktree inspection
found concurrent actor.c changes, so radial work is left isolated and no
concurrent source/tooling edits are staged or copied.

The user now requires verified exact changes on master as part of this loop,
without tooling changes. Master already contains awareness in 54876d0 using
the both-shared variant, which independently reached 100% in this matrix.
Preserve that commit rather than replace it with the minimal branch variant.
New exact functions will be integrated individually after verification.

## Function Match Plan: window background branches

GAME 80028914 / 348 bytes, game.menu_draw_window, strict 87.540230%,
352 compiled bytes. Current six views, raw compare, all caller functions,
neighboring prompt/list and drawing-helper bodies are in build/window-evidence.txt
and window-context.txt. Previous row-pointer and conditional-pointer controls
in game-menu-window-rows.md were reviewed; the complete 264-byte window,
24-byte glyph rows, four word arguments, signed title halfword, count>0,
highlight equality, exact confirmation==1 and kind!=6 backdrop policy remain.
There are six calls, seven conditional branches, nine validated address
pairs, no strings or indirect transfers. This is game menu composition with
only game helper calls and no vendor identity.

Retail has a 48-byte frame saving s0..s5 and ra; the candidate adds s6.
Retail materializes both background member addresses independently, compares
row/highlight displacements, and forms layout+row in three call delay slots.
The candidate caches that pointer across calls and derives the confirmed
background by adding twelve to the default address. Shared data ownership
and every physical target stay unchanged; no raw byte view is introduced.

Sixteen combinations cross four background selection forms (existing pointer,
confirmed-first calls, default-first calls, nested row/confirmation calls),
local versus direct typed row addresses, and bottom-tested versus for loops.
Separate call expressions may compile to the one retail static row-background
call; reject any added machine call site. Preserve dynamic title/background/
cursor/string/backdrop order and all read widths. Local-removal/loop controls
alone are not new semantic claims. Inspect first raw differences and complete
strict/raw results; no padding, fake locals or forced registers are allowed.


## GAME map-object spawn: function match plan

At f954711, GAME `80031834` / 404 bytes, `map_object_spawn_effect`
is strict 94.504950%; six of eight unit functions are exact. All six semantic
views, the sole caller's two argument paths, adjacent allocator/action/debris
bodies, prior initialization dossier, identity/vendor rows and history were
reviewed. Five proven calls, ten branches including division guards, one
return and their delay slots agree; two validated global address pairs and
three internal jumps retain their targets. No strings or indirect transfers.
The caller supplies byte kind/ID, a word-coordinate actor position, and
negative half collision height. Group starts 170/180, capacity ten, halfword
sequence, signed division by 2000, and ID thresholds 43/48/65 are unchanged.
This is game policy; the signature-backed LIBAPI rand provider stays external.

The first divergence is ra at stack+32 instead of +36, then sequence/object
register reuse versus separate s0/s1. The exact debris sibling supports the
existing postincrement but does not prove identical syntax here. Test twelve
states: shared versus branch-local acquisition, postincrement versus explicit
publication then increment versus a real halfword sequence snapshot, and
positions-before-cells versus X-cell initialization before Y/Z. The latter
follows retail's first division position. These preserve ownership and all
runtime computations; no artificial pointer lifetime is introduced. Judge
from raw divergence and strict score, retain only humane evidenced source,
and require focused/full validation before banking and master integration.


### Spawn verdict

All twelve states compiled; none reached exact. Shared acquisition with the
original postincrement or a halfword snapshot stayed 94.504950%. Explicit
publication before increment reached 94.396040% and 412 bytes: its first
changed data operation stores the object sequence then reloads the global,
unlike retail's single load. Branch-local acquisition retained the missing
saved register and changed the call setup (93.712870%). Moving X-cell
initialization earlier ranged 82.485146–84.930695%; no candidate is retained.
The original source remains the strongest model; the pointer reuse symptom
alone supplies no further source fact. Results and raw controls are under
`build/hypotheses/20260908-162358-game-map_object-map_object_spawn_effect`
and `build/spawn-controls-compare.txt`.

## GAME map-object pool update: function match plan

GAME `80031cc8` / 3096 bytes, strict 98.966410%, in the same eight-function
unit. Six semantic views, current source, caller game_main_loop, adjacent
clear-link/audio initializer, audio callee, prior family evidence and source
history were reviewed. There are 35 direct calls, one indirect jump, 53 validated outgoing
references, no strings, 64 conditional branches and a single return. The
action-table jump remains an unresolved indirect transfer in the navigator;
99 candidate table rows are not promoted here. The source owns their 396-byte
RODATA contribution. Current and retail CFGs agree on known edges but this
does not prove indirect reachability. All delay slots remain part of their
owner instructions.

The no-argument main-loop call advances 190 typed 44-byte object records.
Byte actions/IDs, halfword timers/angles, word positions, 32-step door motion,
sound volume 127 and the existing sound referents are preserved. Game policy
calls SDK-backed rand/rsin/rcos rather than reconstructing their bodies.
The only current raw differences are two four-instruction reorderings at
the opening and closing door sounds: candidate materializes sound 7 before
loading/comparing object ID, retail loads ID first. Calls and addresses agree.
Test four independently sensible selections at each site: existing pointer
if/else, branch-local calls, conditional argument, and default pointer with
conditional override (16 states). These follow the decoded shared audio call
and alternative referents; no fake lifetime or scheduler attribution. Fresh
strict/raw checks and all unit siblings decide whether any candidate closes.


## GAME notification loop: Function Match Plan

At 44ab6f9, GAME `8001fafc notify_effect_update` / 716 retail bytes is
strict 99.385475%, with three exact siblings in `game.notify_queue`.
All six semantic views, current source, render-frame caller at 8002018c,
formatter/digit callees, neighboring digit/flip boundaries, shared notification
types, source history and previous dequeue/trace dossiers were reviewed.
The no-argument custom UI state machine has five game-helper calls, no SDK
body, no strings or indirect transfer, ten branches, one return, five internal
jumps and 36 validated address pairs. Preserve the 56-byte frame and every
delay slot, byte phases 0/2/3 and IDs 255/19, hold 15, narrowed signed angle
step 128/clamp 512, digit workspace and six sprite clears.

The first real divergence is the initial dequeue-tail load: retail forms an
independent address at +240/+244, candidate uses phase-relative lbu -2(t0),
shortening the body to 712 bytes. All aligned operations/referents otherwise
agree apart from resulting transfer displacements. Prior direct-phase,
pointer-lifetime and explicit byte-snapshot trials failed; do not repeat them
as standalone corrections or alter complete-object ownership.

Test four equivalent loop forms: existing do-condition, infinite for with
two separate breaks, explicit conditional backedge, and for with a combined
break. Each follows retail's mismatch exit followed by gold exclusion, always
clearing at least one queue entry and wrapping the tail modulo eight. Cross
these with the direct initial index, byte snapshot (previous negative control),
and promoted word index. All loads still originate in the existing byte
member. Twelve states test whether the source loop structure explains the
independent initial address; no fake storage, volatile access or alias is
introduced. Compare first raw divergence and exact siblings; only verified
strict 100% will be applied, banked and committed to master.


### Notification loop verdict

Twelve states compiled, none exact. The existing do loop and combined-break
for loop remain 99.385475% for all three index forms. Explicit backedges
reach 95.837990% and change the final comparison register/constant schedule;
separate-break loops reach 86.446930–87.005585%. The independent initial
address is still absent. No notification source change is retained. Results
are under `build/hypotheses/20260908-163800-game-notify_queue-notify_effect_update`
with raw controls in `build/notify-controls-compare.txt`.

## GAME spell pitch: Function Match Plan

GAME `8003a2a0 magic_cast` / 1216 bytes, strict 98.891450%, has three exact
siblings in `game.magic`. Current six semantic views, raw diff, player-update
caller 800193ac, adjacent record loader/sweep, angle callee, source history,
previous casting dossier and authentic SDK vector interfaces were reviewed.
The no-argument game spell policy retains thirteen direct calls, seven retail
internal jumps, twenty-four address pairs and a 144-byte frame. Its five
candidate switch rows and unresolved dispatch jump retain their evidence
tier; the source owns the 20-byte table. No strings or SDK bodies are added.
The existing stack layout now agrees, including both distance outputs and
ground-spawn storage. All constructor argument types and providers remain.

Retail's no-target/non-Lightning arm loads camera pitch, jumps to a shared
store, and clears the local pitch in that jump's delay slot. Current source
copies camera pitch and omits those three words. Test initializing the actual
local pitch to zero and adding camera pitch to it, a consumed initialization
matching that decoded zero store, versus the existing direct copy. This is
not an added dummy store, volatile access or unused object. Keep the targeted
non-Lightning arm and all other pitch rules unchanged.

The two height arms compute the signed vertical difference using world Y,
target Y and offsets 3000/5000. Current output subtracts a negatively adjusted
target; retail adds the positive offset to world Y before subtraction.
Cross the pitch control with four equivalent signed expressions: existing
elevation-minus-target, difference-plus-offset, offset-plus-difference, and
negative-target-plus-elevation. These are bounded algebraic source hypotheses
for the directly observed add/sub sequence, with no type or constant change.
Eight states preserve the height branch, per-arm angle calls, live speed
divisor, all call/return delay slots and the three exact siblings. Apply only
a humane strict-exact result after full raw/reference and repository checks.


### Spell pitch result and height-step follow-up

All eight states compiled. Initializing pitch and adding camera pitch restores
the exact omitted load/jump/zero-store sequence and every shifted destination.
The best source is 1216 bytes and 99.894740%; all stack operands and known
edges agree (37 blocks, 17 branches, one return). Only seven words remain:
both branches use target minus offset instead of world plus offset, reversing
the final subtraction's operand roles. The other algebraic source forms do
not explain that sequence (98.996710–99.078950% with initialization).

Compose the verified pitch operation with five bounded height-step forms:
baseline, per-arm elevation local, elevation then compound subtraction,
separate target/elevation snapshots, and a case-owned elevation variable
assigned and consumed inside each branch. These values represent the actual
loaded target Y and positively adjusted world Y, each used by the angle call.
They introduce no added source operation or type change. Unlike the rejected
historical shared-call trial, calls remain in their original source branches.
Raw load order, positive immediates, final angle store, live-speed division,
complete calls/referents, automatic layout and exact siblings decide success.


## GAME TMD addressing: Function Match Plan

At 3fb0fd2, GAME `8001c7f8 render_enqueue_tmd` / 3896 bytes is strict
99.314170% in `game.render_enqueue`. Current six semantic views, full source
and raw comparison, all seven callers, adjacent transform/model functions,
SDK headers, shared packet/graphics types and source history were reviewed.
The prior GAME TMD/owner dossiers, exact OPEN general emitter and verified
GAME sibling spill-order mechanism supply separate source controls.

Preserve u16 object/s16 bias, word header/count, halfword prepared byte
offsets, signed projected depths, twelve packet modes and their authentic
GPU layouts. Retail has an 88-byte frame, 45 direct calls, 35 address pairs,
eleven internal jumps, 55 branches, one return and one indirect dispatch.
Its 29 switch rows are currently validated; the navigator still leaves the
indirect successor unresolved. All delay slots, clipping/depth predicates,
twelve overflow returns, packet stride and lighting/UV/colour operations
remain. SDK PRIM/SMP providers are not reconstructed or counted as game work.

First raw differences exchange header/count stack homes and normal/projected
registers. Prepared-vertex additions reverse operands, and the final OT load
uses an extra absolute pair rather than retail's projected-base-relative
load. The current 3900-byte candidate otherwise preserves known edges and
ordered calls/referents; the model, map and sprite siblings are exact.

Test eight combinations of three independently supported relationships:
(1) represent the projected base as the 32-bit address consumed by prepared
byte offsets, retaining typed vertex pointers at uses; (2) recover the
existing complete graphics owner from that known member separately in each
accepted packet path, then read its actual ordering-table field; (3) declare
the existing header before the countdown, as predicted by the already
verified ascending-pseudo spill assignment in both GAME sibling emitters.
No new owner, guessed extent, relative-offset padding, instruction, shared
packet carrier or compiler option is added. This is a bounded predicted
order, not a declaration permutation search. Do not apply the rejected OPEN
shared-AddPrim-label forms. Compare from the first raw divergence and require
all four unit functions and the complete switch to match before banking.


### TMD result

All eight states compiled. Address + complete owner + header-first is strict
100%; address + owner + count-first is 99.992810%. Pointer + owner reaches
99.568790% with header first (99.561600% with count first); address without
owner reaches 99.536964%/99.529780%. Pointer/direct reaches
99.321360%/99.314170%. Only the exact combined source is retained.
The [TMD dossier](game-tmd-enqueue.md#strict-closure-2026-09-08) records raw
verification for every unit function and all switch targets.
