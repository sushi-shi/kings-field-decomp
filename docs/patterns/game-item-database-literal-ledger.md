# Database-loader and startup literal ledger

Complete ledger for `item_load_database` and `game_state_initialize`: **119
retained occurrences** (22 database, 97 startup). Named definitions and
retail claims are excluded; negative signs are operators. Other functions
and static initializers in their modules are outside this ledger. See the
[dimension review](game-item-menu-dimensions.md) for the owner evidence.
The initial vertical-state literal was subsequently named by the
[player state review](game-player-vertical-state.md).

| Function | Lines | Tokens | Expression | Reason |
| --- | --- | --- | --- | --- |
| `item_load_database` | 82 | `40` | `char name[40] = "\\KF\\ITEM0\\I000.TMD;1";` | Forty-byte local path workspace holds the 20-character ISO path plus NUL. Only directory/digit positions are rewritten; the original extra-capacity rationale is unproven. |
| `item_load_database` | 87 | `0` | `if (cd_file_load_allocated(&stat_data, "COM\\STAT.DAT") != 0)` | Any nonzero allocation/load result takes the fatal startup exit; zero alone permits parsing the loaded STAT.DAT banks. |
| `item_load_database` | 88 | `1` | `exit(1);` | Nonzero process-exit status for the fatal database-load path; this is not a file or menu-selection ID. |
| `item_load_database` | 105 | `0` | `for (i = 0; i < KF_ITEM_COUNT; i++) {` | Begin the item-ID/file-table scan at zero; the named item count supplies its exclusive endpoint. |
| `item_load_database` | 106 | `1` | `s32 n = i + 1;` | Convert zero-based item ID to the one-based file number I001 through I080. |
| `item_load_database` | 109 | `8, 30, '1'` | `name[8] = i / 30 + '1';` | Path byte eight selects ITEM1/ITEM2/ITEM3; integer division groups IDs 0..29, 30..59 and 60..79 into thirty-file directory bands, and ASCII one encodes the directory digit. Preserve the shipped file organization. |
| `item_load_database` | 110 | `11, 100, '0'` | `name[11] = n / 100 + '0';` | Path byte eleven is the decimal hundreds digit of the one-based file number; divide by one hundred and add ASCII zero. |
| `item_load_database` | 111 | `100` | `rem = n % 100;` | Keep the final two decimal digits after writing the hundreds position. |
| `item_load_database` | 112 | `12, 10, '0'` | `name[12] = rem / 10 + '0';` | Path byte twelve is the decimal tens digit; base ten and ASCII zero encode it. |
| `item_load_database` | 113 | `13, 10, '0'` | `name[13] = rem % 10 + '0';` | Path byte thirteen is the decimal units digit; modulo ten and ASCII zero encode it. |
| `item_load_database` | 114 | `0` | `if (CdSearchFile((CdlFILE *)&cd_file_table[i], name) != 0) {` | Any nonzero SDK search result identifies a found file whose recorded size may be rounded; failed searches leave that table entry alone. |
| `item_load_database` | 115 | `1, 0` | `if ((cd_file_table[i].size & (KF_CD_SECTOR_BYTES - 1)) != 0)` | Subtract one from the power-of-two sector size to form its low-byte-count mask. Zero masked bits mean already aligned; only partial sectors enter the round-up path. |
| `item_load_database` | 117 | `1` | `((cd_file_table[i].size >> KF_CD_SECTOR_SHIFT) + 1) << KF_CD_SECTOR_SHIFT;` | Add one to the truncated complete-sector count before shifting back to bytes; the preceding partial-sector guard prevents rounding an aligned size up unnecessarily. |
| `game_state_initialize` | 57 | `0` | `player_state.experience = 0;` | New session begins with no accumulated experience; the next threshold is loaded separately from growth row zero. |
| `game_state_initialize` | 58 | `1` | `player_state.progress_state.level = 1;` | Initial displayed/player level is one; growth-table row zero supplies that level's base values. |
| `game_state_initialize` | 59 | `1` | `player_state.progress_state.current_floor = 1;` | New session starts on floor one and records one as the highest reached floor. These are one-based floor identities. |
| `game_state_initialize` | 60 | `1` | `player_state.progress_state.highest_floor = 1;` | New session starts on floor one and records one as the highest reached floor. These are one-based floor identities. |
| `game_state_initialize` | 61 | `0x96` | `player_state.gold = 0x96;` | Authored starting balance is 150 gold units. The original balance rationale is unproven; this startup field assignment retains the literal. |
| `game_state_initialize` | 62 | `0` | `player_state.attack_charge_state.current = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 63 | `0` | `player_state.magic_charge = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 64 | `0` | `player_state.weapon_charge_delay = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 65 | `5` | `player_state.cutting_defense = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 66 | `5` | `player_state.striking_defense = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 67 | `5` | `player_state.piercing_defense = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 68 | `5` | `player_state.poison_resistance = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 69 | `5` | `player_state.magic_defense = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 70 | `5` | `player_state.fire_defense = 5;` | Authored initial value five for this named defense/resistance field. Preserve the per-field assignments without asserting that physical defenses and poison resistance use interchangeable calculations; tuning rationale is unknown. |
| `game_state_initialize` | 71 | `0` | `player_state.view_bob_offset = 0;` | Reset the view-bob displacement and phase to their zero origin; these are distinct state fields rather than a shared scale. |
| `game_state_initialize` | 72 | `0` | `player_state.view_bob_phase = 0;` | Reset the view-bob displacement and phase to their zero origin; these are distinct state fields rather than a shared scale. |
| `game_state_initialize` | 74 | `0` | `player_state.vertical_velocity = 0;` | Start with no vertical motion; zero is the velocity origin. |
| `game_state_initialize` | 75 | `0` | `player_state.vitals.current_hp = player_level_growth_table[0].maximum_hp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 76 | `0` | `player_state.vitals.maximum_hp = player_level_growth_table[0].maximum_hp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 77 | `0` | `player_state.vitals.current_mp = player_level_growth_table[0].maximum_mp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 78 | `0` | `player_state.vitals.maximum_mp = player_level_growth_table[0].maximum_mp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 79 | `0` | `player_state.base_physical_power = player_level_growth_table[0].physical_power_step;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 80 | `0` | `player_state.base_magic = player_level_growth_table[0].magic_step;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 81 | `0` | `player_state.next_level_experience = player_level_growth_table[0].experience_threshold;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 89 | `0` | `player_set_equipment_slot(0, KF_EQUIPMENT_SLOT_REFRESH_ONLY);` | Unused item argument zero when the named refresh-only slot bypasses every equipment assignment and refreshes pointers/stat state. |
| `game_state_initialize` | 98 | `1` | `count = sizeof(map_runtime_state.world_state) - 1;` | Initialize the inclusive byte countdown to complete object size minus one; the loop writes index zero through the last byte before its decremented count reaches -1. |
| `game_state_initialize` | 100, 105 | `0 × 2` | `*cursor++ = 0;` | Write a zero byte while clearing the complete saved-world and stock owners before their initial values are seeded. |
| `game_state_initialize` | 101, 106 | `1 × 2` | `} while (--count != -1);` | Stop after the decrement crosses zero to -1, so a starting count size-minus-one still writes exactly size bytes. |
| `game_state_initialize` | 103 | `1` | `count = sizeof(item_stock) - 1;` | Initialize the inclusive byte countdown to complete object size minus one; the loop writes index zero through the last byte before its decremented count reaches -1. |
| `game_state_initialize` | 107 | `0, 1` | `item_stock[0][KF_ITEM_SHORT_SWORD] = 1;` | Player stock bank zero receives one copy of this named starting item. |
| `game_state_initialize` | 108 | `0, 1` | `item_stock[0][KF_ITEM_MEDICINAL_HERB] = 1;` | Player stock bank zero receives one copy of this named starting item. |
| `game_state_initialize` | 109 | `1 × 2` | `item_stock[1][KF_ITEM_SHORT_SWORD] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 110 | `1 × 2` | `item_stock[1][KF_ITEM_BATTLE_AXE] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 111 | `1 × 2` | `item_stock[1][KF_ITEM_KNIGHT_SWORD] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 112 | `1 × 2` | `item_stock[1][KF_ITEM_IRON_MASK] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 113 | `1 × 2` | `item_stock[1][KF_ITEM_KNIGHT_HELM] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 114 | `1 × 2` | `item_stock[1][KF_ITEM_KNIGHT_PLATE] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 115 | `1 × 2` | `item_stock[1][KF_ITEM_SMALL_SHIELD] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 116 | `1 × 2` | `item_stock[1][KF_ITEM_KNIGHT_SHIELD] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 117 | `1 × 2` | `item_stock[1][KF_ITEM_GAUNTLET] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 118 | `1 × 2` | `item_stock[1][KF_ITEM_IRON_BOOTS] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 119 | `1 × 2` | `item_stock[1][KF_ITEM_MEDICINAL_HERB] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 120 | `1 × 2` | `item_stock[1][KF_ITEM_ANTIDOTE_HERB] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 121 | `1 × 2` | `item_stock[1][KF_ITEM_RECOVERY_MEDICINE] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 122 | `1 × 2` | `item_stock[1][KF_ITEM_DRAGON_KING_GRASS_FRUIT] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 123 | `2, 1` | `item_stock[2][KF_ITEM_KNIGHT_SWORD] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 124 | `2, 1` | `item_stock[2][KF_ITEM_COLICHEMARDE] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 125 | `2, 1` | `item_stock[2][KF_ITEM_CRESCENT_AXE] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 126 | `2, 1` | `item_stock[2][KF_ITEM_KNIGHT_HELM] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 127 | `2, 1` | `item_stock[2][KF_ITEM_GREAT_HELM] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 128 | `2, 1` | `item_stock[2][KF_ITEM_BREASTPLATE] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 129 | `2, 1` | `item_stock[2][KF_ITEM_FULL_PLATE] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 130 | `2, 1` | `item_stock[2][KF_ITEM_FIRE_MAIL] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 131 | `2, 1` | `item_stock[2][KF_ITEM_TOWER_SHIELD] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 132 | `2, 1` | `item_stock[2][KF_ITEM_LEG_GUARDS] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 133 | `2, 1` | `item_stock[2][KF_ITEM_MEDICINAL_HERB] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 134 | `2, 1` | `item_stock[2][KF_ITEM_ANTIDOTE_HERB] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 135 | `2, 1` | `item_stock[2][KF_ITEM_RECOVERY_MEDICINE] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 136 | `2, 1` | `item_stock[2][KF_ITEM_DRAGON_KING_GRASS_LEAF] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 137 | `2, 1` | `item_stock[2][KF_ITEM_DRAGON_KING_GRASS_FRUIT] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 138 | `2, 1` | `item_stock[2][KF_ITEM_LIGHT_RING] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 139 | `2, 1` | `item_stock[2][KF_ITEM_GOLD_CROSS] = 1;` | Second shop bank two starts with one Gold Cross; the buy path decrements this exceptional finite stock entry. |
