# Root, list and save menu retained literals

Complete current per-occurrence ledger for `menu.c`, `menu_panels.c`,
`menu_item_drop.c` and `menu_draw_window.c` after the
[shared confirmation-state review](menu-confirm-state.md): **265 occurrences**.
Named definitions, claims, comments, string contents and identifier digits are
excluded. Duplicate occurrences have individual rows. This consolidates the
older choice, spell, item-list, save and window slices against current source.

Retained protocol selectors below remain explicit follow-up work where noted;
complete accounting does not mean every domain has already been propagated.
Local workspace extents and authored frame counts retain their measured values,
without inventing a reason for the original capacity or timing choices.

| Source / function | Line | Token | Expression | Reason |
|---|---:|---|---|---|
| `menu.c` / `menu_save_confirm` | 22 | `0` | `i = 0;` | Initialize the local draw counter before the preincremented three-frame loop; zero is the arithmetic origin. |
| `menu.c` / `menu_save_confirm` | 26 | `0` | `menu_draw_dialog_frame(0, 3);` | Null save-summary pointer: draw the background quads without reading summary records. |
| `menu.c` / `menu_save_confirm` | 26 | `3` | `menu_draw_dialog_frame(0, 3);` | Composite selector three draws all three slot backgrounds because it is at the slot-count boundary; it is separate from the following draw-loop count. This selector still needs propagation with the dialog-frame domain. |
| `menu.c` / `menu_save_confirm` | 27 | `0` | `menu_draw_window(KF_MENU_WINDOW_SAVE, KF_MENU_SAVE_ROW_COUNT, 0, KF_MENU_CONFIRM_IDLE);` | First visible save-window row; confirmation state is separately typed and initially idle. |
| `menu.c` / `menu_save_confirm` | 29 | `3` | `} while (i < 3);` | Present exactly three initial confirmation-screen frames before the opening cue and input-release wait. This authored draw count is independent of the slot count; its original timing rationale is unproven. |
| `menu.c` / `menu_save_confirm` | 31 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead argument one; the linked SDK routine reads its global pad identifier, not an argument-selected port. |
| `menu.c` / `menu_save_confirm` | 31 | `0` | `while (PadRead(1) != 0)` | Wait until the pad bitset is empty before entering the save panel. |
| `menu.c` / `menu_root` | 46 | `0` | `s32 cursor = 0;` | Start or wrap the zero-based cursor at the first visible row; the named return row supplies the opposite boundary. |
| `menu.c` / `menu_root` | 48 | `0` | `s32 input = 0;` | Initialize previous button bits to none before edge detection. |
| `menu.c` / `menu_root` | 54 | `0` | `i = 0;` | Initialize the initial-frame draw counter before its pre-draw increment. |
| `menu.c` / `menu_root` | 61 | `3` | `} while (i < 3);` | Present exactly three initial root-menu frames before the sound and input-release wait. This is an authored draw count; no evidence establishes why three was chosen or a real-time duration. |
| `menu.c` / `menu_root` | 63 | `1` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 63 | `0` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 72 | `1` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 72 | `0` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 105 | `1` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 105 | `0` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 112 | `1` | `input = PadRead(1);` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu.c` / `menu_root` | 113 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 113 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 115 | `0` | `if (cursor != 0)` | Decrement only above the first zero-based row; at zero, wrap to the named return row. |
| `menu.c` / `menu_root` | 119 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 119 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 124 | `0` | `cursor = 0;` | Start or wrap the zero-based cursor at the first visible row; the named return row supplies the opposite boundary. |
| `menu.c` / `menu_root` | 125 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 125 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 132 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_root` | 132 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu.c` / `menu_use_item_panel` | 162 | `50` | `s16 labels[50][MENU_GLYPHS_PER_ROW];` | Fifty-row local workspace, now sharing the named glyph width. The admitted item sets need at most 34 distinct rows; why the original reserves fifty is unproven. |
| `menu.c` / `menu_use_item_panel` | 163 | `56` | `u8 counts[56];` | Parallel 56-byte quantity/item-code workspace in the retail frame. Its capacity differs from the fifty label rows and exceeds the current admitted set; preserve the observed extents without inventing a common capacity. |
| `menu.c` / `menu_use_item_panel` | 164 | `56` | `u8 codes[56];` | Parallel 56-byte quantity/item-code workspace in the retail frame. Its capacity differs from the fifty label rows and exceeds the current admitted set; preserve the observed extents without inventing a common capacity. |
| `menu.c` / `menu_use_item_panel` | 170 | `0` | `s32 input = 0;` | Initial previous input state has no pressed buttons for edge detection. |
| `menu.c` / `menu_use_item_panel` | 174 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu.c` / `menu_use_item_panel` | 174 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu.c` / `menu_use_item_panel` | 178 | `0` | `inv = item_stock[0];` | Player-owned stock bank zero; other banks belong to the wider stock/save/shop domain, so no merchant identity is inferred from this index. |
| `menu.c` / `menu_use_item_panel` | 179 | `0` | `found = 0;` | Begin appending compacted rows at the first workspace entry. |
| `menu.c` / `menu_use_item_panel` | 180 | `0` | `if (inv[KF_ITEM_WATCHMAN_MAP] != 0) {` | Any nonzero owned map quantity includes its row before the other usable-item ranges. |
| `menu.c` / `menu_use_item_panel` | 181 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu.c` / `menu_use_item_panel` | 187 | `0` | `if (inv[KF_ITEM_SORCERER_MAP] != 0) {` | Any nonzero owned map quantity includes its row before the other usable-item ranges. |
| `menu.c` / `menu_use_item_panel` | 188 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu.c` / `menu_use_item_panel` | 195 | `0` | `if (code != KF_ITEM_WATCHMAN_MAP && code != KF_ITEM_SORCERER_MAP && inv[code] != 0) {` | Append only owned quantities greater than zero after excluding the maps already listed. The quantity is an unsigned byte; zero means no stock. |
| `menu.c` / `menu_use_item_panel` | 196 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu.c` / `menu_use_item_panel` | 204 | `0` | `if (code != KF_ITEM_WATCHMAN_MAP && code != KF_ITEM_SORCERER_MAP && inv[code] != 0) {` | Append only owned quantities greater than zero after excluding the maps already listed. The quantity is an unsigned byte; zero means no stock. |
| `menu.c` / `menu_use_item_panel` | 205 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu.c` / `menu_use_item_panel` | 214 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu.c` / `menu_use_item_panel` | 214 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu.c` / `menu_use_item_panel` | 218 | `0` | `if (ctx.entry_count != 0) {` | Only preview/render an item when the list has entries. |
| `menu.c` / `menu_use_item_panel` | 219 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu.c` / `menu_use_item_panel` | 229 | `0` | `KF_MENU_PREVIEW_ITEM_MODEL, codes[ctx.selected_index], 0, KF_ITEM_PRICE_BUY)` | Unused shop index zero in the shared confirmation API; this model-preview path does not request a shop-price detail view. |
| `menu.c` / `menu_use_item_panel` | 237 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu.c` / `menu_use_item_panel` | 237 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu.c` / `menu_use_item_panel` | 243 | `1` | `input = PadRead(1);` | Ignored retail PadRead call-site argument; the linked routine uses its global pad identifier. |
| `menu.c` / `menu_use_item_panel` | 244 | `0` | `if (ctx.entry_count == 0) {` | No eligible owned rows produces the empty-list input path; any subsequent pressed input exits with no selection. |
| `menu.c` / `menu_use_item_panel` | 245 | `0` | `if (input != 0) {` | Any pressed input acknowledges the empty-list path. |
| `menu.c` / `menu_use_item_panel` | 249 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 249 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 251 | `0` | `if (ctx.selected_index != 0) {` | First list-entry boundary for upward cursor movement. |
| `menu.c` / `menu_use_item_panel` | 253 | `0` | `if (ctx.cursor_row == 0)` | At the first visible row, upward movement scrolls the window; otherwise it decrements the visible cursor row. |
| `menu.c` / `menu_use_item_panel` | 258 | `1` | `ctx.selected_index = ctx.entry_count - 1;` | Wrap to the last zero-based entry. |
| `menu.c` / `menu_use_item_panel` | 260 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu.c` / `menu_use_item_panel` | 261 | `1` | `ctx.cursor_row = ctx.entry_count - 1;` | Bottom row of a list shorter than the visible window. |
| `menu.c` / `menu_use_item_panel` | 264 | `1` | `ctx.cursor_row = ctx.visible_rows - 1;` | Last zero-based visible cursor row. |
| `menu.c` / `menu_use_item_panel` | 267 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu.c` / `menu_use_item_panel` | 269 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 269 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 271 | `1` | `if (ctx.selected_index < ctx.entry_count - 1) {` | Downward movement stops at the last zero-based entry before wrapping. |
| `menu.c` / `menu_use_item_panel` | 273 | `1` | `if (ctx.cursor_row == ctx.visible_rows - 1)` | At the last zero-based visible row, downward movement scrolls the window; otherwise it increments the visible cursor row. |
| `menu.c` / `menu_use_item_panel` | 278 | `0` | `ctx.selected_index = 0;` | Wrap selection back to the first entry. |
| `menu.c` / `menu_use_item_panel` | 279 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu.c` / `menu_use_item_panel` | 280 | `0` | `ctx.cursor_row = 0;` | Reset the visible cursor to the top row. |
| `menu.c` / `menu_use_item_panel` | 282 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu.c` / `menu_use_item_panel` | 284 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 284 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 290 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu.c` / `menu_use_item_panel` | 295 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 295 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu.c` / `menu_use_item_panel` | 301 | `0` | `if (ctx.entry_count != 0)` | Only preview an item when the list has entries. |
| `menu.c` / `menu_use_item_panel` | 322 | `0` | `player_state.status_effect_flags = 0;` | Grass leaf and fruit clear the entire status bitset. Keep an empty mask distinct from any one named status bit or a claim about additional effects. |
| `menu.c` / `menu_use_item_panel` | 324 | `0` | `player_state.status_effect_flags = 0;` | Grass leaf and fruit clear the entire status bitset. Keep an empty mask distinct from any one named status bit or a claim about additional effects. |
| `menu_panels.c` / `menu_magic_panel` | 31 | `10` | `s16 labels[10][MENU_GLYPHS_PER_ROW];` | Ten-row local workspace, distinct from the named number of glyphs in each row. At most four learned instant spells are appended; original over-allocation rationale is unknown. |
| `menu_panels.c` / `menu_magic_panel` | 32 | `16` | `u8 codes[16];` | Sixteen-byte parallel spell-code workspace; only four learned instant spells can be appended here. Original capacity choice unknown. |
| `menu_panels.c` / `menu_magic_panel` | 37 | `0` | `s32 input = 0;` | Initial previous input state has no pressed buttons for edge detection. |
| `menu_panels.c` / `menu_magic_panel` | 41 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_panels.c` / `menu_magic_panel` | 41 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_panels.c` / `menu_magic_panel` | 45 | `0` | `found = 0;` | Start appending learned spell rows at the first workspace entry. |
| `menu_panels.c` / `menu_magic_panel` | 48 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu_panels.c` / `menu_magic_panel` | 56 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu_panels.c` / `menu_magic_panel` | 56 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu_panels.c` / `menu_magic_panel` | 57 | `0` | `ctx.quantities = 0;` | Null quantity list: selection panels display names without stock counts. |
| `menu_panels.c` / `menu_magic_panel` | 60 | `0` | `if (ctx.entry_count != 0) {` | Only preview/render an item when the list has entries. |
| `menu_panels.c` / `menu_magic_panel` | 61 | `1` | `if (menu_load_item_texture(codes[ctx.selected_index]) == 1)` | Numbered-texture loader returns exactly1 on failure; preserve the existing equality check. |
| `menu_panels.c` / `menu_magic_panel` | 71 | `0` | `KF_MENU_PREVIEW_MAGIC_ICON, codes[ctx.selected_index], 0, KF_ITEM_PRICE_BUY)` | The shop/detail index is an unused zero for the magic-icon confirmation path. |
| `menu_panels.c` / `menu_magic_panel` | 78 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_panels.c` / `menu_magic_panel` | 78 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_panels.c` / `menu_magic_panel` | 86 | `1` | `input = PadRead(1);` | Ignored retail PadRead call-site argument; the linked routine uses its global pad identifier. |
| `menu_panels.c` / `menu_magic_panel` | 87 | `0` | `if (ctx.entry_count == 0) {` | No learned instant spells produces an empty list; it has no appended none entry. |
| `menu_panels.c` / `menu_magic_panel` | 88 | `0` | `if (input != 0) {` | Any pressed input acknowledges the empty-list path. |
| `menu_panels.c` / `menu_magic_panel` | 92 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 92 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 94 | `0` | `if (ctx.selected_index != 0) {` | First list-entry boundary for upward cursor movement. |
| `menu_panels.c` / `menu_magic_panel` | 96 | `0` | `if (ctx.cursor_row == 0)` | Scroll upward at the first visible row; otherwise move the cursor. Retail places the scroll arm first. |
| `menu_panels.c` / `menu_magic_panel` | 101 | `1` | `ctx.selected_index = ctx.entry_count - 1;` | Set the last entry before either upward-wrap viewport arm, including the retained long-list path. |
| `menu_panels.c` / `menu_magic_panel` | 103 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu_panels.c` / `menu_magic_panel` | 104 | `1` | `ctx.cursor_row = ctx.entry_count - 1;` | Bottom row of a list shorter than the visible window. |
| `menu_panels.c` / `menu_magic_panel` | 107 | `1` | `ctx.cursor_row = ctx.visible_rows - 1;` | Last zero-based visible cursor row. |
| `menu_panels.c` / `menu_magic_panel` | 110 | `1` | `if (menu_load_item_texture(codes[ctx.selected_index]) == 1)` | Numbered-texture loader returns exactly1 on failure; preserve the existing equality check. |
| `menu_panels.c` / `menu_magic_panel` | 112 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 112 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 114 | `1` | `if (ctx.selected_index < ctx.entry_count - 1) {` | Downward movement stops at the last zero-based entry before wrapping. |
| `menu_panels.c` / `menu_magic_panel` | 116 | `1` | `if (ctx.cursor_row == ctx.visible_rows - 1)` | Scroll downward at the bottom visible row; otherwise move the cursor. Retail places the scroll arm first. |
| `menu_panels.c` / `menu_magic_panel` | 121 | `0` | `ctx.selected_index = 0;` | Wrap selection back to the first entry. |
| `menu_panels.c` / `menu_magic_panel` | 122 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu_panels.c` / `menu_magic_panel` | 123 | `0` | `ctx.cursor_row = 0;` | Reset the visible cursor to the top row. |
| `menu_panels.c` / `menu_magic_panel` | 125 | `1` | `if (menu_load_item_texture(codes[ctx.selected_index]) == 1)` | Numbered-texture loader returns exactly1 on failure; preserve the existing equality check. |
| `menu_panels.c` / `menu_magic_panel` | 127 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 127 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 130 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 130 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Named button present now and absent previously gives a rising edge; zero means no corresponding bit. |
| `menu_panels.c` / `menu_magic_panel` | 135 | `0` | `if (ctx.entry_count != 0)` | Only preview an item when the list has entries. |
| `menu_panels.c` / `menu_option_root` | 170 | `0` | `s32 cursor = 0;` | Start or wrap the zero-based cursor at the first visible row; the named return row supplies the opposite boundary. |
| `menu_panels.c` / `menu_option_root` | 172 | `0` | `s32 input = 0;` | Initialize previous button bits to none before edge detection. |
| `menu_panels.c` / `menu_option_root` | 179 | `0` | `menu_draw_window(KF_MENU_WINDOW_EQUIPMENT, KF_MENU_EQUIPMENT_ROW_COUNT, 0, KF_MENU_CONFIRM_IDLE);` | Initial visible row index zero; the confirmation-request argument is separately named and typed. |
| `menu_panels.c` / `menu_option_root` | 188 | `1` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu_panels.c` / `menu_option_root` | 188 | `0` | `while (PadRead(1) != 0)` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu_panels.c` / `menu_option_root` | 216 | `1` | `input = PadRead(1);` | Retain the ignored SDK argument one: the linked routine uses global PadIdentifier. Zero returned button bits mean released; the wait consumes held input before proceeding, while assignment samples the next edge. |
| `menu_panels.c` / `menu_option_root` | 217 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 217 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 219 | `0` | `if (cursor != 0)` | Decrement only above the first zero-based row; at zero, wrap to the named return row. |
| `menu_panels.c` / `menu_option_root` | 223 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 223 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 228 | `0` | `cursor = 0;` | Start or wrap the zero-based cursor at the first visible row; the named return row supplies the opposite boundary. |
| `menu_panels.c` / `menu_option_root` | 229 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 229 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 236 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_panels.c` / `menu_option_root` | 236 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear define a rising edge. Zero denotes absence; retain the existing priority among navigation, toggle, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_drop_item` | 42 | `0` | `s32 input = 0;` | Initial previous input state has no pressed buttons for edge detection. |
| `menu_item_drop.c` / `menu_drop_item` | 46 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_item_drop.c` / `menu_drop_item` | 46 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_item_drop.c` / `menu_drop_item` | 50 | `0` | `found = 0;` | Begin appending compacted rows at the first workspace entry. |
| `menu_item_drop.c` / `menu_drop_item` | 51 | `0` | `code = 0;` | Start scanning the complete stock array at its first zero-based item ID. |
| `menu_item_drop.c` / `menu_drop_item` | 53 | `0` | `inv = item_stock[0];` | Player-owned stock bank zero; other banks belong to the wider stock/save/shop domain, so no merchant identity is inferred from this index. |
| `menu_item_drop.c` / `menu_drop_item` | 55 | `0` | `if (inv[code] != 0) {` | Only owned nonzero quantities enter the equipped-copy adjustment and row-construction path. |
| `menu_item_drop.c` / `menu_drop_item` | 65 | `0` | `if (counts[found] != 0) {` | Append a discard row only if a quantity remains after subtracting one equipped copy. Preserve the single decrement when any of the seven equipment IDs matches. |
| `menu_item_drop.c` / `menu_drop_item` | 66 | `0` | `for (j = 0; j < MENU_GLYPHS_PER_ROW; j++)` | Copy all ten glyph halfwords, starting at index zero, from the shared fixed-width name row. |
| `menu_item_drop.c` / `menu_drop_item` | 76 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu_item_drop.c` / `menu_drop_item` | 76 | `0` | `ctx.glyph_rows = &labels[0][0];` | Base address of the first glyph in the first row for the flat list-render API. |
| `menu_item_drop.c` / `menu_drop_item` | 77 | `0` | `ctx.quantities = 0;` | Null quantity list: selection panels display names without stock counts. |
| `menu_item_drop.c` / `menu_drop_item` | 80 | `0` | `if (ctx.entry_count != 0) {` | Only preview/render an item when the list has entries. |
| `menu_item_drop.c` / `menu_drop_item` | 81 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu_item_drop.c` / `menu_drop_item` | 91 | `0` | `KF_MENU_PREVIEW_ITEM_MODEL, codes[ctx.selected_index], 0, KF_ITEM_PRICE_BUY)` | Unused shop index zero in the shared confirmation API; this model-preview path does not request a shop-price detail view. |
| `menu_item_drop.c` / `menu_drop_item` | 99 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_item_drop.c` / `menu_drop_item` | 99 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored PadRead call-site argument 1 and wait until the returned button bits are zero; the linked SDK uses global PadIdentifier, not this argument as a port. |
| `menu_item_drop.c` / `menu_drop_item` | 105 | `1` | `input = PadRead(1);` | Ignored retail PadRead call-site argument; the linked routine uses its global pad identifier. |
| `menu_item_drop.c` / `menu_drop_item` | 106 | `0` | `if (ctx.entry_count == 0) {` | No eligible owned rows produces the empty-list input path; any subsequent pressed input exits with no selection. |
| `menu_item_drop.c` / `menu_drop_item` | 107 | `0` | `if (input != 0) {` | Any pressed input acknowledges the empty-list path. |
| `menu_item_drop.c` / `menu_drop_item` | 111 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 111 | `0` | `} else if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 113 | `0` | `if (ctx.selected_index != 0) {` | First list-entry boundary for upward cursor movement. |
| `menu_item_drop.c` / `menu_drop_item` | 115 | `0` | `if (ctx.cursor_row == 0)` | At the first visible row, upward movement scrolls the window; otherwise it decrements the visible cursor row. |
| `menu_item_drop.c` / `menu_drop_item` | 120 | `1` | `ctx.selected_index = ctx.entry_count - 1;` | Wrap to the last zero-based entry. |
| `menu_item_drop.c` / `menu_drop_item` | 122 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu_item_drop.c` / `menu_drop_item` | 123 | `1` | `ctx.cursor_row = ctx.entry_count - 1;` | Bottom row of a list shorter than the visible window. |
| `menu_item_drop.c` / `menu_drop_item` | 126 | `1` | `ctx.cursor_row = ctx.visible_rows - 1;` | Last zero-based visible cursor row. |
| `menu_item_drop.c` / `menu_drop_item` | 129 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu_item_drop.c` / `menu_drop_item` | 131 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 131 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 133 | `1` | `if (ctx.selected_index < ctx.entry_count - 1) {` | Downward movement stops at the last zero-based entry before wrapping. |
| `menu_item_drop.c` / `menu_drop_item` | 135 | `1` | `if (ctx.cursor_row == ctx.visible_rows - 1)` | At the last zero-based visible row, downward movement scrolls the window; otherwise it increments the visible cursor row. |
| `menu_item_drop.c` / `menu_drop_item` | 140 | `0` | `ctx.selected_index = 0;` | Wrap selection back to the first entry. |
| `menu_item_drop.c` / `menu_drop_item` | 141 | `0` | `ctx.scroll_offset = 0;` | Reset the scroll window to its first entry. |
| `menu_item_drop.c` / `menu_drop_item` | 142 | `0` | `ctx.cursor_row = 0;` | Reset the visible cursor to the top row. |
| `menu_item_drop.c` / `menu_drop_item` | 144 | `0` | `if (menu_load_item_model(codes[ctx.selected_index]) != 0)` | Any nonzero model-loader result aborts the panel. Item use returns named no-selection; discard returns void. These early exits preserve their existing cleanup order and differ from the normal release path. |
| `menu_item_drop.c` / `menu_drop_item` | 146 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 146 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 149 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 149 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit set and previous bit clear form a rising edge; zero tests Boolean absence. |
| `menu_item_drop.c` / `menu_drop_item` | 155 | `0` | `if (ctx.entry_count != 0)` | Only preview an item when the list has entries. |
| `menu_item_drop.c` / `menu_save_load_hub` | 177 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_item_drop.c` / `menu_save_load_hub` | 180 | `1` | `s32 action = -1;` | No pending System row action. This signed index sentinel is distinct from the root result channel and is reset before the result-exit guard. |
| `menu_item_drop.c` / `menu_save_load_hub` | 186 | `1` | `if (action != -1 \|\| result == action) {` | Redraw the selected frame when an action is pending or the root result equals the no-action index (-1). The latter covers the exit frame; keep this cross-value comparison. |
| `menu_item_drop.c` / `menu_save_load_hub` | 190 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_load_hub` | 190 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_load_hub` | 202 | `0` | `KF_MENU_WINDOW_SYSTEM, KF_MENU_SYSTEM_ROW_COUNT, cursor, 0));` | Null save-summary pointer: the System quit prompt has no slot catalogue to display. |
| `menu_item_drop.c` / `menu_save_load_hub` | 218 | `1` | `if (action != -1 && result == KF_MENU_ROOT_NO_ITEM)` | A sub-action returning no item/cancellation keeps the System hub open only when a row action was pending. The -1 operand tests the action index, not the named result. |
| `menu_item_drop.c` / `menu_save_load_hub` | 220 | `1` | `action = -1;` | No pending System row action. This signed index sentinel is distinct from the root result channel and is reset before the result-exit guard. |
| `menu_item_drop.c` / `menu_save_load_hub` | 226 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_load_hub` | 227 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 227 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 229 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_item_drop.c` / `menu_save_load_hub` | 233 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 233 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 239 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 239 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 247 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_load_hub` | 247 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 271 | `0` | `s32 cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_item_drop.c` / `menu_save_panel` | 273 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_item_drop.c` / `menu_save_panel` | 280 | `1` | `if (status != 1 && status != 3) {` | Retain the save panel's exact accepted catalogue values one and three. The reviewed header wrapper normalizes raw status three to one, so this comparison does not prove a distinct reachable catalogue-result meaning for three. Do not rename it to no-data or silently remove the extra acceptance. |
| `menu_item_drop.c` / `menu_save_panel` | 280 | `3` | `if (status != 1 && status != 3) {` | Retain the save panel's exact accepted catalogue values one and three. The reviewed header wrapper normalizes raw status three to one, so this comparison does not prove a distinct reachable catalogue-result meaning for three. Do not rename it to no-data or silently remove the extra acceptance. |
| `menu_item_drop.c` / `menu_save_panel` | 282 | `1` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 282 | `0` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 290 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 290 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 301 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 301 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 308 | `1` | `if (status == 1) {` | Temporary-file cleanup reports one when open succeeded (file != -1), after close/erase calls. This gates the decoded existing-game-data formatting question; it does not independently prove that erase succeeded. |
| `menu_item_drop.c` / `menu_save_panel` | 310 | `1` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 310 | `0` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 319 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 319 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 329 | `0` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_save_panel` | 329 | `3` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_save_panel` | 336 | `1` | `status = save_system_write_slot(cursor + 1);` | Convert zero-based visible slot index to the one-based on-card slot ID; the unit adjustment is the encoding boundary. |
| `menu_item_drop.c` / `menu_save_panel` | 339 | `0` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_save_panel` | 339 | `3` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_save_panel` | 346 | `1` | `status = memory_card_check_or_format(1);` | Boolean allow_format is enabled because the user has accepted the separate confirmation; the card helper has its own normalized result protocol. |
| `menu_item_drop.c` / `menu_save_panel` | 347 | `0` | `memset(summaries, 0, sizeof(summaries));` | Zero every local summary after the format attempt, preserving the original unconditional clearing before its status check. |
| `menu_item_drop.c` / `menu_save_panel` | 350 | `1` | `if (status != 1) {` | Only normalized operation result one counts as success. Every other write/format result returns the panel to pending after displaying its existing status message; this is not confirmation result zero. |
| `menu_item_drop.c` / `menu_save_panel` | 351 | `1` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 351 | `0` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 359 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 359 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 373 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_save_panel` | 374 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 374 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 376 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_item_drop.c` / `menu_save_panel` | 380 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 380 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 385 | `0` | `cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_item_drop.c` / `menu_save_panel` | 386 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 386 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 391 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_save_panel` | 391 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 416 | `0` | `s32 cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_item_drop.c` / `menu_load_panel` | 418 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_item_drop.c` / `menu_load_panel` | 423 | `1` | `if (save_system_read_catalog(summaries) != 1) {` | The load panel accepts only normalized catalogue result one, preserving its narrower test than the save panel. The wrapper can map no-data to one, so this test alone does not prove a populated slot. |
| `menu_item_drop.c` / `menu_load_panel` | 425 | `1` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 425 | `0` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 433 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 433 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 444 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 444 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 454 | `0` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_load_panel` | 454 | `3` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_item_drop.c` / `menu_load_panel` | 461 | `1` | `if (save_system_read_slot(cursor + 1) != 1) {` | Convert the zero-based row to a one-based file slot and require normalized read result one; all other outcomes keep the panel open after acknowledgement. |
| `menu_item_drop.c` / `menu_load_panel` | 461 | `1` | `if (save_system_read_slot(cursor + 1) != 1) {` | Convert the zero-based row to a one-based file slot and require normalized read result one; all other outcomes keep the panel open after acknowledgement. |
| `menu_item_drop.c` / `menu_load_panel` | 462 | `1` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 462 | `0` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 470 | `1` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 470 | `0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 482 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_item_drop.c` / `menu_load_panel` | 483 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 483 | `0` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 485 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_item_drop.c` / `menu_load_panel` | 489 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 489 | `0` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 494 | `0` | `cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_item_drop.c` / `menu_load_panel` | 495 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 495 | `0` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 500 | `0` | `} else if (summaries[cursor].current_hp == 0) {` | Reject a slot whose summary HP is zero. Preserve this actual test without treating positive HP as independent proof of a valid save file; the catalogue helper's partial initial clearing remains unchanged. |
| `menu_item_drop.c` / `menu_load_panel` | 506 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_item_drop.c` / `menu_load_panel` | 506 | `0` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_draw_window.c` / `menu_draw_window` | 21 | `0` | `if ((s16)layout->title.x != 0) {` | A zero title X coordinate suppresses the title. Preserve the signed-halfword interpretation and coordinate-based guard; this is not a glyph terminator test. |
| `menu_draw_window.c` / `menu_draw_window` | 26 | `0` | `if (count > 0) {` | Positive row count enters the drawing loop; zero and negative counts draw no selectable rows. |
| `menu_draw_window.c` / `menu_draw_window` | 27 | `0` | `row = 0;` | Start at the first zero-based row in the selected layout. |
