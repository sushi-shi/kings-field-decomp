# GAME save-system retained-literal ledger

Complete per-occurrence ledger for `src/game/save_system.c` after the
[status-domain review](game-save-status-domain.md). Claims, named definitions,
comments, string contents and identifier digits are excluded. Signs remain in
expressions; repeated tokens have separate rows. Retained encoded statuses
and unconsumed storage do not acquire invented semantic identities.

**130 retained occurrences.**

| Function | Line | Token | Expression | Reason |
| --- | ---: | --- | --- | --- |
| `save_system_read_catalog` | 78 | `0` | `memset(summaries, 0, 0x24);` | Observed partial clear: zero-fill 36 bytes although the three output summaries occupy 72. Its rationale is unresolved; preserve the exact span. |
| `save_system_read_catalog` | 78 | `0x24` | `memset(summaries, 0, 0x24);` | Observed partial clear: zero-fill 36 bytes although the three output summaries occupy 72. Its rationale is unresolved; preserve the exact span. |
| `save_system_read_catalog` | 82 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `menu_play_input_sound` | 106 | `0xe` | `sound.program = 0xe;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 107 | `0x44` | `sound.note = 0x44;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 109 | `0xd` | `sound.program = 0xd;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 110 | `0x3c` | `sound.note = 0x3c;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 112 | `0xf` | `sound.program = 0xf;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 113 | `0x3f` | `sound.note = 0x3f;` | Authored VAB program/note pair for cursor (14/68), confirm (13/60) or cancel/error (15/63), independently identified by callers; these are audio recipe values rather than menu-state codes. |
| `menu_play_input_sound` | 117 | `0` | `VSync(0);` | SDK vertical-sync wait mode zero. |
| `memory_card_initialize` | 124 | `0x80` | `u8 buffer[0x80];` | Retail retains this 128-byte initialized local without passing it to any service or otherwise reading it. Its purpose remains unresolved; equal card-sector size does not identify a sector buffer. |
| `memory_card_initialize` | 126 | `0xff` | `memset(buffer, 0xff, sizeof(buffer));` | Fill the unconsumed local with all-one bytes as retail does; no established consumer explains that initialization policy. |
| `memory_card_initialize` | 127 | `0` | `memory_card_io_end_event = OpenEvent(HwCARD, EvSpIOE, EvMdNOINTR, 0);` | Null callback for the selected non-interrupt BIOS event mode; each event specification already names its role. |
| `memory_card_initialize` | 128 | `0` | `memory_card_timeout_event = OpenEvent(HwCARD, EvSpTIMOUT, EvMdNOINTR, 0);` | Null callback for the selected non-interrupt BIOS event mode; each event specification already names its role. |
| `memory_card_initialize` | 129 | `0` | `memory_card_new_device_event = OpenEvent(HwCARD, EvSpNEW, EvMdNOINTR, 0);` | Null callback for the selected non-interrupt BIOS event mode; each event specification already names its role. |
| `memory_card_initialize` | 130 | `0` | `memory_card_error_event = OpenEvent(HwCARD, EvSpERROR, EvMdNOINTR, 0);` | Null callback for the selected non-interrupt BIOS event mode; each event specification already names its role. |
| `memory_card_initialize` | 137 | `0` | `_card_auto(0);` | Retained SDK control argument zero; this call alone does not establish a further project-specific mode identity. |
| `memory_card_begin_status_check` | 154 | `0` | `if (_card_info(KF_CARD_CHANNEL) != 0) {` | BIOS operation-start result: zero means the request did not start. The guard preserves the direct failure exit versus asynchronous event polling. |
| `memory_card_wait_event` | 173 | `1` | `if (TestEvent(memory_card_io_end_event) == 1) {` | Require the exact BIOS event-ready result one, preserving the IO-end/timeout/new-device/error polling order. |
| `memory_card_wait_event` | 177 | `1` | `if (TestEvent(memory_card_timeout_event) == 1) {` | Require the exact BIOS event-ready result one, preserving the IO-end/timeout/new-device/error polling order. |
| `memory_card_wait_event` | 181 | `1` | `if (TestEvent(memory_card_new_device_event) == 1) {` | Require the exact BIOS event-ready result one, preserving the IO-end/timeout/new-device/error polling order. |
| `memory_card_wait_event` | 185 | `1` | `if (TestEvent(memory_card_error_event) == 1) {` | Require the exact BIOS event-ready result one, preserving the IO-end/timeout/new-device/error polling order. |
| `memory_card_check_or_format` | 233 | `9` | `case KF_ENUM_DECODE(KfSaveStatus, 9):` | Internal status 9/10 maps to an unusable-card image (and formatting failure); no distinct producer establishes separate meanings. Keep the encoded case explicit rather than inventing a status identity. |
| `memory_card_check_or_format` | 234 | `10` | `case KF_ENUM_DECODE(KfSaveStatus, 10):` | Internal status 9/10 maps to an unusable-card image (and formatting failure); no distinct producer establishes separate meanings. Keep the encoded case explicit rather than inventing a status identity. |
| `memory_card_format` | 251 | `0` | `s32 attempt = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `memory_card_format` | 259 | `1` | `if (formatted == 1) {` | The SDK format call reports exact success one. Keep the same comparison for the early break and final failure check; the attempt limit includes the first call. |
| `memory_card_format` | 264 | `1` | `if (formatted != 1) {` | The SDK format call reports exact success one. Keep the same comparison for the early break and final failure check; the attempt limit includes the first call. |
| `save_file_write_slot` | 331 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_write_slot` | 333 | `16` | `file = open(save_main_file_path, O_CREAT \| (SAVE_FILE_BLOCKS << 16));` | The BIOS create request stores the file block count in the high halfword; shift sixteen encodes that format independently of the five I/O attempts. |
| `save_file_write_slot` | 335 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_write_slot` | 339 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_write_slot` | 347 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_write_slot` | 354 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_write_slot` | 362 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_write_slot` | 374 | `0` | `for (index = 0; index < KF_MAGIC_RECORD_COUNT; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_write_slot` | 379 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_write_slot` | 383 | `0` | `index = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `save_file_write_slot` | 409 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_write_slot` | 412 | `0` | `index = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `save_file_write_slot` | 415 | `0` | `lseek(file, 0, SEEK_SET);` | Seek to byte zero at the start of the save header, relative to the named file-origin seek mode. |
| `save_file_read_header` | 477 | `0` | `memset(save_header_buffer, 0, sizeof(KfSaveHeader));` | Zero byte pattern initializes the specified typed header/payload range; preserve the exact destination and extent. |
| `save_file_read_header` | 481 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_read_header` | 484 | `0` | `attempt = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `save_file_read_header` | 487 | `0` | `lseek(file, 0, SEEK_SET);` | Seek to byte zero at the start of the save header, relative to the named file-origin seek mode. |
| `save_file_read_slot` | 555 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_read_slot` | 566 | `1` | `if (file == -1) {` | SDK file-open failure sentinel minus one. Preserve the operation-specific error path and any preceding unconditional close; this is separate from a directory index. |
| `save_file_read_slot` | 569 | `0` | `index = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `save_file_read_slot` | 572 | `0` | `lseek(file, 0, SEEK_SET);` | Seek to byte zero at the start of the save header, relative to the named file-origin seek mode. |
| `save_file_read_slot` | 584 | `0` | `for (index = 0; index < KF_SAVE_DIRECTORY_ENTRIES; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_file_read_slot` | 602 | `0` | `index = 0;` | Start the attempt counter at zero before the first operation; the named bound limits total attempts, including that first call. |
| `save_file_read_slot` | 625 | `0` | `for (index = 0; index < KF_MAGIC_RECORD_COUNT; index++) {` | Begin the complete named directory-entry or magic-record scan at its first zero-based index. |
| `save_workspace_allocate` | 637 | `0` | `if (save_header_buffer == 0) {` | Null pointer reports allocation failure. |
| `save_workspace_allocate` | 638 | `1` | `return -1;` | Negative allocation-failure result; this workspace API uses zero on success. |
| `save_workspace_allocate` | 640 | `1` | `save_payload_buffer = (KfSavePayload *)(save_header_buffer + 1);` | The payload begins after exactly one complete typed header in the contiguous allocation. |
| `save_workspace_allocate` | 641 | `0` | `memset(save_header_buffer, 0, sizeof(KfSaveHeader));` | Zero byte pattern initializes the specified typed header/payload range; preserve the exact destination and extent. |
| `save_workspace_allocate` | 642 | `0` | `memset(save_payload_buffer, 0, sizeof(KfSavePayload));` | Zero byte pattern initializes the specified typed header/payload range; preserve the exact destination and extent. |
| `save_workspace_allocate` | 643 | `0` | `return 0;` | Zero reports successful workspace allocation and initialization. |
| `save_file_initialize_buffers` | 657 | `0` | `memset(save_header_buffer, 0, sizeof(KfSaveHeader));` | Zero byte pattern initializes the specified typed header/payload range; preserve the exact destination and extent. |
| `save_file_initialize_buffers` | 658 | `0` | `save_header_buffer->playstation_header.magic[0] = 'S';` | Required PlayStation card-header signature bytes S/C and their consecutive byte positions. |
| `save_file_initialize_buffers` | 658 | `'S'` | `save_header_buffer->playstation_header.magic[0] = 'S';` | Required PlayStation card-header signature bytes S/C and their consecutive byte positions. |
| `save_file_initialize_buffers` | 659 | `1` | `save_header_buffer->playstation_header.magic[1] = 'C';` | Required PlayStation card-header signature bytes S/C and their consecutive byte positions. |
| `save_file_initialize_buffers` | 659 | `'C'` | `save_header_buffer->playstation_header.magic[1] = 'C';` | Required PlayStation card-header signature bytes S/C and their consecutive byte positions. |
| `save_file_initialize_buffers` | 666 | `0` | `memcpy(save_header_buffer->playstation_header.icon_frames[0], &image[SAVE_ICON_TIM_PIXELS_OFFSET],` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 667 | `0` | `sizeof(save_header_buffer->playstation_header.icon_frames[0]));` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 669 | `1` | `memcpy(save_header_buffer->playstation_header.icon_frames[1], &image[SAVE_ICON_TIM_PIXELS_OFFSET],` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 670 | `1` | `sizeof(save_header_buffer->playstation_header.icon_frames[1]));` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 672 | `2` | `memcpy(save_header_buffer->playstation_header.icon_frames[2], &image[SAVE_ICON_TIM_PIXELS_OFFSET],` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 673 | `2` | `sizeof(save_header_buffer->playstation_header.icon_frames[2]));` | The destination and sizeof select the same ordered icon frame, loaded from ICO1, ICO2 or ICO3. Frame positions are zero-based indices. |
| `save_file_initialize_buffers` | 674 | `0` | `memset(save_payload_buffer, 0, sizeof(KfSavePayload));` | Zero byte pattern initializes the specified typed header/payload range; preserve the exact destination and extent. |
| `memory_card_show_status_message` | 691 | `1` | `message = -1; /* Distinct from the image loader's 255 skip value. */` | The status-OK arm selects literal image value minus one; it is distinct from the loader skip code 255 and would still enter filename construction. |
| `memory_card_show_status_message` | 715 | `9` | `case KF_ENUM_DECODE(KfSaveStatus, 9):` | Internal status 9/10 maps to an unusable-card image (and formatting failure); no distinct producer establishes separate meanings. Keep the encoded case explicit rather than inventing a status identity. |
| `memory_card_show_status_message` | 716 | `10` | `case KF_ENUM_DECODE(KfSaveStatus, 10):` | Internal status 9/10 maps to an unusable-card image (and formatting failure); no distinct producer establishes separate meanings. Keep the encoded case explicit rather than inventing a status identity. |
| `memory_card_show_status_message` | 733 | `1` | `if (result == 1) {` | Image-loader failure one takes the explicit minus-one return. The successful C path falls through; all retail callers discard the result. |
| `memory_card_show_status_message` | 734 | `1` | `return -1;` | Map the image-loader failure result one to minus one. This is not the selected image ID, even though the OK image arm also uses minus one. |
| `menu_load_message_image` | 742 | `16` | `char path[16] = "TIM\\M000.";` | Sixteen-byte local workspace for the TIM/Mddd. path and terminator; the original extra-capacity rationale is unresolved. |
| `menu_load_message_image` | 747 | `100` | `remainder = message_id % 100;` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 748 | `5` | `path[5] = message_id / 100 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 748 | `100` | `path[5] = message_id / 100 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 748 | `'0'` | `path[5] = message_id / 100 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 749 | `6` | `path[6] = remainder / 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 749 | `10` | `path[6] = remainder / 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 749 | `'0'` | `path[6] = remainder / 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 750 | `7` | `path[7] = remainder % 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 750 | `10` | `path[7] = remainder % 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 750 | `'0'` | `path[7] = remainder % 10 + '0';` | Decimal image filename encoding: hundreds at path byte five, tens at six, units at seven. Division/modulo by ten or one hundred and ASCII zero express the number format. |
| `menu_load_message_image` | 752 | `0` | `if (cd_file_load_into(buffer, path) != 0) {` | Nonzero CD-load result takes the loader failure path; zero permits TIM upload. |
| `menu_load_message_image` | 753 | `1` | `return 1;` | One reports message-image CD loading failure to the status dispatcher. |
| `menu_load_message_image` | 757 | `0` | `return 0;` | Zero reports successful upload or the explicit skip-ID path. |
| `save_file_cleanup_temporary` | 766 | `0` | `if (_card_info(KF_CARD_CHANNEL) == 0) {` | BIOS operation-start result: zero means the request did not start. The guard preserves the direct failure exit versus asynchronous event polling. |
| `save_file_cleanup_temporary` | 774 | `1` | `return KF_ENUM_DECODE(KfSaveCleanupResult, file != -1);` | SDK open failure is the descriptor sentinel -1. Convert its inequality result into the cleanup open-result domain; close and erase results are not examined. |
| `screen_show_image_until_input` | 784 | `0` | `u8 pressed = 0;` | Begin by waiting for all buttons to be released before accepting a new press. |
| `screen_show_image_until_input` | 787 | `0` | `DrawSync(0);` | SDK blocking draw-synchronization mode zero. |
| `screen_show_image_until_input` | 789 | `1` | `SetSemiTrans(&polygon, 1);` | Enable semitransparency through the SDK primitive flag. |
| `screen_show_image_until_input` | 798 | `0` | `polygon.u0 = 0;` | Texture U/V coordinate origin; the far edges use the shared screen texture spans. |
| `screen_show_image_until_input` | 799 | `0` | `polygon.v0 = 0;` | Texture U/V coordinate origin; the far edges use the shared screen texture spans. |
| `screen_show_image_until_input` | 801 | `0` | `polygon.v1 = 0;` | Texture U/V coordinate origin; the far edges use the shared screen texture spans. |
| `screen_show_image_until_input` | 802 | `0` | `polygon.u2 = 0;` | Texture U/V coordinate origin; the far edges use the shared screen texture spans. |
| `screen_show_image_until_input` | 806 | `0` | `polygon.clut = GetClut(0, KF_SYSTEM_SCREEN_CLUT_Y);` | Measured palette X origin zero; its Y coordinate is the shared screen palette row. |
| `screen_show_image_until_input` | 810 | `0` | `if (cd_file_load_into(game_graphics_runtime.display_state.asset_load_buffer, path) != 0) {` | Nonzero CD-load result aborts before texture upload and display setup. |
| `screen_show_image_until_input` | 814 | `0` | `index = game_graphics_runtime.display_state.buffer_index == 0;` | Choose the opposite double-buffer index using equality with zero. |
| `screen_show_image_until_input` | 815 | `0` | `game_graphics_runtime.display_draw_environments[index].isbg = 0;` | Clear the selected draw environment background/display-area flags for the image loop and restore both to one afterward; preserve the explicit assignments. |
| `screen_show_image_until_input` | 816 | `0` | `game_graphics_runtime.display_draw_environments[index].dfe = 0;` | Clear the selected draw environment background/display-area flags for the image loop and restore both to one afterward; preserve the explicit assignments. |
| `screen_show_image_until_input` | 828 | `0` | `DrawSync(0);` | SDK blocking draw-synchronization mode zero. |
| `screen_show_image_until_input` | 829 | `1` | `DrawOTag(&game_graphics_runtime.display_state.ordering_table[KF_ORDERING_TABLE_LENGTH - 1]);` | Last zero-based element of the reverse-linked ordering table. |
| `screen_show_image_until_input` | 830 | `0` | `if (pressed == 0) {` | Release has not yet been observed; the initial held buttons do not count as acknowledgement. |
| `screen_show_image_until_input` | 831 | `1` | `if (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 831 | `0` | `if (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 832 | `1` | `pressed = 1;` | Latch the observed release, enabling the following press and final release sequence. |
| `screen_show_image_until_input` | 834 | `1` | `} else if (PadRead(1) != 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 834 | `0` | `} else if (PadRead(1) != 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 835 | `1` | `while (PadRead(1) != 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 835 | `0` | `while (PadRead(1) != 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits indicate release and nonzero bits indicate a press; the loop preserves release/press/release order. |
| `screen_show_image_until_input` | 840 | `1` | `game_graphics_runtime.display_draw_environments[index].isbg = 1;` | Clear the selected draw environment background/display-area flags for the image loop and restore both to one afterward; preserve the explicit assignments. |
| `screen_show_image_until_input` | 841 | `1` | `game_graphics_runtime.display_draw_environments[index].dfe = 1;` | Clear the selected draw environment background/display-area flags for the image loop and restore both to one afterward; preserve the explicit assignments. |
| `screen_show_image_until_input` | 842 | `0` | `DrawSync(0);` | SDK blocking draw-synchronization mode zero. |
| `talk_show_dialogue_page` | 848 | `10` | `s32 tens = KF_ENUM_ENCODE(s32, character_id) / 10;` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 849 | `10` | `s32 ones = KF_ENUM_ENCODE(s32, character_id) % 10;` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 851 | `6` | `char *directory_character = &talk_image_path_template[6];` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 853 | `0xa` | `talk_image_path_template[0xa] = KF_ENUM_ENCODE(u8, floor) + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 853 | `'0'` | `talk_image_path_template[0xa] = KF_ENUM_ENCODE(u8, floor) + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 854 | `0xb` | `talk_image_path_template[0xb] = stage + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 854 | `'0'` | `talk_image_path_template[0xb] = stage + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 855 | `0xe` | `talk_image_path_template[0xe] = page + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 855 | `'0'` | `talk_image_path_template[0xe] = page + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 856 | `0` | `directory_character[0] = talk_image_path_template[0xc] = tens + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 856 | `0xc` | `directory_character[0] = talk_image_path_template[0xc] = tens + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 856 | `'0'` | `directory_character[0] = talk_image_path_template[0xc] = tens + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 857 | `1` | `directory_character[1] = talk_image_path_template[0xd] = ones + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 857 | `0xd` | `directory_character[1] = talk_image_path_template[0xd] = ones + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |
| `talk_show_dialogue_page` | 857 | `'0'` | `directory_character[1] = talk_image_path_template[0xd] = ones + '0';` | Decimal dialogue-path construction: tens/ones select both directory and character digits, while fixed filename positions receive floor, stage and page. Subscripts identify path bytes, and ASCII zero/base ten encode digits. |

The [save-result review](save-result-domains.md) names the wrapper outputs and
format-confirmation state, with a separate cleanup result and typed shared
storage in the save panel. The remaining encoded catalogue value three and
internal statuses nine/ten retain the documented evidence limits.
