# Effect-pool remainder literal ledger

Complete per-occurrence account of **224 retained numeric literals** in `src/game/effect_pool.c`,
including the existing alignment/offset witnesses. The
[map-effect follow-up](game-map-effect-identities.md) names the former kind-48
occurrence and updates this ledger. The [constructor review](game-effect-pool-constants.md)
records named values, effect identities and verification. Named definitions and retail claims
are excluded; comments/strings/identifier digits are not literals, and signs are operators.
`effect_pool_set_current` has no remaining numeric literals.
The [warp-mode review](game-warp-mode-domain.md) names kind 21 and establishes
its Y-scale and yaw consumers; its model asset remains unidentified.

| Function | Line | Token | Expression | Reason |
| --- | ---: | --- | --- | --- |
| `initializers` | 5 | `4` | `typedef char effect_state_alignment[__alignof__(KfEffectState) == 4 ? 1 : -1];` | Existing four-byte alignment witness for the shared effect owner; one/-one are the C typedef success/failure extents. Keep the measured ABI constraint explicit. |
| `initializers` | 5 | `1` | `typedef char effect_state_alignment[__alignof__(KfEffectState) == 4 ? 1 : -1];` | Existing four-byte alignment witness for the shared effect owner; one/-one are the C typedef success/failure extents. Keep the measured ABI constraint explicit. |
| `initializers` | 5 | `1` | `typedef char effect_state_alignment[__alignof__(KfEffectState) == 4 ? 1 : -1];` | Existing four-byte alignment witness for the shared effect owner; one/-one are the C typedef success/failure extents. Keep the measured ABI constraint explicit. |
| `initializers` | 7 | `0` | `(u32)&((KfEffectState *)0)->records == 0x1e0 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 7 | `0x1e0` | `(u32)&((KfEffectState *)0)->records == 0x1e0 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 7 | `1` | `(u32)&((KfEffectState *)0)->records == 0x1e0 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 7 | `1` | `(u32)&((KfEffectState *)0)->records == 0x1e0 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 9 | `0` | `(u32)&((KfEffectState *)0)->current_magic == 0xd20 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 9 | `0xd20` | `(u32)&((KfEffectState *)0)->current_magic == 0xd20 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 9 | `1` | `(u32)&((KfEffectState *)0)->current_magic == 0xd20 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 9 | `1` | `(u32)&((KfEffectState *)0)->current_magic == 0xd20 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 11 | `0` | `(u32)&((KfEffectState *)0)->current_record == 0xd24 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 11 | `0xd24` | `(u32)&((KfEffectState *)0)->current_record == 0xd24 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 11 | `1` | `(u32)&((KfEffectState *)0)->current_record == 0xd24 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `initializers` | 11 | `1` | `(u32)&((KfEffectState *)0)->current_record == 0xd24 ? 1 : -1];` | Existing member-offset witness for the shared effect owner: records 480, current_magic 3360, current_record 3364 bytes. Null-based member arithmetic and one/-one implement the C typedef check; these are ABI measurements, not gameplay settings. |
| `effect_pool_find_free` | 41 | `0` | `} while (--i != 0);` | The predecrement halfword loop stops at zero after visiting exactly the named capacity; zero is count exhaustion. |
| `effect_pool_find_free` | 42 | `0` | `return 0;` | The free-slot scan returns null after exhausting all slots; zero expresses pointer absence. |
| `effect_pool_construct` | 56 | `0` | `if (record != 0) {` | Only a non-null result from the free-slot scan may be initialized; zero is the allocation-miss pointer. |
| `effect_pool_construct` | 61 | `0` | `record->phase = 0;` | Common initialization starts the kind-specific age at zero. Some kinds override it before return, so this is not a universal lifecycle enumerator. |
| `effect_pool_construct` | 66 | `0` | `record->visual.animation_phase = 0;` | Initialize the animation/pulse union at the animation phase origin. Scatter construction later overwrites the same storage with its supplied base scale. |
| `effect_pool_construct` | 67 | `0` | `record->sound_played = 0;` | Clear the sound-acceptance boolean for later effect updates; it is distinct from sound definition slot zero. |
| `effect_pool_construct` | 74 | `0` | `record->base_render_id = 0;` | Authored render-resource index for KF_EFFECT_KIND_FIRE_BALL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 75 | `0` | `record->render_id = 0;` | Authored render-resource index for KF_EFFECT_KIND_FIRE_BALL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 76 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 77 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 78 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 79 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 84 | `5` | `record->base_render_id = 5;` | Authored render-resource index for KF_EFFECT_KIND_WIND_CUTTER: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 85 | `5` | `record->render_id = 5;` | Authored render-resource index for KF_EFFECT_KIND_WIND_CUTTER: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 87 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 88 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 89 | `2` | `if (va[2] != 0) {` | Index 2 relative to the saved fifth argument selects O32 argument 7, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 89 | `0` | `if (va[2] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 90 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 94 | `0x17` | `case KF_ENUM_DECODE(KfEffectKind, 0x17):` | Input kind 23 becomes Lightning Bolt kind 4 with billboard base 17 instead of 6. Its particular alternate resource identity is not established. |
| `effect_pool_construct` | 95 | `0x11` | `record->base_render_id = 0x11;` | Authored render-resource index for 0x17: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 96 | `0x11` | `record->render_id = 0x11;` | Authored render-resource index for 0x17: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 100 | `6` | `record->base_render_id = 6;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_BOLT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 101 | `6` | `record->render_id = 6;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_BOLT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 104 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 105 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 106 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 107 | `1` | `record->control.frames_remaining = *(u16 *)(va + 1);` | Slot 1 after the saved direction argument is O32 argument 6; this branch reads its frame-count halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 108 | `2` | `if (va[2] != 0) {` | Index 2 relative to the saved fifth argument selects O32 argument 7, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 108 | `0` | `if (va[2] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 110 | `0` | `&magic_records[KF_ENUM_ENCODE(u8, KF_MAGIC_LIGHTNING_BOLT)].sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 116 | `0x29` | `case KF_ENUM_DECODE(KfEffectKind, 0x29):` | Input kind 41 becomes Lightning Impact kind 32 with billboard base 19 instead of 11. The alternate resource identity remains unresolved. |
| `effect_pool_construct` | 117 | `0x13` | `record->base_render_id = 0x13;` | Authored render-resource index for 0x29: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 118 | `0x13` | `record->render_id = 0x13;` | Authored render-resource index for 0x29: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 122 | `0xb` | `record->base_render_id = 0xb;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_IMPACT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 123 | `0xb` | `record->render_id = 0xb;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_IMPACT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 126 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 127 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 128 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 130 | `1` | `&magic_records[KF_ENUM_ENCODE(u8, KF_MAGIC_LIGHTNING_BOLT)].sounds[1],` | Select the second SoundRef of the explicitly named magic row: lightning impact or Moonlight construction. This slot is not universally an impact-only cue. |
| `effect_pool_construct` | 135 | `0x2a` | `case KF_ENUM_DECODE(KfEffectKind, 0x2a):` | Input kind 42 becomes Lightning Radial Blast kind 33 with model 15 instead of 0. The alternate resource identity remains unresolved. |
| `effect_pool_construct` | 136 | `0xf` | `record->base_render_id = 0xf;` | Authored render-resource index for 0x2a: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 137 | `0xf` | `record->render_id = 0xf;` | Authored render-resource index for 0x2a: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 139 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 140 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 141 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 142 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 145 | `0` | `record->base_render_id = 0;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_RADIAL_BLAST: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 146 | `0` | `record->render_id = 0;` | Authored render-resource index for KF_EFFECT_KIND_LIGHTNING_RADIAL_BLAST: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 147 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 148 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 149 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 150 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 153 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 154 | `1` | `record->base_render_id = 1;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_BRANCH: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 155 | `1` | `record->render_id = 1;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_BRANCH: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 156 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 157 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 158 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 159 | `0` | `record->scale_y = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 161 | `1` | `KfEffectGroundBranchRole branch_role = KF_ENUM_DECODE(KfEffectGroundBranchRole, *(u16 *)(va + 1));` | Slot 1 after the saved direction argument is O32 argument 6; this branch reads its branch role halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 166 | `0` | `sound_ref_play(&magic->sounds[0], EFFECT_GROUND_BRANCH_SOUND_VOLUME);` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 170 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 171 | `2` | `record->base_render_id = 2;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_BRANCH_VISUAL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 172 | `2` | `record->render_id = 2;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_BRANCH_VISUAL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 173 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 174 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 175 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 176 | `0` | `record->scale_y = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 179 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 180 | `3` | `record->base_render_id = 3;` | Authored render-resource index for KF_EFFECT_KIND_ACTOR_SPAWNER: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 181 | `3` | `record->render_id = 3;` | Authored render-resource index for KF_EFFECT_KIND_ACTOR_SPAWNER: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 182 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 183 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 184 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 185 | `0` | `record->scale_z = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 186 | `0` | `record->scale_y = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 187 | `0` | `record->scale_x = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 188 | `1` | `record->control.frames_remaining = *(u16 *)(va + 1);` | Slot 1 after the saved direction argument is O32 argument 6; this branch reads its frame-count halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 189 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 194 | `9` | `record->base_render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_SCATTER_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 195 | `9` | `record->render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_SCATTER_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 196 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 197 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 198 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 199 | `1` | `record->propagation.generations_remaining = *(u16 *)(va + 1);` | Slot 1 after the saved direction argument is O32 argument 6; this branch reads its generation halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 200 | `2` | `record->control.frames_remaining = *(u16 *)(va + 2);` | Slot 2 after the saved direction argument is O32 argument 7; this branch reads its frame-count halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 202 | `3` | `record->visual.pulse_base_scale = *(u16 *)(va + 3);` | Slot 3 after the saved direction argument is O32 argument 8; this branch reads its pulse/base-scale halfword. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 203 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 208 | `8` | `record->base_render_id = 8;` | Authored render-resource index for KF_EFFECT_KIND_DARKNESS_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 209 | `8` | `record->render_id = 8;` | Authored render-resource index for KF_EFFECT_KIND_DARKNESS_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 210 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 211 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 212 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 213 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 218 | `0xa` | `record->base_render_id = 0xa;` | Authored render-resource index for KF_EFFECT_KIND_CURSE_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 219 | `0xa` | `record->render_id = 0xa;` | Authored render-resource index for KF_EFFECT_KIND_CURSE_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 220 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 221 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 222 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 223 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 227 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 228 | `9` | `record->base_render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_EMERGING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 229 | `9` | `record->render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_EMERGING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 230 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 231 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 232 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 239 | `0xe` | `case KF_ENUM_DECODE(KfEffectKind, 0xe):` | Kind 14 selects model 4 and copies a supplied rotation. Its shared collision path is known, but the specific projectile identity is unresolved. |
| `effect_pool_construct` | 240 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 241 | `4` | `record->base_render_id = 4;` | Authored render-resource index for 0xe: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 242 | `4` | `record->render_id = 4;` | Authored render-resource index for 0xe: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 243 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 246 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 247 | `5` | `record->base_render_id = 5;` | Authored render-resource index for 0x30: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 248 | `5` | `record->render_id = 5;` | Authored render-resource index for 0x30: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 249 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 252 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 253 | `6` | `record->base_render_id = 6;` | Authored render-resource index for KF_EFFECT_KIND_LIGHT_NEEDLE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 254 | `6` | `record->render_id = 6;` | Authored render-resource index for KF_EFFECT_KIND_LIGHT_NEEDLE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 255 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 257 | `2` | `if (va[2] != 0) {` | Index 2 relative to the saved fifth argument selects O32 argument 7, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 257 | `0` | `if (va[2] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 258 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 262 | `0x16` | `case KF_ENUM_DECODE(KfEffectKind, 0x16):` | Kind 22 selects model 14 and an optional sound. Its damage-channel path differs from Light Needle despite similar construction; do not infer identity from shape. |
| `effect_pool_construct` | 263 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 264 | `0xe` | `record->base_render_id = 0xe;` | Authored render-resource index for 0x16: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 265 | `0xe` | `record->render_id = 0xe;` | Authored render-resource index for 0x16: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 266 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 268 | `2` | `if (va[2] != 0) {` | Index 2 relative to the saved fifth argument selects O32 argument 7, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 268 | `0` | `if (va[2] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 269 | `0` | `audio_play_spatial_default_range(&magic->sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 274 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 275 | `7` | `record->base_render_id = 7;` | Authored render-resource index for KF_EFFECT_KIND_SWINGING_HAZARD_SHORT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 276 | `7` | `record->render_id = 7;` | Authored render-resource index for KF_EFFECT_KIND_SWINGING_HAZARD_SHORT: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 277 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 279 | `0` | `record->direction.words.z = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 280 | `0` | `record->direction.words.y = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 281 | `0` | `record->direction.words.x = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 287 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 288 | `7` | `record->base_render_id = 7;` | Authored render-resource index for KF_EFFECT_KIND_SWINGING_HAZARD_LONG: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 289 | `7` | `record->render_id = 7;` | Authored render-resource index for KF_EFFECT_KIND_SWINGING_HAZARD_LONG: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 290 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 292 | `0` | `record->direction.words.z = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 293 | `0` | `record->direction.words.y = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 294 | `0` | `record->direction.words.x = 0;` | Clear the swinging hazard control halfword: X supplies angular velocity to the 3D helper, while this constructor also initializes Y/Z to zero. Do not assign unsupported independent meanings to those other lanes. |
| `effect_pool_construct` | 297 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 298 | `9` | `record->base_render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_ORBITING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 299 | `9` | `record->render_id = 9;` | Authored render-resource index for KF_EFFECT_KIND_ORBITING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 300 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 301 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 302 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 303 | `0` | `record->control.orbit_angle = 0;` | Start the orbit at angular origin zero; it advances by the existing fraction of a full turn. |
| `effect_pool_construct` | 312 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 313 | `0xa` | `record->base_render_id = 0xa;` | Authored render-resource index for KF_EFFECT_KIND_MOONLIGHT_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 314 | `0xa` | `record->render_id = 0xa;` | Authored render-resource index for KF_EFFECT_KIND_MOONLIGHT_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 315 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 316 | `0xff` | `record->control.bytes.high = 0xff;` | Moonlight construction stores 0xff in this separate control byte; no behavioral read establishes its meaning. Do not borrow a free-slot, target or completed-timer sentinel from another field. |
| `effect_pool_construct` | 317 | `0xff` | `record->control.bytes.low = 0xff;` | Moonlight construction stores 0xff in this separate control byte; no behavioral read establishes its meaning. Do not borrow a free-slot, target or completed-timer sentinel from another field. |
| `effect_pool_construct` | 320 | `1` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_RADIAL_BLAST)].sounds[1],` | Select the second SoundRef of the explicitly named magic row: lightning impact or Moonlight construction. This slot is not universally an impact-only cue. |
| `effect_pool_construct` | 327 | `0xe` | `record->base_render_id = 0xe;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_TRAIL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 328 | `0xe` | `record->render_id = 0xe;` | Authored render-resource index for KF_EFFECT_KIND_GROUND_TRAIL: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 330 | `1` | `u8 argument = *(u8 *)(va + 1);` | Slot 1 after the saved direction argument is O32 argument 6; this branch reads its parent-effect byte. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 331 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 332 | `0` | `record->rotation.vy = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 333 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 337 | `0x2c` | `case KF_ENUM_DECODE(KfEffectKind, 0x2c):` | Input kind 44 becomes Radial Blast kind 18 with model 17 instead of 11; the homing model-16 variant requests this child. Its specific visual identity remains unresolved. |
| `effect_pool_construct` | 338 | `0x11` | `record->base_render_id = 0x11;` | Authored render-resource index for 0x2c: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 339 | `0x11` | `record->render_id = 0x11;` | Authored render-resource index for 0x2c: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 341 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 342 | `1` | `if (va[1] != 0) {` | Index 1 relative to the saved fifth argument selects O32 argument 6, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 342 | `0` | `if (va[1] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 344 | `0` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_RADIAL_BLAST)].sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 351 | `0xb` | `record->base_render_id = 0xb;` | Authored render-resource index for KF_EFFECT_KIND_RADIAL_BLAST: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 352 | `0xb` | `record->render_id = 0xb;` | Authored render-resource index for KF_EFFECT_KIND_RADIAL_BLAST: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 353 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 354 | `1` | `if (va[1] != 0) {` | Index 1 relative to the saved fifth argument selects O32 argument 6, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 354 | `0` | `if (va[1] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 356 | `0` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_RADIAL_BLAST)].sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 362 | `0x18` | `case KF_ENUM_DECODE(KfEffectKind, 0x18):` | Input kind 24 becomes Homing Projectile kind 20 with model 16 instead of 12, retaining the supplied target control and half X/Y scale. Its specific variant identity remains unresolved. |
| `effect_pool_construct` | 363 | `0x10` | `record->base_render_id = 0x10;` | Authored render-resource index for 0x18: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 364 | `0x10` | `record->render_id = 0x10;` | Authored render-resource index for 0x18: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 366 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 367 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 369 | `2` | `record->control.target_mode = KF_ENUM_DECODE(KfEffectHomingMode, *(u8 *)(va + 2));` | Slot 2 after the saved direction argument is O32 argument 7; this branch reads its target-control byte. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 371 | `2` | `record->scale_y = KF_FIXED12_ONE / 2;` | Homing construction gives X/Y exactly half Q12 unity while Z retains unity; two expresses the geometric half ratio. |
| `effect_pool_construct` | 372 | `2` | `record->scale_x = KF_FIXED12_ONE / 2;` | Homing construction gives X/Y exactly half Q12 unity while Z retains unity; two expresses the geometric half ratio. |
| `effect_pool_construct` | 373 | `3` | `if (va[3] != 0) {` | Index 3 relative to the saved fifth argument selects O32 argument 8, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 373 | `0` | `if (va[3] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 375 | `0` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_HOMING_PROJECTILE)].sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 380 | `0xc` | `record->base_render_id = 0xc;` | Authored render-resource index for KF_EFFECT_KIND_HOMING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 381 | `0xc` | `record->render_id = 0xc;` | Authored render-resource index for KF_EFFECT_KIND_HOMING_PROJECTILE: the branch chooses billboard or model mode separately. Base and current selector start equal. Keep the resource ordinal until the corresponding sprite/model asset has an independent semantic identity; equal indices in different modes do not identify the same resource. |
| `effect_pool_construct` | 382 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 383 | `1` | `record->rotation = *(const SVECTOR *)va[1];` | Slot one after the saved direction argument is argument six, a pointer to the supplied SVECTOR rotation. Preserve its pointer interpretation and SDK copy. |
| `effect_pool_construct` | 385 | `2` | `record->control.target_mode = KF_ENUM_DECODE(KfEffectHomingMode, *(u8 *)(va + 2));` | Slot 2 after the saved direction argument is O32 argument 7; this branch reads its target-control byte. Keep positional arithmetic and the original width instead of conflating variadic domains. |
| `effect_pool_construct` | 387 | `2` | `record->scale_y = KF_FIXED12_ONE / 2;` | Homing construction gives X/Y exactly half Q12 unity while Z retains unity; two expresses the geometric half ratio. |
| `effect_pool_construct` | 388 | `2` | `record->scale_x = KF_FIXED12_ONE / 2;` | Homing construction gives X/Y exactly half Q12 unity while Z retains unity; two expresses the geometric half ratio. |
| `effect_pool_construct` | 389 | `3` | `if (va[3] != 0) {` | Index 3 relative to the saved fifth argument selects O32 argument 8, used here as the optional sound flag. The argument position varies by kind. |
| `effect_pool_construct` | 389 | `0` | `if (va[3] != 0) {` | Zero suppresses this optional constructor sound; any nonzero word enables the call. This is a boolean test of the selected argument slot. |
| `effect_pool_construct` | 391 | `0` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_HOMING_PROJECTILE)].sounds[0],` | Select the first stored SoundRef for this construction path. Other effect kinds use the same slot at different events, so no universal cast/impact enum is inferred. |
| `effect_pool_construct` | 396 | `0` | `record->animation_clip = 0;` | Select the first animation clip in this model resource. It is a resource ordinal; the billboard mode uses its own named sentinel. |
| `effect_pool_construct` | 397 | `0xd` | `record->base_render_id = 0xd;` | Authored model-resource index for the warp shimmer. Base and current selectors start equal. Keep the ordinal until the model geometry has an independent semantic identity. |
| `effect_pool_construct` | 398 | `0xd` | `record->render_id = 0xd;` | Authored model-resource index for the warp shimmer. Base and current selectors start equal. Keep the ordinal until the model geometry has an independent semantic identity. |
| `effect_pool_construct` | 399 | `0` | `record->scale_y = 0;` | This branch begins the indicated visual scale at zero for its later growth. Scale zero is a numeric origin, independently of pool liveness. |
| `effect_pool_construct` | 400 | `0x1800` | `record->scale_z = 0x1800;` | Warp-shimmer X/Z scale is 6144 Q12 units, exactly 1.5 unity; Y begins at zero. Preserve this authored model scale; the original tuning rationale is unresolved. |
| `effect_pool_construct` | 401 | `0x1800` | `record->scale_x = 0x1800;` | Warp-shimmer X/Z scale is 6144 Q12 units, exactly 1.5 unity; Y begins at zero. Preserve this authored model scale; the original tuning rationale is unresolved. |
| `effect_pool_construct` | 402 | `0` | `record->rotation.vx = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_construct` | 404 | `0` | `record->rotation.vz = 0;` | Zero the indicated Euler angle in this branch. These are neutral orientation components; other branches copy supplied angles or camera yaw. |
| `effect_pool_spawn_typed` | 421 | `0` | `if (record != 0) {` | Only a non-null result from the free-slot scan may be initialized; zero is the allocation-miss pointer. |
| `effect_pool_spawn_typed` | 434 | `0` | `record->direction.words.z = 0;` | Start the floor-controller progress accumulator at zero before forward interpolation. The same union serves motion for other effect kinds. |
