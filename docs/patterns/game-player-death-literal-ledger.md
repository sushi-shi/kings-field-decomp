# Retained player death, vitals and combat literals

Complete current ledger for all thirteen functions and initializers in
`src/game/player_death.c`: **139 numeric/character occurrences**. This combines
the previous death/combat remainder, startup and progression slices without
changing their reasons. The [damage review](game-player-damage-units.md),
[startup review](game-item-database-literal-ledger.md) and
[progression review](game-player-progression.md) retain their evidence scopes.

The [ledger reconciliation](game-retained-ledger-reconciliation.md) verified
every token and complete source-line expression, including duplicates. Claims,
comments, string contents, identifier digits and named definitions are excluded;
signs are expression operators. Every occurrence has its own row below. This
accounts for retained values; it does not prove all useful naming is exhausted.

The [floor enum review](game-floor-enum-domain.md) propagates floor identifiers
through the current source; this ledger reflects its named comparisons and
explicit numeric boundaries.

The [sound-slot review](game-player-sound-slots.md) names the table bound
and its death/level-up indices; the nine authored selector bytes remain below.

| Function | Line | Token | Expression | Reason |
| --- | ---: | --- | --- | --- |
| `initializers` | 19 | `7` | `{7, 0, 80},` | Program 7, tone 0, note 80 for the weapon-attack-start cue selected by player_begin_weapon_attack. These are authored VAB selectors, not HP or animation units. |
| `initializers` | 19 | `0` | `{7, 0, 80},` | Program 7, tone 0, note 80 for the weapon-attack-start cue selected by player_begin_weapon_attack. These are authored VAB selectors, not HP or animation units. |
| `initializers` | 19 | `80` | `{7, 0, 80},` | Program 7, tone 0, note 80 for the weapon-attack-start cue selected by player_begin_weapon_attack. These are authored VAB selectors, not HP or animation units. |
| `initializers` | 20 | `7` | `{7, 1, 89},` | Program 7, tone 1, note 89 for the death-start cue. Keep the distinct tone/note choice within the same program; original sound-design rationale is unknown. |
| `initializers` | 20 | `1` | `{7, 1, 89},` | Program 7, tone 1, note 89 for the death-start cue. Keep the distinct tone/note choice within the same program; original sound-design rationale is unknown. |
| `initializers` | 20 | `89` | `{7, 1, 89},` | Program 7, tone 1, note 89 for the death-start cue. Keep the distinct tone/note choice within the same program; original sound-design rationale is unknown. |
| `initializers` | 21 | `13` | `{13, 0, 67}` | Program 13, tone 0, note 67 for the level-up cue. Preserve the packed three-byte selector and its authored resource values. |
| `initializers` | 21 | `0` | `{13, 0, 67}` | Program 13, tone 0, note 67 for the level-up cue. Preserve the packed three-byte selector and its authored resource values. |
| `initializers` | 21 | `67` | `{13, 0, 67}` | Program 13, tone 0, note 67 for the level-up cue. Preserve the packed three-byte selector and its authored resource values. |
| `player_death_begin` | 38 | `0` | `player_state.death_camera_pitch_step = 0;` | Zero starts the named death pitch-step or visual-blend counter before saving the current render state; these counters have distinct consumers. |
| `player_death_begin` | 39 | `0` | `player_state.death_visual_blend = 0;` | Zero starts the named death pitch-step or visual-blend counter before saving the current render state; these counters have distinct consumers. |
| `game_state_initialize` | 56 | `0` | `player_state.experience = 0;` | New session begins with no accumulated experience; the next threshold is loaded separately from growth row zero. |
| `game_state_initialize` | 57 | `1` | `player_state.progress_state.level = 1;` | Initial displayed/player level is one; growth-table row zero supplies that level's base values. |
| `game_state_initialize` | 61 | `0` | `player_state.attack_charge_state.current = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 62 | `0` | `player_state.magic_charge = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 63 | `0` | `player_state.weapon_charge_delay = 0;` | Start with empty charge and no pending weapon-charge delay, preserving each field's separate role and initialization order. |
| `game_state_initialize` | 70 | `0` | `player_state.view_bob_offset = 0;` | Reset the view-bob displacement and phase to their zero origin; these are distinct state fields rather than a shared scale. |
| `game_state_initialize` | 71 | `0` | `player_state.view_bob_phase = 0;` | Reset the view-bob displacement and phase to their zero origin; these are distinct state fields rather than a shared scale. |
| `game_state_initialize` | 73 | `0` | `player_state.vertical_velocity = 0;` | Start with no vertical motion; zero is the velocity origin. |
| `game_state_initialize` | 74 | `0` | `player_state.vitals.current_hp = player_level_growth_table[0].maximum_hp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 75 | `0` | `player_state.vitals.maximum_hp = player_level_growth_table[0].maximum_hp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 76 | `0` | `player_state.vitals.current_mp = player_level_growth_table[0].maximum_mp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 77 | `0` | `player_state.vitals.maximum_mp = player_level_growth_table[0].maximum_mp;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 78 | `0` | `player_state.base_physical_power = player_level_growth_table[0].physical_power_step;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 79 | `0` | `player_state.base_magic = player_level_growth_table[0].magic_step;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 80 | `0` | `player_state.next_level_experience = player_level_growth_table[0].experience_threshold;` | Use the first zero-based growth row to seed level-one HP, MP, base-stat or next-experience values. Each destination keeps its existing source field. |
| `game_state_initialize` | 88 | `0` | `player_set_equipment_slot(KF_ENUM_DECODE(KfItemId, 0), KF_EQUIPMENT_SLOT_REFRESH_ONLY);` | Unused item argument zero when the named refresh-only slot bypasses every equipment assignment and refreshes pointers/stat state. |
| `game_state_initialize` | 97 | `1` | `count = sizeof(map_runtime_state.world_state) - 1;` | Initialize the inclusive byte countdown to complete object size minus one; the loop writes index zero through the last byte before its decremented count reaches -1. |
| `game_state_initialize` | 99 | `0` | `*cursor++ = 0;` | Write a zero byte while clearing the complete saved-world and stock owners before their initial values are seeded. |
| `game_state_initialize` | 100 | `1` | `} while (--count != -1);` | Stop after the decrement crosses zero to -1, so a starting count size-minus-one still writes exactly size bytes. |
| `game_state_initialize` | 102 | `1` | `count = sizeof(item_stock) - 1;` | Initialize the inclusive byte countdown to complete object size minus one; the loop writes index zero through the last byte before its decremented count reaches -1. |
| `game_state_initialize` | 104 | `0` | `*cursor++ = 0;` | Write a zero byte while clearing the complete saved-world and stock owners before their initial values are seeded. |
| `game_state_initialize` | 105 | `1` | `} while (--count != -1);` | Stop after the decrement crosses zero to -1, so a starting count size-minus-one still writes exactly size bytes. |
| `game_state_initialize` | 106 | `1` | `item_stock[KF_ITEM_STOCK_PLAYER][KF_ENUM_ENCODE(u8, KF_ITEM_SHORT_SWORD)] = 1;` | Player stock bank zero receives one copy of this named starting item. |
| `game_state_initialize` | 107 | `1` | `item_stock[KF_ITEM_STOCK_PLAYER][KF_ENUM_ENCODE(u8, KF_ITEM_MEDICINAL_HERB)] = 1;` | Player stock bank zero receives one copy of this named starting item. |
| `game_state_initialize` | 108 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_SHORT_SWORD)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 109 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_BATTLE_AXE)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 110 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_SWORD)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 111 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_IRON_MASK)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 112 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_HELM)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 113 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_PLATE)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 114 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_SMALL_SHIELD)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 115 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_SHIELD)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 116 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_GAUNTLET)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 117 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_IRON_BOOTS)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 118 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_MEDICINAL_HERB)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 119 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_ANTIDOTE_HERB)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 120 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_RECOVERY_MEDICINE)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 121 | `1` | `item_stock[KF_ITEM_STOCK_FIRST_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_DRAGON_KING_GRASS_FRUIT)] = 1;` | Shop bank 1 is the first shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 122 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_SWORD)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 123 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_COLICHEMARDE)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 124 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_CRESCENT_AXE)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 125 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_KNIGHT_HELM)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 126 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_GREAT_HELM)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 127 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_BREASTPLATE)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 128 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_FULL_PLATE)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 129 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_FIRE_MAIL)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 130 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_TOWER_SHIELD)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 131 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_LEG_GUARDS)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 132 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_MEDICINAL_HERB)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 133 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_ANTIDOTE_HERB)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 134 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_RECOVERY_MEDICINE)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 135 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_DRAGON_KING_GRASS_LEAF)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 136 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_DRAGON_KING_GRASS_FRUIT)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 137 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_LIGHT_RING)] = 1;` | Shop bank 2 is the second shop; value one enables this named item in its initial availability list. Ordinary purchases do not decrement these entries, unlike Gold Cross. No merchant name is inferred. |
| `game_state_initialize` | 138 | `1` | `item_stock[KF_ITEM_STOCK_SECOND_SHOP][KF_ENUM_ENCODE(u8, KF_ITEM_GOLD_CROSS)] = 1;` | Second shop bank two starts with one Gold Cross; the buy path decrements this exceptional finite stock entry. |
| `player_death_restart` | 146 | `0` | `if (map_floor1_script.revival_enabled == KF_MAP_SCRIPT_SET && item_stock[KF_ITEM_STOCK_PLAYER][KF_ENUM_ENCODE(u8, KF_ITEM_DRAGON_KING_GRASS_FRUIT)] != 0) {` | Bank zero is player inventory. Revival requires a nonzero Dragon King Grass Fruit quantity and consumes one with the existing decrement before persisting world state. |
| `player_death_restart` | 149 | `0xfa00` | `player_state.camera_position.vx = 0xfa00;` | Revival X coordinate is 64000 world units on floor one, after the enabled-revival and fruit-possession guards. This is an authored placement; the original coordinate choice is unproven. |
| `player_death_restart` | 152 | `0x4e20` | `player_state.camera_position.vz = 0x4e20;` | Revival Z coordinate is 20000 world units, paired with X=64000; the loaded floor geometry supplies height. This is an authored placement; the original coordinate choice is unproven. |
| `player_death_restart` | 153 | `0` | `player_state.camera_rotation.vy = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 155 | `0x7918` | `player_state.camera_position.vx = 0x7918;` | Fresh-start X coordinate is 31000 world units, chosen when revival is unavailable. This is an authored placement; the original coordinate choice is unproven. |
| `player_death_restart` | 156 | `0x1388` | `player_state.camera_position.vz = 0x1388;` | Fresh-start Z coordinate is 5000 world units, paired with X=31000 before game_state_initialize. This is an authored placement; the original coordinate choice is unproven. |
| `player_death_restart` | 157 | `0` | `player_state.camera_rotation.vy = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 161 | `0` | `player_state.status_effect_flags = 0;` | Clear all player status bits after either restart branch; this is the empty flag set. |
| `player_death_restart` | 162 | `0` | `player_state.camera_rotation.vz = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 163 | `0` | `player_state.camera_rotation.vx = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 166 | `0` | `player_state.map_variant = 0;` | Select the base floor-one resource variant when the restart path reloads the floor. |
| `player_death_restart` | 172 | `0` | `player_state.view_bob_offset = 0;` | Reset the named view-bob, death-pitch or death-blend value to zero before recovery fade; each field keeps its existing update behavior. |
| `player_death_restart` | 174 | `0` | `player_state.death_camera_pitch_step = 0;` | Reset the named view-bob, death-pitch or death-blend value to zero before recovery fade; each field keeps its existing update behavior. |
| `player_death_restart` | 175 | `0` | `player_state.death_visual_blend = 0;` | Reset the named view-bob, death-pitch or death-blend value to zero before recovery fade; each field keeps its existing update behavior. |
| `player_death_restart` | 176 | `0` | `hud_brightness = 0;` | Begin recovery fade with a dark HUD; the fade updater restores brightness. |
| `player_death_restart` | 177 | `0` | `player_state.view_rotation_offset.vz = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 178 | `0` | `player_state.view_rotation_offset.vy = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_death_restart` | 179 | `0` | `player_state.view_rotation_offset.vx = 0;` | Zero the named camera Euler angle or transient view-rotation component, preserving their independent stores and restart orientation. |
| `player_adjust_hp` | 193 | `0` | `if (value <= 0) {` | Zero is the lower resource bound after applying the signed delta. HP reaching it enters death; MP reaching it only stores zero and returns. Positive results are separately limited by the corresponding maximum. |
| `player_adjust_hp` | 194 | `0` | `player_state.vitals.current_hp = 0;` | Zero is the lower resource bound after applying the signed delta. HP reaching it enters death; MP reaching it only stores zero and returns. Positive results are separately limited by the corresponding maximum. |
| `player_adjust_mp` | 212 | `0` | `if (value <= 0) {` | Zero is the lower resource bound after applying the signed delta. HP reaching it enters death; MP reaching it only stores zero and returns. Positive results are separately limited by the corresponding maximum. |
| `player_adjust_mp` | 213 | `0` | `player_state.vitals.current_mp = 0;` | Zero is the lower resource bound after applying the signed delta. HP reaching it enters death; MP reaching it only stores zero and returns. Positive results are separately limited by the corresponding maximum. |
| `player_recalculate_combat_stats` | 241 | `0` | `player_state.cutting_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 242 | `0` | `player_state.striking_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 243 | `0` | `player_state.piercing_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 244 | `0` | `player_state.holy_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 245 | `0` | `player_state.fire_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 246 | `0` | `player_state.cutting_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 247 | `0` | `player_state.striking_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 248 | `0` | `player_state.piercing_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 249 | `0` | `player_state.poison_resistance = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 250 | `0` | `player_state.magic_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 251 | `0` | `player_state.fire_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 256 | `0` | `if (power < 0) {` | Clamp curse-reduced power at the arithmetic zero floor before narrowing back to the player field. |
| `player_recalculate_combat_stats` | 257 | `0` | `power = 0;` | Clamp curse-reduced power at the arithmetic zero floor before narrowing back to the player field. |
| `player_recalculate_combat_stats` | 263 | `0` | `player_state.cutting_attack += weapon->attack_components[0];` | Weapon-record component 0 is the positional field for cutting_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 264 | `1` | `player_state.striking_attack += weapon->attack_components[1];` | Weapon-record component 1 is the positional field for striking_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 265 | `2` | `player_state.piercing_attack += weapon->attack_components[2];` | Weapon-record component 2 is the positional field for piercing_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 266 | `3` | `player_state.holy_attack += weapon->attack_components[3];` | Weapon-record component 3 is the positional field for holy_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 267 | `4` | `player_state.fire_attack += weapon->attack_components[4];` | Weapon-record component 4 is the positional field for fire_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 357 | `1` | `if (player_state.physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_recalculate_combat_stats` | 360 | `1` | `if (player_state.magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_increment_physical_power_training` | 371 | `0` | `player_state.physical_power_training = 0;` | Reset the completed training counter to zero; excess points are discarded rather than carried to another gain. The raw reset occupies the subsequent cap-branch delay slot. |
| `player_increment_physical_power_training` | 372 | `1` | `if (player_state.base_physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_increment_magic_training` | 387 | `0` | `player_state.magic_training = 0;` | Reset the completed training counter to zero; excess points are discarded rather than carried to another gain. The raw reset occupies the subsequent cap-branch delay slot. |
| `player_increment_magic_training` | 388 | `1` | `if (player_state.base_magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_add_experience` | 412 | `1` | `player_state.progress_state.level = level + 1;` | Advance the one-based level by one after the maximum-level guard; the saved previous byte still selects the growth branch and row. |
| `player_add_experience` | 415 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].maximum_hp` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 416 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].maximum_hp;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 418 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].maximum_mp` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 419 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].maximum_mp;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 420 | `1` | `player_state.base_physical_power += player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].physical_power_step;` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 421 | `1` | `player_state.base_magic += player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].magic_step;` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 423 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].experience_threshold` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 424 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].experience_threshold;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 433 | `1` | `if (player_state.vitals.maximum_hp >= KF_PLAYER_VITAL_MAX + 1) {` | One above the inclusive HP/MP cap preserves the original >=10000 test on the already narrowed halfword field. |
| `player_add_experience` | 436 | `1` | `if (player_state.vitals.maximum_mp >= KF_PLAYER_VITAL_MAX + 1) {` | One above the inclusive HP/MP cap preserves the original >=10000 test on the already narrowed halfword field. |
| `player_add_experience` | 439 | `1` | `if (player_state.base_physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_add_experience` | 442 | `1` | `if (player_state.base_magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_calculate_damage_component` | 459 | `0` | `if (attack == 0) {` | An absent attack channel contributes zero immediately, before division or defense arithmetic. |
| `player_calculate_damage_component` | 460 | `0` | `return 0;` | An absent attack channel contributes zero immediately, before division or defense arithmetic. |
| `player_calculate_damage_component` | 464 | `0` | `if (defense < 0) {` | After reusing defense for attack-minus-threshold, clamp its negative excess to zero before adding the squared-attack term. |
| `player_calculate_damage_component` | 465 | `0` | `defense = 0;` | After reusing defense for attack-minus-threshold, clamp its negative excess to zero before adding the squared-attack term. |
| `player_calculate_damage_component` | 467 | `0` | `if (base_power == 0) {` | Replace an exactly zero combined threshold with one so the following denominator is nonzero. Preserve the exact zero check rather than inventing a general negative clamp. |
| `player_calculate_damage_component` | 468 | `1` | `base_power = 1;` | Replace an exactly zero combined threshold with one so the following denominator is nonzero. Preserve the exact zero check rather than inventing a general negative clamp. |
| `player_apply_damage` | 541 | `2` | `damage += PLAYER_DAMAGE_SUBUNITS_PER_HP / 2;` | Half a damage unit is added before converting the summed tenths to HP. The literal two selects the midpoint; preserve this rounding before the independent Q12 and multiplier stages. |
| `player_apply_damage` | 544 | `0` | `if (loss != 0) {` | Only nonzero numeric loss updates HP and requests a damage frame. Status side effects were already processed, even if this branch is skipped. |
| `player_apply_damage` | 546 | `0` | `if (remaining <= 0) {` | Zero is the lower bound for remaining HP after subtraction. Store it without calling death_begin here; retain the existing later player-state handling. |
| `player_apply_damage` | 547 | `0` | `remaining = 0;` | Zero is the lower bound for remaining HP after subtraction. Store it without calling death_begin here; retain the existing later player-state handling. |
| `player_apply_radial_damage` | 575 | `1` | `if (distance == -1) {` | The signed distance-query rejection result -1 skips all radial arithmetic and the damage call. It is not an attenuation or radius value. |
| `player_apply_radial_damage` | 588 | `0` | `component0, component1, component2, 0, component3, component4,` | Status argument zero applies no new status through this radial wrapper; the other channel and multiplier arguments keep their ordered forwarding. |
| `player_select_magic` | 597 | `0` | `player_state.magic_charge = 0;` | Changing selected magic empties the independent magic charge before storing the selected ID. |
| `player_select_magic` | 600 | `0` | `player_state.selected_magic_record = 0;` | The named no-magic sentinel clears the selected record pointer to null; otherwise the stored byte indexes its record. |
