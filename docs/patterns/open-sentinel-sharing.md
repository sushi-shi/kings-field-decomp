# OPEN ending sentinel sharing

## Function Match Plan

OPEN `80014e28 opening_ending_scroll_run` remains 1944 bytes and strict
97.952675% at `1769fb3`. A fresh isolated build and the six semantic views
confirm the 264-byte frame, detached ten-register epilogue, forty direct
calls, eight internal jumps, thirty-one address pairs, and no strings or
candidate outgoing references. The sole caller is `opening_run` at
`80015890`. The preceding ending controller and camera-path implementation
use the same camera completion contract. SDK graphics/audio calls are
external vendor boundaries; this controller's entity and scene policy is
game-owned. All ordered referents in the prior biased-zero diagnostic resolve
to retail; its nine unequal words are real instructions, not symbol names.

The prior instrumented compiler trace attributes its two-register reload
requirement to comparing spilled `sequence_delay` with the loop-hoisted -1
pseudo. The same constant is subsequently shared with the camera-completion
and short scroll-wrap comparisons. Earlier controls changed the sequence
predicate or the camera dispatch construct. Inspect the other two sentinel
expressions independently while retaining the original sequence decrement
and literal comparison. This tests whether sharing from the later predicates
is needed for the observed reload requirement; it does not assert that a
different spelling was the original source.

Use the exact equivalent complement-zero predicate for the camera's signed
word, and the promoted signed-short scroll value. Neither changes the storage
type, wraps arithmetic, adds a carrier, or changes a call. Each diagnostic
must be compiled independently. Compare the first divergence, all raw words,
and exact sibling controls; reject any remaining extra complement instruction
or changed retail branch. Only consider composition if an independent result
supplies evidence for it. No non-exact diagnostic is banked.

## Sentinel control result and additional prior evidence

The camera complement gives 98.858025%; the scroll complement gives
99.670784%, 1944 bytes. The latter needs one spill register, t0, and restores
the literal countdown comparison. Its nine unequal linked words comprise
seven dispatch words and a `nor`/zero-test pair replacing the scroll's -1
comparison. This diagnostic is not a final reconstruction.

A subsequent audit of the older worktree's uncommitted reports found a
stronger existing candidate: a real top-color argument pointer initialized
at frame-loop entry, strict 99.917694%, with only the seven dispatch words
unequal. Its earlier loop-invariant movement prevents the -1 pool from
moving. Existing source hashes equal this worktree's production sources.
That candidate remains the relevant frontier; the new complement trials do
not supersede it.

The unpublished `open-dispatch-constant-lifetimes.md` report further establishes
that adding the real scroll ACTIVE equality moves a three-one pool. Its
subsequent guarded increment compositions preserve that pool and one reload
register, but have the wrong activation and gate instructions. These existing
states must remain available; their lower score alone does not reject them.

## Function Match Plan: Boolean activation producer

The retail scroll-enable local is a short with only zero initialization and
one activation, guarded by its equality to zero. Its later signed positive
gate must remain unchanged. Prior controls changed that gate or expressed
activation as addition/OR; the latter operations did not fold through the
guard's promoted short expression.

Test the real Boolean toggle `!scroll_state` at that guarded activation,
using the existing enum encode/decode boundaries. Logical negation tests the
same zero predicate as the guard and is true on precisely that path. It
retains all reachable values and gives CSE an equality expression, unlike
addition and bitwise OR. Inspect whether CSE folds it to the literal store,
and whether its word-valued Boolean result contributes to the comparison-one
pool before narrowing. No extra comparison consumer, local carrier, guard,
call, or storage view is introduced. First compile it independently of the
top-color pointer and the retained ACTIVE-gate compositions. Compare complete
raw code, constants, calls, referents, frame and exact siblings; a retained
compiler-state improvement is not an exact claim.

The Boolean toggle produces the same strict score, extent, frame and selected
allocation features as the baseline. It does not supply a new one-pool or
reload state. The production source remains unchanged.

## Function Match Plan: actual promoted scroll state

Retail reads the stored short enable state at the handoff and again at the
positive scrolling gate. Both uses pass through a word-valued copy. The
unpublished recurrence controls changed its short arithmetic without linking
that arithmetic to the word value used in the guard. Test one real promoted
scroll-state value, initialized after the camera step, used by the zero guard,
advanced on the guarded activation, narrowed into the existing short state,
and used by the later positive gate. The advance still only maps zero to one.
The value has actual decision and update consumers; it is not a constant
carrier or an extra state transition. The retained short remains the state
across frames. The literal store, second short reload, calls, existing objects
and complete frame are required final checks, not assumed consequences.

Compile independently from canonical, preserving the signed positive gate.
Inspect whether the common word value permits CSE to recover the literal
activation and whether either comparison-one lifetime changes. Retain any
distinct useful intermediate separately, but require full raw equality for
adoption. Do not change the declared SDK types or loop profile.

The promoted-state trial reaches 96.172840%, retaining the baseline's selected
spill/phase/frame features. Its explicit word state does not supply the needed
dispatch lifetime. Keep the generated source for raw review; do not replace
the seven-word frontier with it.

## Working-source plan: retain the verified color-argument lifetime

Apply the previously isolated top-color pointer to this worktree's working
source and rebuild it against the current headers. The pointer denotes the
existing CVECTOR and supplies its three genuine call arguments. It introduces
no storage owner, operation, comparison, branch or call. Retail's existing
stack home for that object's address supports the value representation. Keep
the original source snapshot and every alternate generated state separately.

Acceptance for this intermediate working source requires strict 99.917694%,
exactly the seven previously identified dispatch words remaining, all forty
calls and thirty-one ordered referents, the full 264-byte frame/epilogue and
all six complete exact siblings. This is an explicit non-exact working source;
do not bank it or claim the goal complete. Continue from its actual seven-word
residue rather than repeatedly restoring the less accurate lifetime model.

## Verified working result

The current-header rebuild is strict **99.917694%**, 1944 bytes. Every linked
word agrees except these seven offsets from `80014e28`:

| Offsets | Remaining difference |
| --- | --- |
| `398`, `39c`, `3a0`, `3a4` | Lighting selector uses v1 rather than v0; comparison one uses v0 rather than t0. |
| `3ac` | Default-path jump reaches the second one definition instead of reusing the first. |
| `414`, `418` | Sequence dispatch uses comparison one in v0 rather than t0. |

All forty ordered calls, thirty-one ordered data referents, the complete
264-byte frame and detached epilogue agree. All six sibling functions agree
with every retail word. Normal and instrumented compiles from the same source
path produce identical complete ELF objects. An initial audit compared
different source paths and correctly failed whole-ELF equality: only path
strings and their debug/symbol metadata differed. Repeating from the actual
source path removed that mismatch without filtering object bytes.

The additional control traces explain their outcomes. Boolean activation's
word result is folded to `scroll_state:HI = 1` in CSE1; all code/data bytes,
text relocations and frame descriptors equal the original baseline. The
promoted-state control also folds both activation assignments to literal one,
but retains the real word state in s1 through eight calls, removes the second
short reload and produces 1928 bytes. Its separate word value is not the
retail lifetime. Both controls preserve all calls/referents and six exact
siblings, with normal/instrumented whole-object parity; neither replaces the
working source.

Ruff and all 703 repository tests pass (nine optional controls skipped), as
does `git diff --check`. Full `kf build` was run after the focused rebuild;
it retains the initial build's data-comparison, ownership and section-placement
failures. The exact function count remains 427/471 overall and 106/108 in
OPEN. `display_initialize` remains unchanged at 92.177960%. No baseline or
bank is changed, and neither remaining function is claimed exact.

## Cross-call source control

A read-only GAME retail scan looked for a literal one in t0..s7 immediately
followed by an equality branch using that register. The stricter zero-branch
predecessor pattern finds the known OPEN ending at +3a0 as a positive control,
but no GAME example. The broader scan finds two game-owned candidates and
two SDK routines. `actor_update_awareness` is non-exact and is not used as
an exact compiler control; `CD_ready` and `SpuIsTransferCompleted` are excluded
as library code.

GAME `80033f64 map_ambient_script_floor1` is a useful 648-byte exact control
with two state switches. An unchanged normal/traced rebuild equals the entire
configured ELF, and every resolved function word equals GAME retail. CSE1
merges the first switch's comparison-one definition at UID199 into p73,
defined at UID11 for the preceding revival-enable comparison. Global
allocation puts p73 in s0 with three references, one crossed call and lifetime
24. The audio call therefore does not prevent this actual constant sharing.

The second switch's p160 remains distinct and gets v0, with two references,
zero crossed calls and lifetime four. This independently reproduces the
common-exit boundary's interruption of sharing. It is not evidence for adding
an extra comparison to OPEN: the map script has a real earlier flag consumer
which the ending does not. The control supplies a cross-call counterpart to
the earlier exact OPEN map-cell observation, without a GAME source change or
an additional exact-function claim. Generated scan, parity, raw and trace
evidence is under `build/open-missed-evidence/map-control/`.

## Display member-root composition control

The separately planned `display-member-composition` control combines the
previous explicit INTRO/default branch with a pointer to the first DRAWENV's
`dtd` member. It recovers the containing SDK DRAWENV using the header's member
offset and addresses the second DRAWENV by typed array indexing. This is a
diagnostic composition, not an accepted source reconstruction: repeated
container recovery and the extra INTRO test require independent justification.

Unlike either earlier control alone, all four `dfe` stores remain relative to
the first `dtd` through CSE1, CSE2 and global allocation. That root receives
s0 with nine references, two crossed calls and lifetime 56. Thus the pinned
compiler can retain this specific retail member root; an absolute-address
rewrite is not inevitable. All 18 ordered calls and all 16 ordered explicit
data references match the target, and the three sibling functions retain
every retail instruction. Normal and traced compiles have complete ELF parity.

The result remains non-exact: 93.550850%, 480 bytes versus retail's 472, and a
40-byte frame versus 48. Mode receives s1 instead of s3, while the first flag
value is local instead of retained in s1 across calls. The second DRAWENV
receives the retail s2. The additional INTRO comparison also remains. This
control narrows the remaining question to the original branch/value lifetime
structure; it does not justify adding an artificial constant carrier. The
production display source stays at 92.177960%. Generated source, raw audit
and compiler trace are under
`build/open-missed-evidence/display-member-composition/`.

The paired `display-member-single-case` control removes only the explicit
INTRO case from that composition. It returns to 92.177960%, 484 bytes and a
40-byte frame. CSE1 now folds all four `dfe` addresses to absolute references;
the first `dtd` root has five references, crosses no calls, lives for 30 and
receives a0. The resulting twenty explicit references are the retail sixteen
plus the four `dfe` references, in store order. All eighteen calls and three
raw-exact siblings remain unchanged, with native/traced full ELF parity.

This paired result rejects the composition as a route to a source fix: its
retained base depends on the extra branch that retail does not contain. The
member expression alone does not account for retail's lifetime. The initial
ordered-reference equality assertion failed as expected from the four added
absolute references; the diagnostic audit records the actual sequence rather
than treating that mismatch as a passed check. No production source changed.

## Retail DRAWENV-stride control search

A separate read-only scan selects each of OPEN.EXE, GAME.EXE and PSX.EXE
explicitly and visits every function in its curated catalog. It looks for
two byte stores within twelve instructions whose base-register numbers agree
and whose signed displacements differ by DRAWENV's 92-byte stride. This is
a candidate filter, not a data-flow proof: in particular, repeated assembler
temporary register numbers do not establish a surviving pointer value.

OPEN returns the known display initializer, ending-scene RGB assignments and
VRAM-view background assignments. GAME returns its display initializer; PSX
returns none. Among these hits, only the unmatched OPEN initializer uses a
saved-register base (s0); the other pairs use assembler temporary at with
absolute-address materialization. Consequently this specific retail pattern
provides no additional reconstructed source control, including in the
cataloged functions without source. The search does not exclude other
instruction spacings, different field strides or uncataloged code.

The source-side DFE consumers in GAME's error-screen/image-wait functions and
OPEN's VRAM-view function instead select the buffer at runtime. Such an
address cannot become a fixed absolute address, so retaining it through a
call is not a control for the initialization function's constant-address
folding. Generated script and per-image candidate lists are retained as
`build/open-missed-evidence/scan_drawenv_pairs.py` and `drawenv-pairs-*.json`.

## Switch-expression promotion control

The existing storage-width and dispatch trials did not record an explicit
signed-long switch operand. Earlier countdown probes show that this compiler
can distinguish C int from project s32 (long), despite equal widths. Two
isolated controls therefore cast the actual lighting or sequence switch
operand to s32, one at a time, while preserving signed-short state storage,
every transition and the retained top-color pointer. All signed-short input
values fit both promoted types; no state range assumption is needed.

Both controls remain 99.917694%, 1944 bytes with the same seven differing
words. Their complete resolved instruction/call/reference dictionaries equal
the retained working baseline for every function in the unit. Normal and
instrumented complete ELF objects agree for each control, all forty calls
and 31 ordered referents agree with retail, and six sibling functions retain
raw equality. Explicit promotion at these switch consumers therefore does
not explain the remaining comparison-constant lifetime. These diagnostic
casts are not kept in production. The plan, inputs and audits are under
`build/open-missed-evidence/selector-promotion*`.

## Volume countdown value and predicate lifetimes

Three planned controls retain the actual volume transition condition while
varying the value consumed across `SsSetMVol`. The current source decrements
the unescaped word local, submits its quotient by three, then tests the new
volume against zero. The equivalent old-value predicate tests for one.
Prior manifests and retained single-trial sources contained no volume
postdecrement/old-value predicate test. These are diagnostic source states;
none changes production or banks a function.

1. `previous_volume = sequence_volume--`, the unchanged audio call, then
   `if (previous_volume == 1)` gives 99.331276%, 1944 bytes. All seven original
   dispatch differences disappear. Exactly nineteen words differ, all in
   +458..+4a4; every instruction outside that interval equals retail. The
   264-byte frame and all ten save locations agree. The old value receives
   s0 (four references, one crossed call, lifetime ten); the volume block
   copies it for the decrement and compares s0 with one after the call,
   instead of retail's reload of the updated stack value and zero branch.
   CSE1 retains SI-one definitions p156/p168; the loop snapshot contains the
   single p156 definition at UID1085. Global allocation needs one spill
   register, t0, and both lighting tails use it. This provides another actual
   consumer that recovers dispatch sharing, with a precisely localized
   non-retail lifetime.
2. `int fade_finished = sequence_volume-- == 1`, the same audio call, then
   `if (fade_finished)` gives 98.755140%, 1948 bytes. The original seven
   dispatch words differ again and the volume region gains code. Evaluating
   the predicate before the call does not preserve the useful outcome of
   retaining the old volume.
3. Submit `(sequence_volume - 1) / 3`, then test
   `if (sequence_volume-- == 1)`. Moving only the unescaped local update
   across the call preserves its externally visible arguments and every
   subsequent state consumer, but gives 98.765434%, 1956 bytes. The first
   difference is +424; all seven original dispatch offsets now match, but
   the altered volume path adds twelve bytes. It does not recover retail's
   store-before-call and reload-after-call schedule.

Each control has normal/instrumented complete ELF parity, forty ordered
calls, 31 ordered referents and six raw-exact siblings. All retain retail's
frame and saved-register homes. Generated plans, inputs, resolved audits and
traces live under `build/open-missed-evidence/volume-*`. The isolated
old-value source is retained for evidence, while production remains at
99.917694%. No exact-count movement follows from these controls.

## Remaining-count and canonical-type follow-ups

The panel-loop countdown control replaces the sole signed-short index with
a signed-short remaining count, selects row `9 - remaining`, advances the
same panel pointer, and terminates with `remaining-- != 1`. Rows and panel
updates remain ordered 0..8, without an extra counter. It gives 98.792180%,
1956 bytes,53 unequal aligned words, first +398; the original dispatch differences
remain and the compiler does not recover retail's ascending-index sequence.
Normal/traced ELF parity, 40 calls, 31 referents, six raw-exact siblings and all
264-byte frame/save facts pass. The source is not retained; the plan and
raw/trace records are under `build/open-missed-evidence/panel-countdown*`.

The subsequent volume declaration discriminator and canonical-scalar
controls are recorded in [GCC typedef comparisons](gcc257-typedef-comparisons.md).
They establish a typedef-specific early-fold difference in the alternative
volume timing source, but leave both current unmatched bodies byte-identical
to the kept sources when their scalar aliases are removed. No production
change or exact-function claim follows.

## Function Match Plan: carry the committed display frontier forward

The current worktree retained display_initialize at 92.177960%, although
commit 0d409cf records a humane 96.652540% candidate: second DRAWENV pointer
initialized at entry, first pointer derived by one typed array element, and
signed-short lower-buffer Y matching the SDK parameter boundary. The
SDK objects, global owner, all 18 calls and byte writes are unchanged.
This is carrying a previous result into the active campaign, not claiming a
new source hypothesis or repeating its permutation trials.

The retained six-view OPEN 80016adc dossier, sole full-word mode caller,
SDK headers, exact siblings and current canonical-scalar audit establish the
current 92.177960% / 484B / 40-frame baseline and retail 472B / 48-frame. Prior source
history reports that the candidate recovers retail's 48-byte frame and mode,
Y and second-DRAWENV homes s3/s1/s2, but has an early address pair and the
wrong s0 lifetime. Exactness remains unproven.

Apply only those three source edits to the current unit, force the focused
recompile, and compare strict score plus raw constants, ordered calls and
references, sibling instructions and frame against the current catalog.
Keep the candidate only if it reproduces the supported result without an
exact-sibling regression. Run full kf build after the actual source change
and appropriate existing display tests before handoff. No banking unless
strict 100% is achieved.

The current-header rebuild reproduces 96.652540%, 476 bytes. Native and traced
compiles from the actual source path equal the configured canonical ELF in
full. All 18 calls and all three raw-exact siblings survive. The candidate's
seventeen ordered explicit references are exactly the retail sixteen with
`80069acc` prepended for the early second-DRAWENV pointer. The seven-word
prologue and nine-word terminal sequence equal retail, and frame/save
analysis confirms 48 bytes and all five saved-register homes.

All four DFE stores remain base-relative through CSE1, CSE2 and allocation,
using the second DRAWENV at s2 with offsets -69/+23. Retail uses the first
DTD member at s0 with offsets +1/+93. The candidate's DTD root has three
references, no crossed calls and lifetime 28, receiving v1; s0 holds value
one. Thus the result remains non-exact, with the early pointer and wrong
DTD/one lifetimes explicitly outstanding. The existing compiled-path test's
recorded size/reference residue is updated to 476 bytes/one extra second-
DRAWENV reference; its independent argument, ordered-write, frame-return
and deliberate-corruption controls are preserved.

A read-only audit also rechecked the old pass-option controls without
rebuilding or adopting their flags. Disabling second CSE produces 432 bytes,
the 48-byte frame and the desired DTD-relative DFE stores, but reduces
explicit references from 16 to 6 by also retaining non-retail relative stores
in initialization; its render_initialize sibling regresses. Disabling
extended CSE produces 472 bytes / 16 references but a 40-byte frame, two whole-
DRAWENV bases with DFE at +23, and a local value-one register. Those controls
therefore do not supply a profile fix. Generated raw audit records are in
`build/open-missed-evidence/display-pass-controls-audit.*`; retained-source
parity/raw evidence is under `display-frontier-retained/`.

Post-edit verification completed: all 703 repository tests pass (9 skipped),
and `ruff check scripts tests` and `git diff --check` pass. The full `kf build`
still fails the existing data-ownership/section-placement checks; OPEN retains
106/108 exact game functions, 6/6 exact vendored source functions and 34/38
verified target-relink units. No function is newly banked. Logs are
`build/open-missed-evidence/{full-build,tests,ruff}-display-frontier.log`.

## Immutable-pointer control on the retained display source

Review of the historical display improvement commits confirms that
`ccc2b05`, `038cc64` and `0d409cf` form the forward-pointer, reverse-pointer,
then signed-halfword argument sequence already represented by the current
render-init source. The ending selector-layout follow-up `bca32e7` records
reverted controls rather than a stronger source candidate.

The retained display trace now has an explicit expression-lineage audit.
At CSE1 uid230, `first_draw + 23` (p74) becomes `second_draw - 69` (p75).
The first DTD root p81 exists separately; it never supplies the DFE base in
this candidate. This is an earlier distinction than final register allocation.

A separately planned full-TU control qualifies the existing, never reassigned
local as `DRAWENV *const second_draw`, leaving its SDK pointee mutable. Native
and traced objects agree in full. Strict score falls to 89.661020%, 488 bytes,
with a 40-byte frame. All eighteen calls and three raw-exact siblings survive.
Its twenty-one references are the target sixteen plus the early second-DRAWENV
address and four extra DFE address pairs. The production qualifier is not kept.

Unlike the retained source, the const control uses the first DTD root for all
four DFE stores at CSE1; CSE2 folds all four to absolute addresses. Expansion
already reduces p75 from five occurrences to two: its initialization and the
second PutDrawEnv argument. Arithmetic and DFE accesses instead expand from
constant addresses. The pinned source's `c-typeck.c` `default_conversion`
and `decl_constant_value` permit replacement of readonly locals by constant
initializers; `stmt.c` explicitly disables blanket RTX_UNCHANGING assignment
from TREE_READONLY. Thus this control is not merely a changed allocation hint.
It distinguishes source-expression propagation from the later address loss,
but does not recover retail and does not justify adding a qualifier for score.

Generated plan, compiler parity, raw reference audit and trace lineage are
under `build/open-missed-evidence/display-const-pointer*` and
`display-frontier-retained/lineage.json`. No production source or configuration
changed during this control, so the preceding full verification remains the
latest applicable result.

## Ending pointer and translation-unit context controls

The analogous `CVECTOR *const top` control in the ending changes neither
the complete resolved target nor any sibling. Native/traced full-ELF parity,
forty calls, 31 ordered referents, the 264-byte frame and all six exact siblings
pass. It remains 99.917694%, 1944 bytes, with the same seven unequal words.
Unlike the display pointer, this real local has only call-argument consumers
and an automatic-object address initializer. No qualifier is kept. Generated
plan and evidence are under `build/open-missed-evidence/ending-const-top*`.

The exactly equivalent signed predicate `scroll_state >= ENDING_SCROLL_ACTIVE`
was considered as a possible additional comparison-one source. The pinned
`fold-const.c` comparison handler at 4220 explicitly changes `X >= CST` to
`X > CST-1` for positive constants. It therefore removes this one before RTL
loop optimization; no compile trial or claimed score is needed to establish
that this source spelling cannot supply a later one pool through that path.

A separate ownership/context plan tests each unchanged target body as the
only function in its generated translation unit, preserving the complete
original prefix of headers, typedefs, enums, data and rodata declarations.
This checks dependence on compilation of other function bodies; it does not
assert an original TU boundary or alter production ownership.

Both isolated targets have complete resolved instruction/call/reference
equality with their full-TU counterparts: ending 99.917694%/1944 bytes and
display 96.652540%/476 bytes. Native and traced objects agree in full within
each context. Fresh actual-source full-TU compiles also equal the configured
canonical ELF objects byte for byte, and all nine exact siblings retain raw
retail equality. Thus preceding function compilation does not explain either
remaining residue under these fixed declarations and profile. This does not
test alternative declarations or data ownership. No source split is retained.
The reproducible source slicing and full/isolated parity records are under
`build/open-missed-evidence/open-target-tu-context-plan.md`,
`open_target_tu_context.py`, `target-tu-context.log` and `target-tu-context/`.

## Immutable matrix and unsigned switch-consumer controls

Qualifying the ending's single initialized, never modified automatic matrix
as `const MATRIX light_matrix` produces identical complete resolved dictionaries
for the target and all six siblings. Native/traced whole-ELF parity, forty
calls, 31 referents and the 264-byte frame pass. Ending remains 99.917694%,
1944 bytes, with the same seven dispatch words unequal. This aggregate-copy
control is separate from the pointer-object qualifiers; neither changes the
kept source. Generated evidence is under `ending-const-matrix*` in the campaign
build directory.

Two independently planned switch-consumer controls retain the signed-halfword
state declarations, stores and all producers, converting only one switch
operand to `u16`. This differs from the older unsigned-storage experiment.
Lighting cases are 0/1 and sequence cases are 1/2, so unsigned conversion of
any signed-halfword value preserves the selected/default arm in each switch.

The lighting view yields 99.711940%, 1940 bytes. At +390 it loads the phase
directly into v1 rather than t0 (`97a300b0` versus `97a800b0`), then omits the
required post-load copy at +398. Subsequent addresses shift by four bytes;
256 aligned words differ. The sequence view yields 99.362140%, 1944 bytes:
the seven kept dispatch differences remain, with two additional differences
at +3b0 and +410. Both replace retail `addu v1,s5,zero` (`02a01821`) with
`andi v1,s5,0xffff` (`32a3ffff`). These masks are actual surviving operations,
not relocation noise.

Both variants retain forty ordered calls, 31 ordered referents, all six
raw-exact siblings, native/traced whole-ELF parity and the 264-byte frame.
The lighting store tails still share t0 and reload still uses one spill
register. Neither control recovers the dispatch-one lifetime, and neither
cast is retained or composed. Plans and full raw/trace/baseline comparisons
are under `ending-switch-unsigned-view-plan.md`, `ending_switch_unsigned_view.py`
and `selector-unsigned*` in `build/open-missed-evidence/`. Production source
and configuration remain unchanged; the preceding full verification applies.

## Ownership and local-layout revalidation

The display's complete graphics-owner model has independent evidence from
the startup clear span and cross-field chains in exact consumers; see
[open-graphics-runtime-owner.md](open-graphics-runtime-owner.md). Earlier
separate/compact environment owner controls already failed to supply the
retained DTD lifetime. Repeating those ownership splits is not a new source
hypothesis, and no owner change follows from this review.

The ending's concrete local addresses supply a further constraint on possible
aggregate regrouping. Its two CVECTOR objects are four-byte copies at sp+152
and sp+160; its two nine-word tables start at sp+72 and sp+112. The pinned
BLKmode stack allocation rounds independent objects to eight-byte boundaries.
These facts reject compact color-array and paired-table-struct substitutions
that would use strides four and 36. They do not justify adding padding or
inventing a tenth table element. The reusable source chain and exact retail
sites are recorded in
[gcc257-stack-object-alignment.md](gcc257-stack-object-alignment.md).
No new compilation or score is claimed for this read-only layout audit.

## Legacy decompiler review and sequence-phase width

The existing main-worktree Ghidra proposal was extracted by image/VA rather
than treated as current source evidence. Its display record has the correct
472-byte extent and describes the already reconstructed call/write order.
Its ending record still advertises 1896 bytes rather than the current 1944,
omits a real lighting-callee argument, and divides the nine-element stack
tables into short arrays plus scalars. These are limitations of that generated
proposal, not corrections to the curated function or SDK declarations.
The original report hash and exact rows are preserved under
`build/open-missed-evidence/ghidra-target-review/`.

The proposal's word sequence-phase variable suggested an independent width
question. Raw retail keeps that phase in s5, assigns only literals 0/1/2/3,
and copies/tests it without a local halfword memory home. Saving s5 in the
prologue preserves the caller's register and does not establish the phase's
source width. The lighting phase's separate lhu/sh home at sp+176 must not
be used as proof of the sequence phase's type. The older 'phase width'
commits 031dea0/b58936d tested explicit constant decoding and blend bounds,
not this word declaration.

A separately planned generated full-TU control changes only the sequence
declaration from `KfEndingSequencePhase` to `s32`. All produced values and
transitions are preserved. It gives 99.434160%, 1940 bytes, with 238 unequal
aligned words. The first difference is +c0: the initial lighting-blend zero
uses s5 instead of s4; the sequence initialization at +2a8 makes the reciprocal
change. The sequence selector loses a required copy, shortening the function
and shifting the subsequent instructions. The comparison-one discrepancy
is not recovered.

Native/traced whole-ELF parity, forty ordered calls, 31 ordered referents,
the 264-byte frame and six raw-exact siblings pass. The word declaration is
not retained. This discriminates these source models under the pinned probe;
it does not prove a unique historical declaration from a saved register.
Plan, generated source, trace, raw results and baseline comparisons are under
`ending-sequence-word*` in `build/open-missed-evidence/`. Production source
and configuration remain unchanged.

## Bottom-color pointer and single CSE traversal control

The original top-pointer probe did not explicitly name the bottom pointer.
A new bounded control adds `CVECTOR *bottom = &bottom_color` beside top at
frame-loop entry and uses it for its three existing call arguments. It keeps
the separate object, addresses and all statements intact. Native/traced
whole-ELF parity passes; every resolved dictionary in the full seven-function
unit equals the kept source. Ending remains 99.917694%, 1944 bytes, with the
same seven unequal words, forty calls, 31 references and 264-byte frame.
The neutral pointer declaration is not retained. Generated evidence is under
`ending-bottom-pointer*` in the campaign build directory.

A separate diagnostic compiles the unchanged actual source with the one
additional option `-fno-cse-skip-blocks`. The old objects for that option
covered display only. The pinned O2 setup enables this CSE traversal flag;
the control checks whether that traversal choice changes the ending's local
comparison-one result. It is not a source edit, compiler-version change or
historical flag attribution.

The target remains completely resolved-byte-identical at 99.917694%, and
five siblings remain raw exact. However, `opening_ending_scene_run` falls
from 100%/756 bytes to 97.195770%/752 bytes, retaining its call sequence but
changing its ordered address references. Native/traced whole-ELF parity
passes for the control. No production option or profile is changed: this
option supplies no target gain and regresses a banked sibling. Per-function
strict/raw/call/reference results and target trace features are under
`ending-cse-skip-control*` in `build/open-missed-evidence/`. The production
objects and preceding full verification remain applicable.

## CSE traversal and a false exact branch comparison

A separate `-fno-cse-follow-jumps` diagnostic leaves every resolved function
in the seven-function unit identical. Combining it with
`-fno-cse-skip-blocks` exercises the explicit both-flags-disabled path in
CSE. The ending still has the same seven unequal words, forty calls and
31 references, but its preceding sibling grows by four bytes and regresses
to 90.248680%. Neither option is adopted.

Despite identical function-relative instructions, the combined control's
ending score rises from 99.917694% to 99.927986%. At +3ac the target jumps
to +418, whereas both candidates jump to +414. The preceding sibling's
four-byte growth makes the wrong jump's section-relative encoded address
coincide with retail. With the compiler's zero-sized local label present,
objdiff's relocation comparison accepts that address and its equal-byte
fast path skips argument comparison. Explicit `functionRelocDiffs=all`
also exhibits the error.

A six-instruction synthetic function isolates the defect: its jump selects
between returning 1 and returning 2, with both return values in delay slots.
The target selects 2; a candidate selecting 1 incorrectly scores 100% when
an eight-byte prefix shifts the function and a zero-sized local label aliases
its start. Without the prefix or alias, the wrong destination is rejected.
Correct destinations remain exact with and without either feature. These
are tooling fixtures, not assembly replacements for game functions.

`patches/objdiff-branch-destination.patch` makes the instruction fast path
also compare aligned branch destination rows, using the existing argument
comparison when they differ. `tests/objdiff_mips_smoke.py` checks the selected
function's score and covers correct/wrong destinations with both placements
and alias states. The negative control failed against the previous binary
with `expected exact=False, got 100.0%`. The normal patched build passes all eight branch controls and the existing
leaf/relocation controls. Both kept and shifted OPEN candidates now score
99.917694%, with the +3ac jump correctly marked as an argument mismatch.

Generated plans, whole-object native/traced parity results and comparison
reports are under `ending-cse-follow-control*`, `ending-cse-basic-control*`
and `objdiff-branch-position-local-repro/` in the campaign build directory.

The full rebuilt report preserves all 427 game exacts and thirteen vendored
exacts, including OPEN 106/108. Eight already non-exact GAME functions lose
small fuzzy increments; direct old/new instruction comparisons confirm only
newly detected branch-destination mismatches. Ruff, 703 repository tests
(nine skips), `git diff --check`, and `nix flake check -L` pass. Both affected
OPEN units were recompiled. Full `kf build --reconfigure` still fails the
same pre-existing data/ownership/relink checks: data-owning units matching
remain PSX 0/1, OPEN 3/19 and GAME 9/42; target relinks remain 1/1, 34/38 and
75/77 respectively. Neither OPEN target becomes exact, and nothing is banked.
See [the comparison fix](objdiff-local-branch-comparison.md) for the upstream
PowerPC snapshot audit and durable regression-test details.

## Sequence storage and dispatch conversion are distinct

The word-only sequence control changed both the local's storage mode and the
switch's operand. Retail's sequence state remains register-held with only
0/1/2/3 producers; its saved-register stack slot does not establish a local
halfword type. A separate planned control keeps `s32 sequence_phase` but
uses `switch ((KfEndingSequencePhase)sequence_phase)`, explicitly converting
to the existing signed-short enum representation at dispatch. All producers,
the later zero test, calls and other statements stay unchanged. Narrowing
preserves every reachable state. Previous generated-source/manifest searches
found no such word-storage/narrow-consumer control.

| Sequence storage | Dispatch consumer | Strict score | Bytes |
| --- | --- | ---: | ---: |
| Kept signed short | Ordinary switch | 99.917694% | 1944 |
| Previous s32 control | Ordinary switch | 99.434160% | 1940 |
| New s32 control | Explicit signed-short conversion | 99.917694% | 1944 |

The new complete resolved unit equals the kept unit, including all reconstructed
instructions, calls and referents. It retains the same seven unequal retail
words, forty calls, 31 references, 264-byte frame and six raw-exact siblings.
Native/traced complete-ELF parity passes. No cast or declaration is retained.

The traces distinguish these source models even though their final code
agrees. The kept sequence pseudo is HI, while the new one is SI. Combination
reduces the new dispatch's narrowing/promotion to an SI register copy. Both
models then give the sequence state thirteen weighted references, nineteen
crossed calls, live length 254 and allocation to s5. The word-only control
has no selector copy, fifteen references, live length 253 and allocation to
s4. Therefore neither the retail copy nor the earlier failed word-only
control uniquely establishes short storage. This counterexample supplies a
limit on type inference, not an explanation for the remaining comparison-one
residue or permission to select types from fuzzy scores.

Plan, isolated source, complete traces, raw comparisons and the three-way
trace audit are under `ending-sequence-word-narrow*` in
`build/open-missed-evidence/`. Production source and configuration remain
unchanged; the preceding full verification still applies.

## Lighting guard orientation and case exits

The main-worktree review at `6a56ebd` found only literal/count naming changes
in the two OPEN targets and their relevant shared declarations. The new GAME
closures do not supply an OPEN type or call-boundary correction. No unrelated
changes were imported. The existing display independent-arm and single-pass
controls were also located, so those forms were not repeated.

Two separately planned, isolated ending controls examine the decoded lighting
case exits without changing phase storage, the sequence dispatcher, calls or
other work. Prior generated-source and manifest searches found neither form.

The transition-first control spells `lighting_blend > KF_FIXED12_ONE` and its
phase advance/reset before the interpolation else arm in each case. It is
1944 bytes at 98.549385%, with seventeen unequal words. All original seven
dispatch differences remain. Ten additional differences move the shared reset
tail before the second interpolation block and redirect the two blend guards.
The first difference is still +398; the extra differences lie between +3c0
and +40c. The state-one/two stores still use t0, and the tails still share.
This source orientation is rejected rather than composed with another control.

The early-break control starts from the kept source. It preserves the <=4096
interpolation guard, breaks immediately after that call/increment path, and
places the phase advance/reset after the guard with its own case break.
Removing the else source node produces the entire kept resolved unit exactly:
99.917694%, 1944 bytes and the same seven words. It is not retained.

Both controls pass native/traced complete-ELF parity, forty ordered calls,
31 ordered referents, all 264-byte frame/save checks and six raw-exact
siblings. Plans, source, traces, full raw comparisons and the transition-first
instruction audit are under `ending-transition-first*` and
`ending-case-early-break*` in the campaign build directory. Production code
and configuration remain unchanged; whitespace checks pass and the preceding
full verification still applies.

A proposed display-height narrowing control was stopped at the SDK evidence
check, before generation or compilation. Pinned Release 2.5 `LIBGPU.H` lines
605–606 declare both SetDefDispEnv and SetDefDrawEnv with `int x, y, w, h`.
The short rectangle fields do not make those API arguments short. Thus that
API premise does not support narrowing the current word-sized height local;
no height-type experiment or result is claimed.

## Sequence early exits and direct display-height arguments

The sequence control makes the pending countdown and fade paths break
immediately from their outer switch cases. It preserves the decrement before comparison with -1,
the pre-call volume decrement, post-call zero test, phase assignments
and sequence-load timing. Earlier nested-switch and named-predicate controls
do not express these early outer-case exits. The separately planned complete
TU produces every kept resolved instruction/call/reference exactly:
99.917694%, 1944 bytes and the same seven retail differences. Native/traced
whole-ELF parity, forty calls, 31 references, the 264-byte frame and six exact
siblings pass. The source rearrangement is not retained.

A separate display control removes the unescaped `framebuffer_height` local,
assigned 240 once and used only by the four setup calls, and spells
KF_DISPLAY_HEIGHT directly at those arguments. It preserves the signed-short
lower-buffer Y and the actual SDK `int` dimension interface. Of 75 prior
generated initializer sources inspected, those without this local were GAME
bodies or reduced DTD-only controls, not full OPEN direct-height trials.
The GAME homolog also uses the fixed height directly; this is a source-form
control, not proof of historical spelling or a new width claim.

All four resolved functions equal the kept display unit. Display remains
96.652540%, 476 bytes, eighteen calls and the same extra early second-DRAWENV
reference. Native/traced whole-ELF parity and the retail 48-byte frame/save
map pass. The first-DTD address still has three references, crosses no calls
and receives v1; all four DFE stores remain relative to the second DRAWENV
through CSE1, CSE2 and allocation. Removing the height local therefore does
not account for the retained-base residue and is not adopted.

Plans, isolated sources, complete traces and baseline comparisons are under
`ending-sequence-early-exit*` and `display-direct-height*` in the campaign
build directory. Production code/configuration remain unchanged; whitespace
checks pass and the preceding full verification still applies.

## Previous-volume consumer declaration

The earlier postcall int/bare-long controls changed the counter declaration,
not the separate old-value consumer in the postvalue experiment. A planned
control changes only `s32 previous_volume = sequence_volume--;` to `int`
in that retained generated source. Both are signed 32-bit under the pinned
ABI; the counter, decrement timing, submitted values, completion comparison
and phase transition remain unchanged. Previous generated-source searches
found no int previous-volume consumer.

The entire resolved seven-function unit equals the previous postvalue trial.
It remains 99.331276%, 1944 bytes, with nineteen differences confined to the
audio block and all original seven dispatch words recovered. Native/traced
whole-ELF parity, forty calls, 31 references, 264-byte frame and six raw-exact
siblings pass. The old value still survives the call and the completion test
still compares it with one; nominally changing this consumer does not recover
retail's new-value reload and zero test. No declaration is retained.

Plan, isolated source, traces and full prior-object comparison are under
`volume-previous-int*` in the campaign build directory. Production code and
configuration remain unchanged, and whitespace checks pass. This result does
not justify extrapolating the distinct postcall typedef behavior to a local
that is already materialized before the call.

## Register-held tick comparisons and the top-color lifetime

The unescaped signed-short scroll tick starts at zero and cycles through
0,3,2,1 while scrolling is active. Decrement to -1 is immediately replaced
with 3 before another consumer runs. The panel guard selects update states
0/2; states 1/3 hold. A new planned predicate excludes those two hold states
without changing the representation or update. It agrees on all reachable
states, although retail's actual 0/2 branch instructions remain mandatory.
Previous tick trials changed decrement/sentinel syntax, not this predicate.

| Tick panel source | Top-color source | Strict score | Bytes | Original seven words recovered |
| --- | --- | ---: | ---: | --- |
| Exclude hold states 1/3 | Kept frame-entry pointer | 99.164610% | 1948 | No |
| Exclude hold states 1/3 | Original direct arguments | 99.236626% | 1948 | Yes |
| Explicit update and hold switch cases | Original direct arguments | 98.117290% | 1976 | No |

The first control leaves its three SI comparison-one definitions separate
through CSE1, loop and CSE2. The tick remains register-held. Its original
seven differences remain; the extra guard instruction shifts the later
panel code. This supplies a distinct control from the earlier equality on
the separately spilled scroll-enable local.

The second control removes only the previously introduced top-color alias,
restoring its original direct arguments. Earlier measured invariant-selection
sensitivity justifies this specific pair. Three comparison-one definitions
at CSE1 (UIDs 574, 641, 903) become one definition (UID1077) after loop and
remain shared after CSE2. Reload still uses only t0. Every word before +658
matches retail, including all seven previously unequal dispatch words. The
first difference is the panel-exit branch's displaced target; +674 starts
the wrong 1/3 hold guard. The function is still four bytes too long. This
is a new shared-constant control, not an exact match or a source adoption.

The third control expresses all four real states in a switch: 0/2 share
the existing panel-update body; 1/3 and the implicit default do nothing.
It is equivalent to the original guard even for other short values. This
previously untested switch adds a larger branch tree and loses the shared
one pool: all three definitions remain separate through loop and CSE2.
The original seven differences return. The panel loop also changes register
assignments, and the function grows by 32 bytes. No table or new referent
appears. Reading the pinned switch expansion shows its short-index promotion
and case-tree generation; no source-backed simplification to retail's compact
0/2 guard was established. The switch is rejected.

All three controls pass native/traced complete-ELF parity, forty ordered
calls, 31 ordered referents, the full 264-byte frame/save map and six raw-exact
siblings. Shared lighting phase-one/two stores use t0 with the shared tail;
all three controls need one spill register. Plans, sources, traces and raw
comparison audits are under `ending-tick-hold-guard*`,
`ending-tick-hold-direct-top*` and `ending-tick-state-switch*` in the campaign
build directory. Neither guard nor pointer change is retained in production.
Whitespace checks pass; the preceding full production verification applies.

### Update-only switch follow-up

Pinned `stmt.c:group_case_nodes` merges only consecutive values whose labels
lead to the same real instruction. Alternating update values 0/2 and hold
values 1/3 cannot merge through this rule. An explicit default label alone
therefore gives no supported reason to expect the four-state tree to become
retail's compact guard; that default-label spelling was not tested.

A separately planned control removes the empty 1/3 hold cases, leaving only
0/2 sharing the actual update body and an implicit no-work default. It keeps
the preceding candidate's original direct top-color arguments. This source
matches the original predicate for every short value and adds no operations.

It restores the 1944-byte extent but falls to 97.942390%, with 160 unequal
aligned words. The first divergence is +2b4, where the top-color pointer uses
t1 instead of retail t0; subsequent pointer setup/scheduling also changes.
Reload needs t0 and t1 again. The two SI comparison-one definitions (UID574
and UID641) remain separate through CSE1, loop and CSE2. Thus removing the
hold comparisons loses the useful invariant sharing, rather than retaining
it while recovering retail's guard.

Native/traced complete-ELF parity, forty calls, 31 ordered references, the
264-byte frame and six raw-exact siblings pass. The control is rejected and
production remains unchanged. Plan, source, traces and first-divergence audit
are under `ending-tick-update-switch*` in the campaign build directory.
Whitespace checks pass; no new exact result or banking claim is made.

### Native loop-decision audit

Native GCC's `-dL` dump supplies the profitability decisions absent from the
JSON trace. Recompiling the three existing tick controls with and without
that dump option leaves assembly identical except for the single compiler
argument comment recording `-dL`. No production compiler option changes.

The hold predicate with direct color arguments has 230 real instructions in
the outer loop. Its one pool has lifetime three and savings three; UID574 is
moved to UID1077. The explicit four-state switch has 235 outer-loop
instructions and rejects that same lifetime/savings pool as not desirable.
Both contain three matching comparison-one definitions, so the failed switch
is not missing a source use of one. Its larger generated branch tree changes
the movement decision.

The hold predicate with the kept top pointer also has 230 outer-loop
instructions, but moves its address at UID480 first. The following one pool,
also lifetime three/savings three, is rejected. This agrees with the previously
observed threshold reduction after an invariant moves. The native source's
movement test is `threshold * savings * lifetime >= insn_count`, subject to
its other dependency/forced-movement conditions. These are observations of
the pinned probe, not historical compiler attribution or a reason to remove
real operations to satisfy the heuristic.

Dump files, compact decisions and assembly parity checks are under
`tick-loop-decisions/` in the campaign build directory. No new source
candidate was introduced by this audit; both OPEN targets remain non-exact.

### Tick-switch consumer width

A separate planned control keeps the signed-short tick and wrap update but
views it as u16 at the explicit four-state switch. All reachable dispatch
values are 0..3. For other signed-short values, both source switches still
perform no panel work; only cases 0/2 update. Prior unsigned-consumer trials
covered lighting/sequence, not this tick switch. No storage or state encoding
changes, and the direct color arguments remain as in the previous control.

This removes one instruction from the signed switch, producing 1972 bytes at
97.870370%. All seven original dispatch words become exact again. Native loop
dumps explain the change: the outer loop decreases from 235 real instructions
to 234, and the lifetime-three/savings-three one pool now moves (UID574 to
UID1098). Dump/plain assembly differs only in the known argument comment.
This is a paired observation of the profitability boundary, not an optimizer
attribution for the original retail compiler.

The first retail divergence is +658, the displaced panel-exit target. The
panel loop retains its larger switch tree and changed register assignments;
the unsigned selector uses an andi instead of the signed promotion sequence.
The function remains 28 bytes longer than retail. Native/traced whole-ELF
parity, forty calls, 31 references, one t0 spill, the 264-byte frame and six
raw-exact siblings pass. The cast/switch source is not adopted.

Source, plan and full comparisons are under `ending-tick-switch-unsigned*`;
the native decisions are under `tick-loop-decisions/ending-tick-switch-unsigned`.
Production inputs remain unchanged and whitespace checks pass.

### Panel-cadence parity control

A separately planned control replaces only the kept panel0/2 predicate with
`(KF_ENUM_ENCODE(s16, scroll_tick) & 1) == 0`. The private tick's reachable
0,3,2,1 cycle supports that cadence interpretation; no state, update, pointer
or other operation changes. Previous generated-source/manifest searches found
no parity guard. Equivalence over reachable states does not establish that
retail's literal two-comparison guard came from this source expression.

The candidate is1932 bytes at99.053500%, twelve bytes shorter than retail.
It retains all seven original dispatch differences. At+668 it computes the
low bit with `andi s3,s6,1`, and the panel guard becomes one nonzero branch.
The first additional difference at+658 is the displaced exit target. The
two comparison-one definitions remain separate through CSE1, loop and CSE2.
Native/traced whole-ELF parity, forty calls,31references, one t0 spill, the
264-byte frame and six raw-exact siblings pass. The source is rejected;
it does not reproduce the retail selector promotion or pair of branches.

Plan, source, traces and raw inspection are under `ending-tick-parity*`.
Production inputs remain unchanged; whitespace checks pass. No exact or
banking claim is made.

## Top-color pointer initialized with its object

The original pointer control names top only at frame-loop entry. A separate
planned lifetime control moves that same typed pointer initialization beside
the local color declarations, preserving the three actual arguments and all
other statements. The independent SDK objects, stack spacing and their uses
are unchanged. Retail forms and saves their addresses before its frame loop;
this permits testing the source lifetime but does not prove entry-time setup.

The candidate is1944 bytes at97.495890%, with309 unequal aligned words. The
first difference is+74: early top-color address construction and its stack
store precede the retail initialization sequence. Reload again requires t0
and t1. Native/traced whole-ELF parity, forty calls,31ordered references,
the264-byte frame/save map and six raw-exact siblings pass. The earlier
initialization is rejected; the kept loop-entry pointer remains unchanged.

Plan, source, traces and complete baseline comparisons are under
`ending-top-object-lifetime*` in the campaign build directory. No production
input changes or banking occur, and whitespace checks pass.

## Display SDK return-value exclusion

A fresh SDK/caller audit checks a distinct possible pointer lifetime before
compiling it. Pinned LIBGPU.H602 declares `DRAWENV *PutDrawEnv(DRAWENV *env)`.
OPEN's provider at80030a2c saves its input in s1 and returns that pointer via
`move v0,s1` at80030ad4. Its return delay slot restores the frame.

However, display initialization does not consume either returned pointer.
Its calls at80016c24/80016c2c have respectively the second DFE clear and
`move a0,s2` as delay slots. Immediately afterward,80016c34/80016c38 restore
both DFE bytes through s0+1/+93 using s1. There is no intervening transfer
or address derivation from v0. A source assignment from PutDrawEnv followed
by use of that returned pointer would add a dependency not present here;
the separately compiled SDK boundary does not supply an identity-return
optimization contract to the caller. No return-capturing source is generated.

This excludes that proposed correction under the pinned separate-compilation
model, not the authentic SDK return type. The image-qualified caller capture
is `display-return-value-audit.txt` in the campaign build directory. Source
and configuration stay unchanged; no new match or compilation is claimed.

## Explicit terminal sequence case

A planned control adds only `case ENDING_SEQUENCE_REPLACED: break` to the
kept sequence switch. That terminal state is explicitly assigned by the fade
completion path. The initial two-corpus search found an explicit WAIT_SCROLL
trial but no REPLACED case. A subsequent all-worktree audit found the REPLACED
construct in open-remaining on a biased-countdown base; this was a new
composition with the retained top pointer, not a previously untried construct.
This tests the existing terminal exit's switch lowering,
without changing the state transition or adding a source operation.

The result is 1944 bytes at98.401240%, with22 unequal aligned words. The
first difference remains+398, and the lighting dispatch's original five
unequal words remain. At+414 the sequence switch now loads2 first, compares
that case, and then uses a signed less-than3 test before testing1. This is a
new sequence decision tree, not the retail two-equality dispatch. The altered
branch structure shifts instructions through+460 before rejoining retail.
Native/traced whole-ELF parity, forty calls,31ordered references, the264-byte
frame/save map, one t0 spill and six raw-exact siblings pass. The candidate
is rejected and production source remains unchanged.

The plan, generated source, trace and full raw comparisons are under
`ending-sequence-terminal*` in the campaign build directory. No banking or
exact claim is made.

## Read-only top-color draw view

A planned role-split control keeps the frame-entry top pointer for the two
sprite arguments, qualifies its pointee const, and restores the interpolation
output to direct `&top_color`. The shared signatures support this separation:
interpolation writes a CVECTOR output while sprite_add_g4 reads const CVECTOR
inputs. Prior controls qualified the pointer object or shared it across all
three consumers; this control adds no object or source operation.

The result is1940 bytes at99.588480%. All seven original dispatch differences
remain. Its first additional difference is+564: `addiu a2,sp,152` fills a slot
that retail leaves empty, and the later top-output address reload at+584 is
removed. Following words therefore shift by four bytes; the140 unequal
aligned words must not be interpreted as140 independent source defects.
Native/traced whole-ELF parity, forty calls,31ordered references,264-byte
frame/save map, one t0 spill and six raw-exact siblings pass. This distinct
argument lifetime does not recover the retail schedule and is rejected.

The plan, generated source, raw comparison and traces are preserved under
`ending-top-draw-view*`. Production inputs and banked results are unchanged.

## Interpolation-only top-color pointer

The complementary planned control retains top only as the interpolation
output pointer and restores both direct sprite arguments. Searches of both
worktrees' generated C found no prior output-only pointer trial. No type,
scope, object, operation or state changes accompany the two argument edits.

This produces precisely the kept resolved words, calls and references for
every function in the unit:1944 bytes,99.917694%, the same seven dispatch
words unequal. Native/traced whole-ELF parity, forty calls,31references and
six exact siblings pass. The control is neutral and is not adopted.
Together with the draw-only result, this shows that the explicit writable
output pointer is sufficient for the retained code; spelling the later two
sprite arguments through that pointer is not necessary. Further permutations
of those two identical read-only consumers have no evidenced purpose.

Plan, source and full comparisons are under `ending-top-output-view*`.
Production source, exact counts and banking remain unchanged.

## Independent bottom-color pointer

The preceding bottom-pointer control retained top. A separate planned control
uses bottom as the sole explicit color pointer, restoring all three direct
top arguments. Retail independently saves both object addresses, and searches
of both generated-C corpora found no previous bottom-only trial. No object,
state, operation or profile changes accompany this lifetime control.

The result is1944 bytes at99.901240%, with15 unequal words. All seven original
dispatch differences remain. The other eight exchange the address homes:
+2b4/+2cc form sp+160/sp+152 in the opposite order, then+584,+5a0,+5ac,+5b0,
+5b4,+5b8 exchange the corresponding sp+208/sp+216 reloads. These are real
address scheduling differences, with the same resolved color referents.
Native/traced whole-ELF parity,40calls,31references,six exact siblings and
one t0 spill pass. The bottom-only source is rejected.

The result shows that the single-spill outcome is not unique to top; bottom
alone also supplies it, but fails the retail address-home ordering and the
same comparison-one dispatch. This narrows the prior role-split conclusion
without claiming a new exact result. Full evidence is under
`ending-bottom-only*`; production inputs and banking remain unchanged.

## Enumerator-name frontend exclusion

A direct pinned-frontend audit checks whether naming the state constant1
could itself introduce a different expression from its literal initializer.
The legacy KF_ENUM macros declare scalar typedefs and anonymous enumerators;
they do not cast each enumerator use to the short state storage type.
In c-decl.c5645..5678, build_enumerator stores the integer value as the
CONST_DECL initializer and selects at least integer precision. All local
ending-state initializers fit signed int. In c-parse.y572..579, an identifier
resolved to CONST_DECL is replaced with DECL_INITIAL and wrapped in a
same-type NOP_EXPR, explicitly to distinguish enum zero from a null-pointer
constant. The numeric binary-operator path strips same-type no-op wrappers
in c-typeck.c1892..1894. Thus the parser does retain a distinction, but this
wrapper supplies no evidenced correction to the numeric state comparisons
by replacing their enumerator names with the same integer literals.
No literal-renaming source control is generated. This is distinct from the
already tested scalar typedef identity issue, and does not assert historical
compiler attribution or solve the remaining dispatch residue.

## Native loop decisions for the independent color views

A native -dL audit of the two existing role controls confirms their changed
address order before allocation. Bottom-only's outer loop has229 real
instructions: UID480/p141,life160/savings1, moves first toUID1077 and computes
fp+160; the later top address moves toUID1078 and computes fp+152. The
interpolation-only top control has230 instructions: UID480/p141,life143/
savings2, moves first toUID1081 and computes fp+152; bottom moves next to
UID1082 and computes fp+160. Its additional direct top address matches the
first invariant in the loop report.

Both reject moving the two-use comparison-one pool (UID578/p156,life2,
savings2) and the three-use sentinel pool (UID592/p157,life3,savings3).
Thus the observed address-home exchange already has a corresponding loop
movement order; these controls give no new dispatch-pool state. The different
instruction counts also caution against equating identical final code with
identical intermediate optimizer input.

For both, plain/dump native assembly is identical except for the exact
known compiler-argument comment containing -dL. No production compiler option
or source changes. Dumps and paired results are in
`tick-loop-decisions/{ending-bottom-only,ending-top-output-view}` and
`tick-loop-decisions/color-role-result.json`.

## Retained campaign verification

Before committing these source lifetimes, both units were freshly compiled
with kf try. A separate full raw audit against the preserved retained results
confirmed every resolved instruction, call and reference in both units.
Strict results remain99.917694%/1944B for ending and96.652540%/476B for
display. All nine sibling functions across the two units remain raw exact.

Repository verification passes703 tests with9 skipped, ruff check scripts
tests, and git diff --check. Full kf build still fails the previously recorded
data-ownership and section-placement checks; it reports OPEN106/108 exact,
with6/6 vendored source functions exact. No new banking occurs. This commit
preserves nonexact source improvements and evidence, not closure of either
function. Fresh logs are `precommit-{ending,display,raw,ruff,tests,full-build}`
under the campaign build directory. Generated products are not committed.

## Broader generated-corpus coverage correction

An all-registered-worktree search found additional generated target sources
outside this worktree and gcc257-trace: 692 in main, 1943 in open-remaining,
20 display-named sources in game-362, and 3 each in data-matching and
unknown-names. A function name alone does not establish the OPEN image; the
game-362 display sources must retain their GAME identity. Main's dominant
constant-names directory contains 687 sources; the remaining main controls
include the earlier color-scope and DFE-pair experiments, both recorded as
neutral against their own older baselines.

The open-remaining hypothesis inventory contains 125 reports for the two
OPEN targets. Its maximum recorded display score is 96.652540%/476B and
ending score 99.781890%/1944B. These historical percentages are discovery
metadata, not closure evidence under the corrected branch comparator. No
report exceeds the retained results, and no old candidate is adopted here.
In particular open-scroll-inactive-cases-16 already tested explicit sequence
REPLACED cases with a biased countdown. The recent terminal-case test's
original novelty claim was too broad and is corrected above. Future searches
must include this corpus before calling a source construct untested.

Generated path and report inventories are
`other-worktree-generated-sources.json` and
`other-worktree-result-frontiers.json` in the campaign build directory.
This audit changes the exclusion corpus, not production code or banked scores.

### Additional display trace coverage

The newly inventoried open-remaining corpus has five display trace summaries:
three copies of the 96.652540% baseline and two lower-scoring candidates.
The branch-pointer candidate is 90.652540% with a 40-byte frame; its identified
DTD root crosses zero calls. The late-second complete-fields candidate is
90.957630% with a 40-byte frame and no uniquely identified DTD-root pseudo.
Neither summary supplies the required 48-byte frame together with a retained
DTD root across PutDrawEnv. Zero store counters or an unavailable pseudo
are not treated as proof that a particular memory operation is absent.

The hypothesis directory retains no object files; the gcc257 directory has
28 objects including target copies and ending controls. Thus the 125 score
reports are not 125 independently available object audits. Do not elevate
those historical scores or missing features into a raw-byte verdict, and do
not recompile their source spellings without an independent reason.

### Default-environment return values

The SDK-return audit extends to SetDefDrawEnv and SetDefDispEnv, rather than
assuming the earlier PutDrawEnv exclusion covers them. Pinned LIBGPU.H:605–606
returns DISPENV* and DRAWENV* respectively; each takes four int coordinates
and dimensions after its environment pointer. The display caller's four
calls are at 80016b44, 80016b60, 80016b7c, and 80016b98. Their delay slots all set a3
to 320. No following instruction reads the returned v0: setup proceeds from
s0/s1 and fresh immediate addresses, and v0 is explicitly replaced with 254
at 80016bfc. Thus deriving retained draw pointers from these SDK return values
would introduce a dependency absent from retail's separate-compilation call
sequence. No return-capturing candidate is generated. This excludes a
specific pointer-origin proposal; it does not change the authentic API types.
