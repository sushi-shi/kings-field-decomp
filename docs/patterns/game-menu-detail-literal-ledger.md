# Retained GAME item-detail and dialog literals

Complete account of **100 numeric/character occurrences** in
`src/game/menu_item_detail.c` after the [detail/dialog review](game-menu-detail-constants.md).
Comments, strings, identifier digits, named definitions and retail claims are
excluded. Every repeated token has its own row; signs are expression operators.
The marker and foreground helper functions have no remaining literal tokens.

The [floor enum review](game-floor-enum-domain.md) propagates floor identifiers
through the current source; this ledger reflects its named comparisons and
explicit numeric boundaries.

| Function | Line | Token | Expression | Reason |
| --- | ---: | --- | --- | --- |
| `menu_draw_item_detail` | 48 | `0` | `rot.t[0] = 0x230;` | GTE translation x component 560, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 48 | `0x230` | `rot.t[0] = 0x230;` | GTE translation x component 560, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 49 | `1` | `rot.t[1] = 0x8c;` | GTE translation y component 140, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 49 | `0x8c` | `rot.t[1] = 0x8c;` | GTE translation y component 140, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 50 | `2` | `rot.t[2] = 0x5dc;` | GTE translation z component 1500, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 50 | `0x5dc` | `rot.t[2] = 0x5dc;` | GTE translation z component 1500, in the transform coordinate units; authored preview placement, not an angle or Q12 coefficient. |
| `menu_draw_item_detail` | 56 | `0` | `lsrc.m[0][0] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 56 | `0` | `lsrc.m[0][0] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 57 | `0` | `lsrc.m[0][1] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 57 | `1` | `lsrc.m[0][1] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 58 | `0` | `lsrc.m[0][2] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 58 | `2` | `lsrc.m[0][2] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 59 | `1` | `lsrc.m[1][0] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 59 | `0` | `lsrc.m[1][0] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 60 | `1` | `lsrc.m[1][1] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 60 | `1` | `lsrc.m[1][1] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 61 | `1` | `lsrc.m[1][2] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 61 | `2` | `lsrc.m[1][2] = -KF_FIXED12_ONE;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 62 | `2` | `lsrc.m[2][0] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 62 | `0` | `lsrc.m[2][0] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 62 | `0` | `lsrc.m[2][0] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 63 | `2` | `lsrc.m[2][1] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 63 | `1` | `lsrc.m[2][1] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 63 | `0` | `lsrc.m[2][1] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 64 | `2` | `lsrc.m[2][2] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 64 | `2` | `lsrc.m[2][2] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 64 | `0` | `lsrc.m[2][2] = 0;` | Matrix row/column indices select the 3×3 directional coefficients: first two rows use named negative Q12 unity; third row is zero and contributes no direction. |
| `menu_draw_item_detail` | 73 | `0xae` | `gs.x = 0xae;` | Authored text-origin x coordinate 174 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 74 | `0x24` | `gs.y = 0x24;` | Authored text-origin y coordinate 36 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 76 | `0` | `for (i = 0; i < MENU_GLYPHS_PER_ROW; i++) {` | Copy the ten signed glyph halfwords of the shared name-row representation, starting at its first entry. |
| `menu_draw_item_detail` | 81 | `0xc8` | `gs.x = 0xc8;` | Authored text-origin x coordinate 200 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 87 | `1` | `menu_format_number(prices[item_id][shop_id - 1], 6, 0, gs.codes);` | Convert one-based shop column to a zero-based array index; render six price digits with blank rather than zero padding. |
| `menu_draw_item_detail` | 87 | `6` | `menu_format_number(prices[item_id][shop_id - 1], 6, 0, gs.codes);` | Convert one-based shop column to a zero-based array index; render six price digits with blank rather than zero padding. |
| `menu_draw_item_detail` | 87 | `0` | `menu_format_number(prices[item_id][shop_id - 1], 6, 0, gs.codes);` | Convert one-based shop column to a zero-based array index; render six price digits with blank rather than zero padding. |
| `menu_draw_item_detail` | 90 | `0xf2` | `gs.x = 0xf2;` | Authored text-origin x coordinate 242 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 91 | `0` | `gs.codes[0] = MENU_TEXT_DAKUTEN \| 0x9;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 91 | `0x9` | `gs.codes[0] = MENU_TEXT_DAKUTEN \| 0x9;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 92 | `1` | `gs.codes[1] = 0x2d;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 92 | `0x2d` | `gs.codes[1] = 0x2d;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 93 | `2` | `gs.codes[2] = 0x2a;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 93 | `0x2a` | `gs.codes[2] = 0x2a;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 94 | `3` | `gs.codes[3] = MENU_TEXT_DAKUTEN \| 0x13;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 94 | `0x13` | `gs.codes[3] = MENU_TEXT_DAKUTEN \| 0x13;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 95 | `4` | `gs.codes[4] = MENU_TEXT_END;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 98 | `0xf2` | `gs.x = 0xf2;` | Authored text-origin x coordinate 242 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 99 | `0` | `gs.codes[0] = 0xca;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 99 | `0xca` | `gs.codes[0] = 0xca;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 100 | `1` | `gs.codes[1] = 0xcb;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 100 | `0xcb` | `gs.codes[1] = 0xcb;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 101 | `2` | `gs.codes[2] = MENU_TEXT_END;` | Consecutive glyph or terminator position in the authored gold/held-quantity labels. Numeric right-hand sides are font-atlas codes; named diacritic/terminator bits retain their distinct encoding meanings. |
| `menu_draw_item_detail` | 105 | `0x11c` | `gs.x = 0x11c;` | Authored text-origin x coordinate 284 screen pixels; retained as local layout data, with original placement rationale unknown. |
| `menu_draw_item_detail` | 106 | `0` | `menu_format_number(item_stock[0][item_id], 2, 0, gs.codes);` | Player-owned stock bank zero; two quantity digits fit the known 99 stack capacity, with blank leading padding. |
| `menu_draw_item_detail` | 106 | `2` | `menu_format_number(item_stock[0][item_id], 2, 0, gs.codes);` | Player-owned stock bank zero; two quantity digits fit the known 99 stack capacity, with blank leading padding. |
| `menu_draw_item_detail` | 106 | `0` | `menu_format_number(item_stock[0][item_id], 2, 0, gs.codes);` | Player-owned stock bank zero; two quantity digits fit the known 99 stack capacity, with blank leading padding. |
| `menu_draw_item_detail` | 114 | `28` | `gs.x = menu_window_layouts[KF_ENUM_ENCODE(s32, KF_MENU_WINDOW_SHOP)].rows[KF_SHOP_ROW_GOLD].x + 28;` | Place the gold value 28 screen pixels right of menu window 7 row 3, with the original spacing rationale unproven. |
| `menu_draw_item_detail` | 116 | `6` | `menu_format_number(player_state.gold, 6, 0, gs.codes);` | Render six gold digits without leading zeros; authored field width, shared numeric-render API Boolean. |
| `menu_draw_item_detail` | 116 | `0` | `menu_format_number(player_state.gold, 6, 0, gs.codes);` | Render six gold digits without leading zeros; authored field width, shared numeric-render API Boolean. |
| `menu_draw_dialog_frame` | 160 | `0` | `if (highlighted_slot == 0) {` | Zero-based first slot: omit its named overlay and enqueue the second and third slot overlays. |
| `menu_draw_dialog_frame` | 166 | `1` | `if (highlighted_slot == 1) {` | Zero-based second slot: omit its named overlay and enqueue the first and third slot overlays. |
| `menu_draw_dialog_frame` | 172 | `2` | `if (highlighted_slot == 2) {` | Zero-based third slot: omit its named overlay and enqueue the first and second slot overlays. |
| `menu_draw_dialog_frame` | 187 | `0` | `if (rows == 0) {` | A null summary pointer suppresses text after overlay insertion; the selector still controls overlays independently. |
| `menu_draw_dialog_frame` | 191 | `0` | `for (i = 0; i < KF_SAVE_SLOT_COUNT; i++) {` | Start at the first zero-based save summary and visit all three records. |
| `menu_draw_dialog_frame` | 192 | `30` | `gs.y = i * MENU_SAVE_SUMMARY_ROW_HEIGHT + 30;` | Authored first-summary baseline is screen Y30; named row spacing locates later slots. The original choice of this six-pixel offset from the first overlay top is unknown. |
| `menu_draw_dialog_frame` | 193 | `0` | `if ((s32)rows[i].current_hp > 0) {` | Only positive saved HP draws a populated summary; preserve the signed interpretation and zero boundary. |
| `menu_draw_dialog_frame` | 195 | `0` | `gs.codes[0] = 0x82;` | Glyph position 0 in the authored summary label: text-atlas cell 0x82, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 195 | `0x82` | `gs.codes[0] = 0x82;` | Glyph position 0 in the authored summary label: text-atlas cell 0x82, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 196 | `1` | `gs.codes[1] = 0x83;` | Glyph position 1 in the authored summary label: text-atlas cell 0x83, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 196 | `0x83` | `gs.codes[1] = 0x83;` | Glyph position 1 in the authored summary label: text-atlas cell 0x83, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 197 | `2` | `gs.codes[2] = 0x84;` | Glyph position 2 in the authored summary label: text-atlas cell 0x84, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 197 | `0x84` | `gs.codes[2] = 0x84;` | Glyph position 2 in the authored summary label: text-atlas cell 0x84, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 198 | `3` | `gs.codes[3] = MENU_TEXT_END;` | Glyph position 3 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 201 | `251` | `gs.x = 251;` | Authored screen X251 for the six-digit experience value; no recovered reason chooses this origin. |
| `menu_draw_dialog_frame` | 202 | `6` | `menu_format_number(rows[i].experience, 6, 0, gs.codes);` | Present experience in six decimal positions with blank leading padding. Keep this local display width rather than equating it with the numeric storage range. |
| `menu_draw_dialog_frame` | 202 | `0` | `menu_format_number(rows[i].experience, 6, 0, gs.codes);` | Present experience in six decimal positions with blank leading padding. Keep this local display width rather than equating it with the numeric storage range. |
| `menu_draw_dialog_frame` | 206 | `0` | `gs.codes[0] = 0xcc;` | Glyph position 0 in the authored summary label: text-atlas cell 0xcc, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 206 | `0xcc` | `gs.codes[0] = 0xcc;` | Glyph position 0 in the authored summary label: text-atlas cell 0xcc, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 207 | `1` | `gs.codes[1] = 0xcd;` | Glyph position 1 in the authored summary label: text-atlas cell 0xcd, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 207 | `0xcd` | `gs.codes[1] = 0xcd;` | Glyph position 1 in the authored summary label: text-atlas cell 0xcd, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 208 | `2` | `gs.codes[2] = MENU_TEXT_END;` | Glyph position 2 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 212 | `286` | `gs.x = 286;` | Authored screen X286 for the single-digit floor value; no recovered reason chooses this origin. |
| `menu_draw_dialog_frame` | 213 | `1` | `menu_format_number(KF_ENUM_ENCODE(u32, rows[i].current_floor), 1, 0, gs.codes);` | Present the floor in one decimal position with blank leading padding; the field is a display parameter, not a count of available floors. |
| `menu_draw_dialog_frame` | 213 | `0` | `menu_format_number(KF_ENUM_ENCODE(u32, rows[i].current_floor), 1, 0, gs.codes);` | Present the floor in one decimal position with blank leading padding; the field is a display parameter, not a count of available floors. |
| `menu_draw_dialog_frame` | 217 | `0` | `gs.codes[0] = 0xf0;` | Glyph position 0 in the authored summary label: text-atlas cell 0xf0, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 217 | `0xf0` | `gs.codes[0] = 0xf0;` | Glyph position 0 in the authored summary label: text-atlas cell 0xf0, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 218 | `1` | `gs.codes[1] = 242;` | Glyph position 1 in the authored summary label: text-atlas cell 242, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 218 | `242` | `gs.codes[1] = 242;` | Glyph position 1 in the authored summary label: text-atlas cell 242, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 219 | `2` | `gs.codes[2] = MENU_TEXT_END;` | Glyph position 2 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 224 | `0` | `menu_format_number(rows[i].current_hp, MENU_SAVE_STATUS_DIGITS, 0, gs.codes);` | Zero requests blank leading padding in the existing numeric-format API; the four-digit width is named separately. |
| `menu_draw_dialog_frame` | 227 | `0` | `gs.codes[0] = MENU_NUMBER_SLASH;` | Glyph position 0 in the authored summary label: the named slash belongs to the numeric atlas. |
| `menu_draw_dialog_frame` | 228 | `1` | `gs.codes[1] = MENU_TEXT_END;` | Glyph position 1 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 233 | `0` | `menu_format_number(rows[i].maximum_hp, MENU_SAVE_STATUS_DIGITS, 0, gs.codes);` | Zero requests blank leading padding in the existing numeric-format API; the four-digit width is named separately. |
| `menu_draw_dialog_frame` | 237 | `0` | `gs.codes[0] = 0xf1;` | Glyph position 0 in the authored summary label: text-atlas cell 0xf1, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 237 | `0xf1` | `gs.codes[0] = 0xf1;` | Glyph position 0 in the authored summary label: text-atlas cell 0xf1, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 238 | `1` | `gs.codes[1] = 242;` | Glyph position 1 in the authored summary label: text-atlas cell 242, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 238 | `242` | `gs.codes[1] = 242;` | Glyph position 1 in the authored summary label: text-atlas cell 242, not an ASCII/Unicode value. Retain this encoded label data instead of inventing a per-character constant. |
| `menu_draw_dialog_frame` | 239 | `2` | `gs.codes[2] = MENU_TEXT_END;` | Glyph position 2 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 244 | `0` | `menu_format_number(rows[i].current_mp, MENU_SAVE_STATUS_DIGITS, 0, gs.codes);` | Zero requests blank leading padding in the existing numeric-format API; the four-digit width is named separately. |
| `menu_draw_dialog_frame` | 247 | `0` | `gs.codes[0] = MENU_NUMBER_SLASH;` | Glyph position 0 in the authored summary label: the named slash belongs to the numeric atlas. |
| `menu_draw_dialog_frame` | 248 | `1` | `gs.codes[1] = MENU_TEXT_END;` | Glyph position 1 in the authored summary label: the named terminator ends this stream. |
| `menu_draw_dialog_frame` | 253 | `0` | `menu_format_number(rows[i].maximum_mp, MENU_SAVE_STATUS_DIGITS, 0, gs.codes);` | Zero requests blank leading padding in the existing numeric-format API; the four-digit width is named separately. |
