# Player progression and combat-stat literal ledger

Complete ledger for the four functions below: **45 retained occurrences**,
down from 63. Claims and named definitions are excluded; signs are operators.
Other functions and initializers in `player_death.c` remain outside this ledger.
See the [progression review](game-player-progression.md) for evidence and verification.

| Function | Lines | Tokens | Expression | Reason |
| --- | --- | --- | --- | --- |
| `player_recalculate_combat_stats` | 238 | `0` | `player_state.cutting_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 239 | `0` | `player_state.striking_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 240 | `0` | `player_state.piercing_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 241 | `0` | `player_state.holy_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 242 | `0` | `player_state.fire_attack = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 243 | `0` | `player_state.cutting_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 244 | `0` | `player_state.striking_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 245 | `0` | `player_state.piercing_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 246 | `0` | `player_state.poison_resistance = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 247 | `0` | `player_state.magic_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 248 | `0` | `player_state.fire_defense = 0;` | Clear this named attack/defense/resistance accumulator before rebuilding equipment contributions; zero is the additive origin, not a shared state code. |
| `player_recalculate_combat_stats` | 253 | `0` | `if (power < 0) {` | Clamp curse-reduced power at the arithmetic zero floor before narrowing back to the player field. |
| `player_recalculate_combat_stats` | 254 | `0` | `power = 0;` | Clamp curse-reduced power at the arithmetic zero floor before narrowing back to the player field. |
| `player_recalculate_combat_stats` | 260 | `0` | `player_state.cutting_attack += weapon->attack_components[0];` | Weapon-record component 0 is the positional field for cutting_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 261 | `1` | `player_state.striking_attack += weapon->attack_components[1];` | Weapon-record component 1 is the positional field for striking_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 262 | `2` | `player_state.piercing_attack += weapon->attack_components[2];` | Weapon-record component 2 is the positional field for piercing_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 263 | `3` | `player_state.holy_attack += weapon->attack_components[3];` | Weapon-record component 3 is the positional field for holy_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 264 | `4` | `player_state.fire_attack += weapon->attack_components[4];` | Weapon-record component 4 is the positional field for fire_attack; the array order is cutting, striking, piercing, holy, fire. Preserve the existing resource representation. |
| `player_recalculate_combat_stats` | 318 | `5` | `player_state.holy_attack += 5;` | Light Ring adds five holy-attack points. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 321 | `7` | `player_state.magic_defense += 7;` | Moon Amulet adds seven magic-defense points. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 324 | `7` | `player_state.fire_defense += 7;` | Wind Blade Bracelet adds seven fire-defense points. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 327 | `8` | `player_state.magic += 8;` | Two-headed Dragon Ring adds eight derived-magic points. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 330 | `1` | `player_state.magic += 1;` | The Verdite accessory arm adds one derived-magic point; this differs from using it for training. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 333 | `3` | `player_state.holy_attack += 3;` | The Gold Cross accessory arm adds three holy-attack points. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 337 | `8` | `player_state.physical_power -= 8;` | Black Mask subtracts eight derived physical-power points without adding a zero-floor check. This named item/field assignment identifies the tuning; its original magnitude rationale is unproven. |
| `player_recalculate_combat_stats` | 354 | `1` | `if (player_state.physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_recalculate_combat_stats` | 357 | `1` | `if (player_state.magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_increment_physical_power_training` | 368 | `0` | `player_state.physical_power_training = 0;` | Reset the completed training counter to zero; excess points are discarded rather than carried to another gain. The raw reset occupies the subsequent cap-branch delay slot. |
| `player_increment_physical_power_training` | 369 | `1` | `if (player_state.base_physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_increment_magic_training` | 384 | `0` | `player_state.magic_training = 0;` | Reset the completed training counter to zero; excess points are discarded rather than carried to another gain. The raw reset occupies the subsequent cap-branch delay slot. |
| `player_increment_magic_training` | 385 | `1` | `if (player_state.base_magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_add_experience` | 409 | `1` | `player_state.progress_state.level = level + 1;` | Advance the one-based level by one after the maximum-level guard; the saved previous byte still selects the growth branch and row. |
| `player_add_experience` | 412 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].maximum_hp` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 413 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].maximum_hp;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 415 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].maximum_mp` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 416 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].maximum_mp;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 417 | `1` | `player_state.base_physical_power += player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].physical_power_step;` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 418 | `1` | `player_state.base_magic += player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].magic_step;` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 420 | `1` | `player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 1].experience_threshold` | Count minus one selects the final loaded record, row 39. Its HP/MP/experience values supply an endpoint difference, while its power fields remain direct per-level increments. |
| `player_add_experience` | 421 | `2` | `- player_level_growth_table[KF_PLAYER_LEVEL_GROWTH_COUNT - 2].experience_threshold;` | Count minus two selects the penultimate loaded record, row 38, whose value is subtracted from the final row to extrapolate the same field. |
| `player_add_experience` | 430 | `1` | `if (player_state.vitals.maximum_hp >= KF_PLAYER_VITAL_MAX + 1) {` | One above the inclusive HP/MP cap preserves the original >=10000 test on the already narrowed halfword field. |
| `player_add_experience` | 433 | `1` | `if (player_state.vitals.maximum_mp >= KF_PLAYER_VITAL_MAX + 1) {` | One above the inclusive HP/MP cap preserves the original >=10000 test on the already narrowed halfword field. |
| `player_add_experience` | 436 | `1` | `if (player_state.base_physical_power >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_add_experience` | 439 | `1` | `if (player_state.base_magic >= KF_PLAYER_POWER_MAX + 1) {` | One above the inclusive power cap preserves the original >=1000 test after the preceding halfword stores. Do not move the clamp before those stores. |
| `player_add_experience` | 444 | `2` | `sound_ref_play(&player_sound_refs[2], KF_AUDIO_MAX_VOLUME);` | Sound-reference row two is the level-up cue, distinct from death and other player sounds. The volume argument uses the shared full-volume constant. |
