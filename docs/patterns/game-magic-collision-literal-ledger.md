# Magic and effect-collision remainder literal ledger

Complete per-occurrence account of **52 retained numeric literals**:
nine in `src/game/magic.c`, 43 in `src/game/effect_map_collision.c`.
The latter includes all 28 authored rectangle halfwords. Named definitions
and retail claims are excluded; comments/strings/identifier digits are not
numeric literals, and signs remain operators. See the
[campaign review](game-magic-collision-constants.md) for units and verification.

| Source | Function | Line | Token | Expression | Reason |
| --- | --- | --- | --- | --- | --- |
| `magic.c` | `effect_pool_reset` | 30 | `0` | `for (i = 0; i < KF_EFFECT_CAPACITY; i++) {` | Zero starts the slot ordinal; the named capacity bounds the complete reset. |
| `magic.c` | `magic_load_records` | 42 | `0` | `for (count = sizeof effect_state.magic / sizeof *source; count != 0; count--) {` | Zero terminates the word count derived from the existing magic owner and source element sizes; no raw record count is duplicated. |
| `magic.c` | `magic_cast` | 84 | `0` | `if (target == 0) {` | Null denotes an absent actor from the cone query; this chooses untargeted versus targeted placement. |
| `magic.c` | `magic_cast` | 97 | `1` | `map_cell_attribute_grid[target->cell_z][target->cell_x] - 1]` | Lightning alone indexes the preceding attribute-table halfword; retail uses map_cell_attribute_height_table-2 plus twice the attribute. Keep this measured lookup bias, without inventing a level identity or new bounds guard. |
| `magic.c` | `magic_cast` | 126 | `1` | `KF_ENUM_DECODE(KfEffectKind, KF_ENUM_ENCODE(u8, player_state.selected_magic_id)), &world_pos, &direction, &rotation, 1);` | True requests Light Needle constructor sound in the seventh slot; the sixth slot is the rotation pointer, not a countdown. |
| `magic.c` | `magic_cast` | 130 | `1` | `KF_ENUM_DECODE(KfEffectKind, KF_ENUM_ENCODE(u8, player_state.selected_magic_id)), &world_pos, &direction, distance, 1);` | True requests the constructor sound in the seventh slot; the sixth value is consumed as a halfword travel countdown only by Lightning on this casting branch. |
| `magic.c` | `magic_cast` | 141 | `0` | `if (target != 0) {` | Null denotes an absent actor from the cone query; this chooses untargeted versus targeted placement. |
| `magic.c` | `effect_pool_sweep` | 172 | `1` | `u16 i = KF_EFFECT_CAPACITY - 1;` | Start at capacity minus one because the do/while tests the old count after processing each slot. |
| `magic.c` | `effect_pool_sweep` | 180 | `0` | `} while (i-- != 0);` | Zero is the final old countdown value after the last slot; postdecrement preserves exactly the named capacity of visits. |
| `effect_map_collision.c` | `initializers` | 9 | `500` | `{500, -2000, 1500, -1250},` | Rectangle 0, x_min=500: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 9 | `2000` | `{500, -2000, 1500, -1250},` | Rectangle 0, y_min=-2000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 9 | `1500` | `{500, -2000, 1500, -1250},` | Rectangle 0, x_max=1500: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 9 | `1250` | `{500, -2000, 1500, -1250},` | Rectangle 0, y_max=-1250: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 10 | `1000` | `{1000, -2500, 2000, -1000},` | Rectangle 1, x_min=1000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 10 | `2500` | `{1000, -2500, 2000, -1000},` | Rectangle 1, y_min=-2500: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 10 | `2000` | `{1000, -2500, 2000, -1000},` | Rectangle 1, x_max=2000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 10 | `1000` | `{1000, -2500, 2000, -1000},` | Rectangle 1, y_max=-1000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 11 | `0` | `{0, -2500, 2000, -1000},` | Rectangle 2, x_min=0: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 11 | `2500` | `{0, -2500, 2000, -1000},` | Rectangle 2, y_min=-2500: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 11 | `2000` | `{0, -2500, 2000, -1000},` | Rectangle 2, x_max=2000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 11 | `1000` | `{0, -2500, 2000, -1000},` | Rectangle 2, y_max=-1000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 12 | `0` | `{0, -2500, 1000, -1000},` | Rectangle 3, x_min=0: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 12 | `2500` | `{0, -2500, 1000, -1000},` | Rectangle 3, y_min=-2500: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 12 | `1000` | `{0, -2500, 1000, -1000},` | Rectangle 3, x_max=1000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 12 | `1000` | `{0, -2500, 1000, -1000},` | Rectangle 3, y_max=-1000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. |
| `effect_map_collision.c` | `initializers` | 13 | `1000` | `{1000, -10000, 2000, -15000},` | Rectangle 4, x_min=1000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 13 | `10000` | `{1000, -10000, 2000, -15000},` | Rectangle 4, y_min=-10000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `initializers` | 13 | `2000` | `{1000, -10000, 2000, -15000},` | Rectangle 4, x_max=2000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 13 | `15000` | `{1000, -10000, 2000, -15000},` | Rectangle 4, y_max=-15000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `initializers` | 14 | `0` | `{0, -10000, 2000, -15000},` | Rectangle 5, x_min=0: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 14 | `10000` | `{0, -10000, 2000, -15000},` | Rectangle 5, y_min=-10000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `initializers` | 14 | `2000` | `{0, -10000, 2000, -15000},` | Rectangle 5, x_max=2000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 14 | `15000` | `{0, -10000, 2000, -15000},` | Rectangle 5, y_max=-15000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `initializers` | 15 | `0` | `{0, -10000, 1000, -15000},` | Rectangle 6, x_min=0: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 15 | `10000` | `{0, -10000, 1000, -15000},` | Rectangle 6, y_min=-10000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `initializers` | 15 | `1000` | `{0, -10000, 1000, -15000},` | Rectangle 6, x_max=1000: authored inclusive boundary along the orientation-selected cell axis, in world units. This is a geometry datum, not a distinct state or tuning identity. |
| `effect_map_collision.c` | `initializers` | 15 | `15000` | `{0, -10000, 1000, -15000},` | Rectangle 6, y_max=-15000: authored inclusive floor-relative world-Y boundary. The sign belongs to the initializer expression; no independent map-location identity is established. Rows 4..6 have reversed vertical bounds; preserve them rather than normalizing the interval. |
| `effect_map_collision.c` | `effect_map_collision` | 49 | `0` | `if (x < 0 \|\| x >= KF_MAP_COLUMNS \|\| z < 0 \|\| z >= KF_MAP_ROWS) {` | Zero is the lower grid-coordinate bound for X/Z before map access; the upper dimensions are named separately. |
| `effect_map_collision.c` | `effect_map_collision` | 49 | `0` | `if (x < 0 \|\| x >= KF_MAP_COLUMNS \|\| z < 0 \|\| z >= KF_MAP_ROWS) {` | Zero is the lower grid-coordinate bound for X/Z before map access; the upper dimensions are named separately. |
| `effect_map_collision.c` | `effect_map_collision` | 60 | `0` | `if (height < 0) {` | The sign distinguishes floor-relative scalar heights from nonnegative rectangle-table indices; zero selects rectangle zero. |
| `effect_map_collision.c` | `effect_map_collision` | 99 | `1` | `if (((map_collision_grid[z + 1][x] != KF_MAP_CELL_FLOOR` | One-cell step to the next Z neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 100 | `1` | `&& map_collision_grid[z + 1][x] != KF_MAP_CELL_STEP)` | One-cell step to the next Z neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 102 | `1` | `((map_collision_grid[z - 1][x] != KF_MAP_CELL_FLOOR` | One-cell step to the previous Z neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 103 | `1` | `&& map_collision_grid[z - 1][x] != KF_MAP_CELL_STEP)` | One-cell step to the previous Z neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 105 | `1` | `((map_collision_grid[z][x + 1] != KF_MAP_CELL_FLOOR` | One-cell step to the next X neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 106 | `1` | `&& map_collision_grid[z][x + 1] != KF_MAP_CELL_STEP)` | One-cell step to the next X neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 108 | `1` | `if (map_collision_grid[z][x - 1] != KF_MAP_CELL_FLOOR` | One-cell step to the previous X neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 109 | `1` | `&& map_collision_grid[z][x - 1] != KF_MAP_CELL_STEP) {` | One-cell step to the previous X neighbor for the existing blocked-cell shape test; preserve repeated floor/step predicates and short-circuit order. |
| `effect_map_collision.c` | `effect_map_collision` | 146 | `0` | `return collision_query_world(position->vx, position->vy, position->vz, radius, 0,` | Zero supplies the height argument to the actor/player collision query after map checks; radius and target-exclusion flags remain independent. |
| `effect_map_collision.c` | `effect_map_collision` | 150 | `0` | `return collision_query_world(position->vx, position->vy, position->vz, radius, 0,` | Zero supplies the height argument to the actor/player collision query after map checks; radius and target-exclusion flags remain independent. |
| `effect_map_collision.c` | `effect_map_collision` | 154 | `0` | `return collision_query_world(position->vx, position->vy, position->vz, radius, 0,` | Zero supplies the height argument to the actor/player collision query after map checks; radius and target-exclusion flags remain independent. |
| `effect_map_collision.c` | `effect_map_collision` | 158 | `1` | `return 1;` | Raw fallback when the low target-selector bits are zero. It differs from KF_COLLISION_NONE and has no actor/player class bits; the original result identity is unresolved, so neither boolean success nor a known collision class is invented. |
