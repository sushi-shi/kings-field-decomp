# Effect-dispatch remainder literal ledger

Complete per-occurrence account of **133 retained numeric literals** in
`src/game/effect_dispatch.c`.
Named definitions and retail claims are excluded. Comments, strings and identifier
digits are not numeric literals; signs are operators. The
[dispatcher review](game-effect-dispatch-constants.md) explains named units and verification.

| Function | Line | Token | Expression | Reason |
| --- | --- | --- | --- | --- |
| `initializers` | 136 | `0` | `{0, SHORT_SWING_PROBE_LENGTH, 0, 0},` | Zero local X keeps this authored collision probe on the local Y axis. |
| `initializers` | 136 | `0` | `{0, SHORT_SWING_PROBE_LENGTH, 0, 0},` | Zero local Z keeps this authored collision probe on the local Y axis. |
| `initializers` | 136 | `0` | `{0, SHORT_SWING_PROBE_LENGTH, 0, 0},` | Zero SDK SVECTOR pad halfword is part of the initialized eight-byte probe payload. |
| `initializers` | 137 | `0` | `{0, LONG_SWING_PROBE_LENGTH, 0, 0},` | Zero local X keeps this authored collision probe on the local Y axis. |
| `initializers` | 137 | `0` | `{0, LONG_SWING_PROBE_LENGTH, 0, 0},` | Zero local Z keeps this authored collision probe on the local Y axis. |
| `initializers` | 137 | `0` | `{0, LONG_SWING_PROBE_LENGTH, 0, 0},` | Zero SDK SVECTOR pad halfword is part of the initialized eight-byte probe payload. |
| `effect_update_dispatch` | 197 | `1` | `&impact_magic->sounds[1], &effect->position, KF_AUDIO_MAX_VOLUME);` | Second magic sound slot supplies impact playback; fixed-row selection remains separately named where applicable. |
| `effect_update_dispatch` | 203 | `0` | `impact_magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 204 | `2` | `impact_magic->damage_components[2],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 205 | `1` | `impact_magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 206 | `0` | `0, 0, KF_ACTOR_DAMAGE_SCALE_ONE, effect->type);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 206 | `0` | `0, 0, KF_ACTOR_DAMAGE_SCALE_ONE, effect->type);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 210 | `0` | `0, 0, 0, impact_magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 210 | `0` | `0, 0, 0, impact_magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 210 | `0` | `0, 0, 0, impact_magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 210 | `0` | `0, 0, 0, impact_magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 211 | `1` | `impact_magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 220 | `0` | `impact_magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 221 | `2` | `impact_magic->damage_components[2],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 222 | `1` | `impact_magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 223 | `0` | `0, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 223 | `0` | `0, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 223 | `0` | `0, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 226 | `0` | `0, 0, 0, KF_PLAYER_STATUS_DARKNESS, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 226 | `0` | `0, 0, 0, KF_PLAYER_STATUS_DARKNESS, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 226 | `0` | `0, 0, 0, KF_PLAYER_STATUS_DARKNESS, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 226 | `0` | `0, 0, 0, KF_PLAYER_STATUS_DARKNESS, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 226 | `0` | `0, 0, 0, KF_PLAYER_STATUS_DARKNESS, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 229 | `0` | `0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 229 | `0` | `0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 229 | `0` | `0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 229 | `0` | `0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 229 | `0` | `0, 0, 0, KF_PLAYER_STATUS_POISON, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 231 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_CURSE, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 231 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_CURSE, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 231 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_CURSE, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 231 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_CURSE, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 231 | `0` | `player_apply_damage(0, 0, 0, KF_PLAYER_STATUS_CURSE, 0, 0, KF_FIXED12_ONE, effect->id);` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 234 | `0` | `0, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 234 | `0` | `0, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 234 | `0` | `0, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 234 | `0` | `0, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 235 | `0` | `impact_magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 236 | `1` | `impact_magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 245 | `0` | `effect->direction.words.y = 0;` | Begin the emerging projectile fall with zero vertical velocity before subsequent acceleration. |
| `effect_update_dispatch` | 268 | `1` | `remaining = effect->control.frames_remaining - 1;` | Decrement the Lightning Bolt halfword travel countdown by one before testing exhaustion. |
| `effect_update_dispatch` | 270 | `0` | `if ((u16)remaining == 0) {` | Zero is exhaustion of the updated halfword countdown; retain the decrement and storage order. |
| `effect_update_dispatch` | 273 | `1` | `&magic->sounds[1], &effect->position, KF_AUDIO_MAX_VOLUME);` | Second magic sound slot supplies impact playback; fixed-row selection remains separately named where applicable. |
| `effect_update_dispatch` | 291 | `1` | `effect->base_render_id + ((u8)effect->control.frames_remaining & 1);` | Countdown parity alternates the two Lightning Bolt billboard entries; one is the low-bit mask. |
| `effect_update_dispatch` | 299 | `0` | `if (--effect->control.frames_remaining == 0) {` | Zero is exhaustion of the updated halfword countdown; retain the decrement and storage order. |
| `effect_update_dispatch` | 300 | `0` | `if (effect->propagation.generations_remaining != 0) {` | Zero means no further scatter descendants, selecting the final countdown or freeing the expired slot. |
| `effect_update_dispatch` | 301 | `3` | `next = ((s16)effect->scale_x * 3) >> 2;` | Multiply by three and divide by four before halfword storage: descendants retain three quarters of the previous scale. Keep this simple fraction and its rounding explicit. |
| `effect_update_dispatch` | 301 | `2` | `next = ((s16)effect->scale_x * 3) >> 2;` | Multiply by three and divide by four before halfword storage: descendants retain three quarters of the previous scale. Keep this simple fraction and its rounding explicit. |
| `effect_update_dispatch` | 309 | `0` | `if (effect->propagation.generations_remaining == 0) {` | Zero means no further scatter descendants, selecting the final countdown or freeing the expired slot. |
| `effect_update_dispatch` | 325 | `1` | `value = (effect->visual.pulse_base_scale * value) >> (KF_FIXED12_BITS + 1);` | One extra shift beyond Q12 halves the sine/cosine pulse amplitude; direct fixed-point arithmetic, not an RNG extraction. |
| `effect_update_dispatch` | 331 | `1` | `+ ((effect->visual.pulse_base_scale * value) >> (KF_FIXED12_BITS + 1));` | One extra shift beyond Q12 halves the sine/cosine pulse amplitude; direct fixed-point arithmetic, not an RNG extraction. |
| `effect_update_dispatch` | 339 | `0` | `if (rand() < MAP_EMITTER_SOUND_RANDOM_CUTOFF && effect->sound_played == 0) {` | Zero means sound has not been accepted; the preceding rand call still occurs first even when this flag is already nonzero. |
| `effect_update_dispatch` | 341 | `0` | `&magic->sounds[0], &effect->position, KF_AUDIO_MAX_VOLUME,` | First magic sound slot supplies travel/emergence playback; retain the positional sound-record mapping. |
| `effect_update_dispatch` | 369 | `0` | `phase_sound = &magic->sounds[0];` | First magic sound slot supplies travel/emergence playback; retain the positional sound-record mapping. |
| `effect_update_dispatch` | 389 | `0` | `if ((s16)effect->scale_y < 0) {` | Negative signed halfword Y scale terminates the status-projectile shrink; zero itself remains live. |
| `effect_update_dispatch` | 399 | `1` | `if (phase < MOONLIGHT_TRAVEL_LAST + 1) {` | One expresses the exclusive bound including the last moving/holding phase. |
| `effect_update_dispatch` | 406 | `1` | `&magic_records[KF_ENUM_ENCODE(u8, KF_EFFECT_KIND_RADIAL_BLAST)].sounds[1], &effect->position, KF_AUDIO_MAX_VOLUME);` | Second magic sound slot supplies impact playback; fixed-row selection remains separately named where applicable. |
| `effect_update_dispatch` | 413 | `0` | `if ((s16)effect->scale_z < 0) {` | The signed halfword sign test detects length overflow and applies the named maximum clamp. |
| `effect_update_dispatch` | 426 | `1` | `if (((phase - MOONLIGHT_IMPACT_FIRST) & 1) == 0) {` | Mask one and compare with zero to emit on even offsets from impact start; the test precedes terminal-phase invalidation. |
| `effect_update_dispatch` | 426 | `0` | `if (((phase - MOONLIGHT_IMPACT_FIRST) & 1) == 0) {` | Mask one and compare with zero to emit on even offsets from impact start; the test precedes terminal-phase invalidation. |
| `effect_update_dispatch` | 429 | `1` | `&effect->position, &effect->direction.vector, 1);` | True requests the radial-blast constructor sound via its sixth variadic slot; not a phase or actor identifier. |
| `effect_update_dispatch` | 450 | `0` | `(u16)collision, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 450 | `0` | `(u16)collision, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 450 | `0` | `(u16)collision, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 451 | `0` | `magic->damage_components[0], magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 451 | `1` | `magic->damage_components[0], magic->damage_components[1],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 463 | `1` | `if (linked_effect->phase > MOONLIGHT_IMPACT_FIRST - 1) {` | One converts the parent impact threshold into the original strict comparison; trail shrink begins when the parent reaches that phase. |
| `effect_update_dispatch` | 472 | `0` | `if ((s16)scale <= 0) {` | A nonpositive signed halfword scale retires the ground trail; preserve the inclusive zero boundary. |
| `effect_update_dispatch` | 493 | `1` | `if (effect->phase & 1) {` | Oddness of the preincremented radial-blast phase gates damage; radius still uses the old local phase. |
| `effect_update_dispatch` | 497 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 497 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 497 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 497 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 498 | `1` | `magic->damage_components[1], KF_ACTOR_DAMAGE_SCALE_ONE, effect->type);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 502 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 502 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 502 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 502 | `0` | `0, 0, 0, magic->damage_components[0],` | The first three zeros omit physical damage channels; the bracketed zero selects the first magic component for the later channel. Preserve argument order and positional resource mapping. |
| `effect_update_dispatch` | 503 | `1` | `magic->damage_components[1], EFFECT_PLAYER_RADIAL_SCALE_Q12, effect->id);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 516 | `0` | `if (phase == 0) {` | The initial age randomizes homing direction. This is the origin of a counter that later tracks and can wrap, not a separate lifecycle field. |
| `effect_update_dispatch` | 544 | `0` | `if (target != 0) {` | Null is the actor-cone query miss; only an existing actor provides position and height. |
| `effect_update_dispatch` | 554 | `1` | `- (definition->collision_height >> 1)),` | Halve actor collision height to aim at its vertical midpoint; retain the directly stated geometric fraction. |
| `effect_update_dispatch` | 558 | `0` | `effect->direction.words.x = 0;` | A missing homing actor target requests level pitch; zero is an angle origin, not a target index. |
| `effect_update_dispatch` | 568 | `0` | `local_motion.vy = 0;` | Zero local X/Y leaves only the named forward displacement before pitch/yaw transforms. |
| `effect_update_dispatch` | 569 | `0` | `local_motion.vx = 0;` | Zero local X/Y leaves only the named forward displacement before pitch/yaw transforms. |
| `effect_update_dispatch` | 587 | `1` | `&effect->position, &effect->direction.vector, 1);` | True requests the radial-blast constructor sound via its sixth variadic slot; not a phase or actor identifier. |
| `effect_update_dispatch` | 591 | `1` | `&effect->position, &effect->direction.vector, 1);` | True requests the radial-blast constructor sound via its sixth variadic slot; not a phase or actor identifier. |
| `effect_update_dispatch` | 617 | `1` | `phase_sound = &magic_records[KF_ENUM_ENCODE(u8, KF_MAGIC_LIGHTNING_BOLT)].sounds[1];` | Second magic sound slot supplies impact playback; fixed-row selection remains separately named where applicable. |
| `effect_update_dispatch` | 632 | `1` | `if (phase & 1) {` | Oddness of the old lightning-blast phase gates damage; the shared increment happens afterward. |
| `effect_update_dispatch` | 643 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 643 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 643 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 644 | `0` | `lightning_magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 645 | `1` | `lightning_magic->damage_components[1], KF_ACTOR_DAMAGE_SCALE_ONE, effect->type);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 647 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 647 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 647 | `0` | `&position, damage_radius, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 648 | `0` | `lightning_magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 649 | `1` | `lightning_magic->damage_components[1], EFFECT_PLAYER_RADIAL_SCALE_Q12, effect->id);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 663 | `0` | `if (effect->control.frames_remaining-- == 0) {` | Test the old branch countdown at zero before marking its timer done; preserve postdecrement scheduling. |
| `effect_update_dispatch` | 685 | `1` | `if (phase == GROUND_BRANCH_GROW_END - 1) {` | One selects the last growth update before assigning the role-dependent hold base and applying the common increment. |
| `effect_update_dispatch` | 700 | `1` | `if ((phase & (GROUND_BRANCH_DAMAGE_PERIOD - 1)) == 0) {` | Subtract one from the power-of-two period to form its low-bit mask; zero selects phases divisible by four. |
| `effect_update_dispatch` | 700 | `0` | `if ((phase & (GROUND_BRANCH_DAMAGE_PERIOD - 1)) == 0) {` | Subtract one from the power-of-two period to form its low-bit mask; zero selects phases divisible by four. |
| `effect_update_dispatch` | 716 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 716 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 716 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 717 | `0` | `magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 718 | `1` | `magic->damage_components[1], KF_ACTOR_DAMAGE_SCALE_ONE, effect->type);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 721 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 721 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 721 | `0` | `GROUND_BRANCH_DAMAGE_RADIUS, KF_FIXED12_ONE, power, 0, 0, 0,` | Zero damage arguments omit channels or status on this specific call. Named actor/player scales and any nonzero status preserve their separate argument positions. |
| `effect_update_dispatch` | 722 | `0` | `magic->damage_components[0],` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 723 | `1` | `magic->damage_components[1], EFFECT_PLAYER_RADIAL_SCALE_Q12, effect->id);` | Magic component array ordinal: physical calls preserve the 0,2,1 mapping, while magic-channel calls use 0,1. No unsupported damage-channel identity is inferred from an ordinal. |
| `effect_update_dispatch` | 755 | `1` | `} else if (phase < ACTOR_SPAWNER_TRAVEL_LAST + 1) {` | One expresses the exclusive bound including the phase that holds movement until collision or countdown exhaustion. |
| `effect_update_dispatch` | 762 | `0` | `position.x, position.y, position.z, ACTOR_SPAWNER_COLLISION_RADIUS, 0,` | Zero probe height passes the existing horizontal movement query contract; the radius and object/event exclusions are separate. |
| `effect_update_dispatch` | 764 | `0` | `if ((phase == ACTOR_SPAWNER_TRAVEL_LAST && value != KF_COLLISION_NONE) \|\| effect->control.frames_remaining == 0) {` | Exhausted movement countdown requests the actor-spawner waiting phase independently of the last-travel-phase collision condition. |
| `effect_update_dispatch` | 783 | `0` | `actor_rotation.x = 0;` | Zero pitch/roll spawns an upright actor; yaw is separately aimed at the player. |
| `effect_update_dispatch` | 784 | `0` | `actor_rotation.z = 0;` | Zero pitch/roll spawns an upright actor; yaw is separately aimed at the player. |
| `effect_update_dispatch` | 790 | `2` | `actor_pool_spawn(2, &position, &actor_rotation);` | Current-floor actor definition slot 2; two separate conditional random draws choose slots 2,4,0. Creature identities are not established by these local resource indices. |
| `effect_update_dispatch` | 792 | `4` | `actor_pool_spawn(4, &position, &actor_rotation);` | Current-floor actor definition slot 4; two separate conditional random draws choose slots 2,4,0. Creature identities are not established by these local resource indices. |
| `effect_update_dispatch` | 794 | `0` | `actor_pool_spawn(0, &position, &actor_rotation);` | Current-floor actor definition slot 0; two separate conditional random draws choose slots 2,4,0. Creature identities are not established by these local resource indices. |
| `effect_update_dispatch` | 833 | `1` | `remaining = effect->direction.words.x - 1;` | Advance the signed halfword floor-deformation sweep countdown by one toward its minus-one terminator. |
| `effect_update_dispatch` | 835 | `1` | `if ((s16)remaining == -1) {` | Minus one terminates the predecremented floor-deformation sweep; keep its signed-halfword comparison. |
| `effect_update_dispatch` | 840 | `0` | `for (column = 0; column < effect->rotation.vy; column++) {` | Start the floor-segment ordinal at zero; the selected first segment and segment count are stored separately. |
| `effect_update_dispatch` | 848 | `1` | `if (--effect->position.vy != -1) {` | Minus one terminates the floor-deformation hold countdown after predecrement; this word is a timer on this effect kind. |
| `effect_update_dispatch` | 856 | `1` | `remaining = effect->direction.words.x - 1;` | Advance the signed halfword floor-deformation sweep countdown by one toward its minus-one terminator. |
| `effect_update_dispatch` | 858 | `1` | `if ((s16)remaining == -1) {` | Minus one terminates the predecremented floor-deformation sweep; keep its signed-halfword comparison. |
| `effect_update_dispatch` | 864 | `0` | `for (column = 0; column < effect->rotation.vy; column++) {` | Start the floor-segment ordinal at zero; the selected first segment and segment count are stored separately. |
