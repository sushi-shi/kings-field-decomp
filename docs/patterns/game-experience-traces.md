# GAME experience-loop compiler traces

## Function Match Plan

Resume after world-query closure at `030e6ae`. GAME
`80016058 player_add_experience`, 548 retail bytes in `game.player_death`,
remains **88.824814%**, 528 compiled bytes. Fresh native and traced whole ELF
objects agree for source SHA-256
`af506a010d2338d75b3fb02ec450a6270be8b9a316a4083c2f029e32ad9cdd92`.
Compiler fingerprint remains
`222b6cc36272847ffde09ad4b7f2db81632fe7fd282bfc6df1f155939b63cb12`.
Generated evidence lives under `build/gcc257/game-experience-traces/`.

The six GAME semantic views, all 137 retail words and the fresh candidate,
sole actor-damage call window, three callee contracts/bodies, neighboring
magic-training and damage-formula bodies, complete player/growth types,
resource-owner evidence, history and the previous
[loop/address campaign](game-experience-loop-addresses.md) were reviewed.
The signed-halfword argument is extended on entry; its caller loads the
definition's unsigned reward at +132. Preserve the existing boundary and
void return. The eighteen-block retail CFG has nine conditional branches,
three proven calls, one validated internal jump and twelve validated address
pairs, with no strings or candidate/indirect outgoing references. Game
progression policy and player/growth ownership exclude an SDK body.

The first divergence remains the extra `s2` save at entry. That register holds
growth row 39 across the loop, replacing eight independent retail field
references. Retail saves only `s0`, `s1` and `ra` in its 32-byte frame; its
two persistent player addresses start at +8 and +16. Preserve the signed
99999 total cap, duplicated byte-level reads, level-255 exit, previous-level
40 test, twelve-byte growth stride, eight terminal-row field references,
halfword stores before the four caps, and all three calls per completed
increase. The twelve sibling functions are strict and resolved-word exact.

Earlier structured-loop and complete/subobject-pointer experiments are
negative controls, not starting points to repeat. A label loop recovered the
independent growth addresses but lost the player anchors; a complete player
pointer recovered the address list while retaining the wrong root and frame.

First test scalar pointers to the actual level byte and maximum-HP halfword,
initialized before an explicitly guarded backedge and used only for those
fields. These are the first dereferenced fields at the two retail anchors;
the previous experiment bound whole progress/vitals subobjects. Keep every
other access through its complete player owner. No pointer arithmetic outside
a scalar, overlapping global, shifted aggregate view or volatile access is
allowed. Compare the initial address-value ownership and reuse through CSE
and the loop passes. A small control must reproduce any claimed distinction
between scalar and subobject pointer forms before interpreting it as a
mechanism. This is a hypothesis, not an assertion that the original C used
field pointers.

Retain only an independently supported source improvement that preserves all
twelve exact siblings and raw calls/referents. Require strict 100%, full
retail-word proof, native/debug/traced parity, focused and full production
builds, lint/tests and diff checks before banking a closure. No compiler
profile, inventory or SDK body changes are planned.

## Scalar-field control verdict

The field-pointer candidate is **81.357666%**, 628 bytes. It restores the
retail save set and independently materializes the eight terminal growth
fields, but emits **31** ordered address pairs instead of twelve. The two
player pointers remain usable only for their selected scalar fields; other
player accesses are repeatedly absolute. Their saved-register roles also
reverse relative to retail. This reproduces the broader limitation of the
earlier subobject-pointer controls, without justifying a new source owner.

All three calls and all twelve exact sibling bodies remain unchanged and
raw-exact. Native, traced and uninstrumented debug whole ELF objects agree
for both the baseline and scalar-field control. Source hash is
`3d52c6777ecfc47ae5cd9eadd059ed6274aef2db10f324d23e13e5abfd647bd8`.
The candidate is rejected; production remains 88.824814%, with no bank
entry. No scalar-versus-subobject optimizer mechanism is claimed from this
negative result, so no synthetic control is used to turn it into one.
