# Save-panel and confirmation literal ledger

Complete ledger for `menu_two_option_prompt`, `menu_save_load_hub`,
`menu_save_panel` and `menu_load_panel`: **140 retained occurrences**,
down from 213. Named constant definitions and retail claims are excluded;
the separate item-drop function is outside this ledger. Negative signs
are operators, so a negative number contributes one numeric token.
See the [result-domain review](game-menu-save-results.md).

| Function | Lines | Tokens | Expression | Reason |
| --- | --- | --- | --- | --- |
| `menu_two_option_prompt` | 19 | `0` | `s32 selected = 0;` | Zero/one select the first/second footer label, はい/いいえ (yes/no). Negating this two-row index constructs the accepted/cancelled result; keep row position distinct from the result enum. |
| `menu_two_option_prompt` | 20 | `0` | `s32 highlight = 0;` | Boolean footer highlight starts and resets off; confirmation sets it for the final presented frame before the release wait. |
| `menu_two_option_prompt` | 21 | `1` | `s32 composite = -1;` | No highlighted composite save frame for non-save/load windows; the negative row index is separate from cancellation and is replaced by highlight_row only for the named save/load kinds. |
| `menu_two_option_prompt` | 22 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_two_option_prompt` | 26, 51 | `1 × 2, 0 × 2` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_two_option_prompt` | 32 | `0x60` | `label_a.x = 0x60;` | Both yes/no labels use the authored X origin 96 screen pixels; no recovered layout rationale selects this anchor. |
| `menu_two_option_prompt` | 33 | `20, 44` | `label_a.y = count * 20 + 44;` | Place footer labels after count twenty-pixel window rows, at baselines count*20+44 and count*20+64. The footer labels are twenty pixels apart; for the three/five/four-row callers their Y pairs are 104/124, 144/164 and 124/144. Preserve the authored offsets. |
| `menu_two_option_prompt` | 34 | `0, 0x59` | `label_a.codes[0] = 0x59;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 35 | `1, 0x41` | `label_a.codes[1] = 0x41;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 36 | `2` | `label_a.codes[2] = MENU_TEXT_END;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 37 | `0x60` | `label_b.x = 0x60;` | Both yes/no labels use the authored X origin 96 screen pixels; no recovered layout rationale selects this anchor. |
| `menu_two_option_prompt` | 38 | `20, 64` | `label_b.y = count * 20 + 64;` | Place footer labels after count twenty-pixel window rows, at baselines count*20+44 and count*20+64. The footer labels are twenty pixels apart; for the three/five/four-row callers their Y pairs are 104/124, 144/164 and 124/144. Preserve the authored offsets. |
| `menu_two_option_prompt` | 39 | `0, 0x41` | `label_b.codes[0] = 0x41;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 40 | `1, 0x41` | `label_b.codes[1] = 0x41;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 41 | `2, 0x43` | `label_b.codes[2] = 0x43;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 42 | `3` | `label_b.codes[3] = MENU_TEXT_END;` | Consecutive glyph/terminator positions spell はい (0x59,0x41) and いいえ (0x41,0x41,0x43) in the retail atlas. Keep the authored character codes and terminate immediately after each label. |
| `menu_two_option_prompt` | 48, 77 | `1 × 2` | `menu_draw_window(kind, count, highlight_row, 1);` | Boolean one keeps the underlying chosen window row highlighted while the yes/no footer is active and on its final frame. |
| `menu_two_option_prompt` | 56 | `0` | `highlight = 0;` | Boolean footer highlight starts and resets off; confirmation sets it for the final presented frame before the release wait. |
| `menu_two_option_prompt` | 58 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_two_option_prompt` | 59 | `0 × 2` | `if (((input & PADLup) != 0 && (prev & PADLup) == 0) \|\|` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_two_option_prompt` | 60 | `0 × 2` | `((input & PADLdown) != 0 && (prev & PADLdown) == 0)) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_two_option_prompt` | 63 | `0` | `selected = 0;` | Zero/one select the first/second footer label, はい/いいえ (yes/no). Negating this two-row index constructs the accepted/cancelled result; keep row position distinct from the result enum. |
| `menu_two_option_prompt` | 65 | `1` | `selected = 1;` | Zero/one select the first/second footer label, はい/いいえ (yes/no). Negating this two-row index constructs the accepted/cancelled result; keep row position distinct from the result enum. |
| `menu_two_option_prompt` | 66 | `0 × 2` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_two_option_prompt` | 68 | `1` | `highlight = 1;` | Boolean footer highlight starts and resets off; confirmation sets it for the final presented frame before the release wait. |
| `menu_two_option_prompt` | 70 | `0 × 2` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_load_hub` | 176 | `0` | `s32 confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_load_hub` | 177 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_save_load_hub` | 180 | `1` | `s32 action = -1;` | No pending System row action. This signed index sentinel is distinct from the root result channel and is reset before the result-exit guard. |
| `menu_save_load_hub` | 186 | `1` | `if (action != -1 \|\| result == action) {` | Redraw the selected frame when an action is pending or the root result equals the no-action index (-1). The latter covers the exit frame; keep this cross-value comparison. |
| `menu_save_load_hub` | 190 | `1, 0` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_save_load_hub` | 202 | `0` | `KF_MENU_WINDOW_SYSTEM, KF_MENU_SYSTEM_ROW_COUNT, cursor, 0));` | Null save-summary pointer: the System quit prompt has no slot catalogue to display. |
| `menu_save_load_hub` | 218 | `1` | `if (action != -1 && result == KF_MENU_ROOT_NO_ITEM)` | A sub-action returning no item/cancellation keeps the System hub open only when a row action was pending. The -1 operand tests the action index, not the named result. |
| `menu_save_load_hub` | 220 | `1` | `action = -1;` | No pending System row action. This signed index sentinel is distinct from the root result channel and is reset before the result-exit guard. |
| `menu_save_load_hub` | 224 | `0` | `confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_load_hub` | 226 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_save_load_hub` | 227 | `0 × 2` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_load_hub` | 229 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_save_load_hub` | 233 | `0 × 2` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_load_hub` | 239 | `0 × 2` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_load_hub` | 241 | `1` | `confirm = 1;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_load_hub` | 247 | `0 × 2` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_panel` | 271 | `0` | `s32 cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_save_panel` | 272 | `0` | `s32 confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_panel` | 273 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_save_panel` | 280 | `1, 3` | `if (status != 1 && status != 3) {` | Retain the save panel's exact accepted catalogue values one and three. The reviewed header wrapper normalizes raw status three to one, so this comparison does not prove a distinct reachable catalogue-result meaning for three. Do not rename it to no-data or silently remove the extra acceptance. |
| `menu_save_panel` | 282, 310, 351 | `1 × 3, 0 × 3` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_save_panel` | 290, 301, 319, 359 | `1 × 4, 0 × 4` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_save_panel` | 296 | `1` | `if (confirm == 1 \|\| result == KF_MENU_CONFIRM_CANCELLED) {` | Enter the pending confirmation/redraw path only for the exact Boolean value one; the named result and row operands retain their separate domains. |
| `menu_save_panel` | 305 | `1` | `if (confirm == 1 && cursor != KF_MENU_SAVE_RETURN_ROW) {` | Enter the pending confirmation/redraw path only for the exact Boolean value one; the named result and row operands retain their separate domains. |
| `menu_save_panel` | 308 | `1` | `if (status == 1) {` | Temporary-file cleanup reports one when open succeeded (file != -1), after close/erase calls. This gates the decoded existing-game-data formatting question; it does not independently prove that erase succeeded. |
| `menu_save_panel` | 329, 339 | `0 × 2, 3 × 2` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_save_panel` | 336 | `1` | `status = save_system_write_slot(cursor + 1);` | Convert zero-based visible slot index to the one-based on-card slot ID; the unit adjustment is the encoding boundary. |
| `menu_save_panel` | 346 | `1` | `status = memory_card_check_or_format(1);` | Boolean allow_format is enabled because the user has accepted the separate confirmation; the card helper has its own normalized result protocol. |
| `menu_save_panel` | 347 | `0` | `memset(summaries, 0, sizeof(summaries));` | Zero every local summary after the format attempt, preserving the original unconditional clearing before its status check. |
| `menu_save_panel` | 350 | `1` | `if (status != 1) {` | Only normalized operation result one counts as success. Every other write/format result returns the panel to pending after displaying its existing status message; this is not confirmation result zero. |
| `menu_save_panel` | 368 | `0` | `confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_panel` | 373 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_save_panel` | 374 | `0 × 2` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_panel` | 376 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_save_panel` | 380 | `0 × 2` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_panel` | 385 | `0` | `cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_save_panel` | 386 | `0 × 2` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_save_panel` | 388 | `1` | `confirm = 1;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_save_panel` | 391 | `0 × 2` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_load_panel` | 416 | `0` | `s32 cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_load_panel` | 417 | `0` | `s32 confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_load_panel` | 418 | `0` | `s32 input = 0;` | Begin edge detection with no previous button bits. |
| `menu_load_panel` | 423 | `1` | `if (save_system_read_catalog(summaries) != 1) {` | The load panel accepts only normalized catalogue result one, preserving its narrower test than the save panel. The wrapper can map no-data to one, so this test alone does not prove a populated slot. |
| `menu_load_panel` | 425, 462 | `1 × 2, 0 × 2` | `while (PadRead(1) == 0) {` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_load_panel` | 433, 444, 470 | `1 × 3, 0 × 3` | `while (PadRead(1) != 0)` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_load_panel` | 439 | `1` | `if (confirm == 1 \|\| result == KF_MENU_CONFIRM_CANCELLED) {` | Enter the pending confirmation/redraw path only for the exact Boolean value one; the named result and row operands retain their separate domains. |
| `menu_load_panel` | 448 | `1` | `if (confirm == 1 && cursor != KF_MENU_LOAD_RETURN_ROW) {` | Enter the pending confirmation/redraw path only for the exact Boolean value one; the named result and row operands retain their separate domains. |
| `menu_load_panel` | 454 | `0, 3` | `for (i = 0; i < 3; i++) {` | Present exactly three frames of the loading/saving/formatting message before starting the operation. Zero initializes the draw counter; three is an authored draw count, independent of the three save slots. |
| `menu_load_panel` | 461 | `1 × 2` | `if (save_system_read_slot(cursor + 1) != 1) {` | Convert the zero-based row to a one-based file slot and require normalized read result one; all other outcomes keep the panel open after acknowledgement. |
| `menu_load_panel` | 477 | `0` | `confirm = 0;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_load_panel` | 482 | `1` | `input = PadRead(1);` | Preserve the ignored retail PadRead argument one; the linked SDK uses its global pad identifier. Zero button bits mean released: !=0 waits for release, ==0 redraws until acknowledgement, and assignment samples the next input edge. |
| `menu_load_panel` | 483 | `0 × 2` | `if ((input & PADLup) != 0 && (prev & PADLup) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_load_panel` | 485 | `0` | `if (cursor != 0)` | Zero is the first-row boundary for decrementing a zero-based cursor; the alternative wraps to the named last row. |
| `menu_load_panel` | 489 | `0 × 2` | `} else if ((input & PADLdown) != 0 && (prev & PADLdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_load_panel` | 494 | `0` | `cursor = 0;` | Start or wrap to the first zero-based save/load slot row. |
| `menu_load_panel` | 495 | `0 × 2` | `} else if ((input & PADRright) != 0 && (prev & PADRright) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
| `menu_load_panel` | 498, 504 | `1 × 2` | `confirm = 1;` | Boolean panel confirmation/highlight state: clear before sampling a new edge and set on the accepted button edge. Preserve its timing on return-row paths as well as actionable rows. |
| `menu_load_panel` | 500 | `0` | `} else if (summaries[cursor].current_hp == 0) {` | Reject a slot whose summary HP is zero. Preserve this actual test without treating positive HP as independent proof of a valid save file; the catalogue helper's partial initial clearing remains unchanged. |
| `menu_load_panel` | 506 | `0 × 2` | `} else if ((input & PADRdown) != 0 && (prev & PADRdown) == 0) {` | Current named button bit nonzero and previous bit zero define a rising edge. Preserve priority of vertical navigation, confirmation and cancellation. |
