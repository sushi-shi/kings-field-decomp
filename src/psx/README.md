# PSX.EXE

Bootstrap and overlay-loader reconstruction. This target has its own link graph
and objdiff project under `build/objdiff/psx`.

Provider-owned Psy-Q loader/runtime functions are excluded; only non-vendored
bootstrap logic belongs here.
