[package]
name = "kf-codec-bridge"
version = "0.1.0"
edition = "2021"
publish = false
[lib]
crate-type = ["staticlib"]
test = false
doctest = false
[dependencies]
kf-codec = { path = "../codecs" }
[profile.release]
panic = "abort"
overflow-checks = true
